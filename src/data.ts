export type Category = {
  id: string
  label: string
  description: string
}

export type SubjectSection = {
  heading: string
  paragraphs?: string[]
  bullets?: string[]
  tables?: SubjectTable[]
}

export type SubjectTable = {
  caption?: string
  columns: string[]
  rows: string[][]
}

export type SubjectLink = {
  label: string
  url: string
}

export type SubjectImage = {
  src: string
  alt: string
  caption: string
}

export type Subject = {
  slug: string
  title: string
  category: string
  summary: string
  level: 'Foundations' | 'Intermediate' | 'Advanced'
  minutes: number
  tags: string[]
  domain?: string
  takeaway: string
  diagram?: string[]
  image?: SubjectImage
  images?: SubjectImage[]
  codeLabel: string
  codeNote: string
  code: string
  codeLanguage?: string
  links?: SubjectLink[]
  sections: SubjectSection[]
}

export const categories: Category[] = [
  { id: 'java-runtime', label: 'Java runtime', description: 'The rules behind everyday Java behavior.' },
  { id: 'jvm-internals', label: 'JVM internals & GC', description: 'Class loading, JIT compilation, memory areas, allocation, and garbage collection.' },
  { id: 'modern-java', label: 'Streams & modern Java', description: 'Streams, records, Optional, and language features that shape current Java code.' },
  { id: 'error-handling', label: 'Exceptions & error handling', description: 'Failure contracts, exception boundaries, resource cleanup, and recovery decisions.' },
  { id: 'design-patterns', label: 'Design patterns', description: 'Small structures for keeping code changeable.' },
  { id: 'kubernetes', label: 'Kubernetes', description: 'Cluster components, API objects, storage, networking, and manifests.' },
  { id: 'containers-git-architecture', label: 'Containers, Git & architecture', description: 'Document-derived notes on Docker, Git workflows, metadata, and service design.' },
  { id: 'collections-caching', label: 'Collections & caching', description: 'Fast access, eviction, and data organization.' },
  { id: 'data-structures', label: 'Data structures', description: 'The shapes your data takes in memory.' },
  { id: 'algorithms', label: 'Algorithms', description: 'Repeatable ways to solve common problems.' },
  { id: 'concurrency', label: 'Concurrency & async', description: 'Multiple tasks, event loops, and non-blocking work.' },
  { id: 'backend-tools', label: 'Event streaming', description: 'Durable logs, topics, and stateful stream processors.' },
  { id: 'data-services', label: 'Data services & OLAP', description: 'Fast state, real-time analytics, and columnar query engines.' },
  { id: 'edge-delivery', label: 'Edge delivery', description: 'Routing content close to users without moving the origin everywhere.' },
  { id: 'lakehouse-formats', label: 'Lakehouse & formats', description: 'Reliable tables and efficient files on object storage.' },
  { id: 'testing-engineering', label: 'Testing & engineering', description: 'Confidence before production finds you.' },
  { id: 'system-design', label: 'System design', description: 'Patterns for systems that have to keep working.' },
  { id: 'api-clients', label: 'REST clients', description: 'Synchronous, asynchronous, reactive, declarative, and low-level HTTP clients.' },
]

export const subjects: Subject[] = [
  {
    slug: 'equals-and-hashcode',
    title: 'equals() and hashCode()',
    category: 'java-runtime',
    summary: 'Make objects behave correctly when Java compares them or stores them in hash-based collections.',
    level: 'Foundations',
    minutes: 6,
    tags: ['objects', 'HashMap', 'contracts'],
    takeaway: 'If two objects are equal, they must have the same hash code.',
    codeLabel: 'User.java',
    codeNote: 'Use the same fields in both methods, and prefer immutable identity fields.',
    code: `import java.util.Objects;\n\nrecord User(String email) {\n    @Override\n    public boolean equals(Object other) {\n        return other instanceof User user\n            && Objects.equals(email, user.email);\n    }\n\n    @Override\n    public int hashCode() {\n        return Objects.hash(email);\n    }\n}`,
    sections: [
      { heading: 'The simple rule', paragraphs: ['equals() answers whether two values represent the same thing. hashCode() produces a bucket number used by HashMap, HashSet, and related collections.', 'Java collections use hashCode() first, then equals() when multiple values land in the same bucket.'] },
      { heading: 'The contract', bullets: ['equals() should be reflexive, symmetric, transitive, consistent, and false for null.', 'Equal objects must return the same hash code.', 'Do not use mutable fields for identity while an object is inside a hash collection.'] },
      { heading: 'When it bites', paragraphs: ['A mutable key can become impossible to find after its hash code changes. That bug is quiet, legal, and irritating, which is a very Java combination.'] },
    ],
  },
  {
    slug: 'java-memory-model',
    title: 'Java Memory Model',
    category: 'java-runtime',
    summary: 'Understand when one thread is guaranteed to see a value written by another thread.',
    level: 'Advanced',
    minutes: 9,
    tags: ['threads', 'visibility', 'volatile', 'happens-before'],
    takeaway: 'Visibility and ordering are guarantees, not guesses about what the CPU will probably do.',
    diagram: ['Thread A writes data', 'release creates happens-before', 'Thread B reads data'],
    codeLabel: 'Publisher.java',
    codeNote: 'The volatile publication flag makes the earlier data write visible to the reader. It does not make compound updates atomic.',
    code: `final class Publisher {\n    private int value;\n    private volatile boolean published;\n\n    void publish(int nextValue) {\n        value = nextValue;\n        published = true;\n    }\n\n    int read() {\n        while (!published) {\n            Thread.onSpinWait();\n        }\n        return value;\n    }\n}`,
    sections: [
      { heading: 'What it solves', paragraphs: ['Modern CPUs and compilers reorder work for speed. The Java Memory Model defines which reads and writes are visible between threads.', 'A happens-before relationship is an edge in that model. If action A happens-before action B, then B must be ordered after A and can see the effects that A made visible. It is a correctness guarantee, not a claim that A finished earlier on a wall clock.'] },
      { heading: 'Happens-before in plain English', paragraphs: ['Imagine Thread A writes a message and then sets a volatile published flag. Thread B waits until it reads that flag as true. The volatile write happens-before the matching volatile read, so Thread B is allowed to see the earlier message write as well.', 'Without a happens-before edge, seeing the write is not guaranteed, even if the code looks obvious and passed ten local tests. Concurrency is not impressed by confidence.'] },
      { heading: 'Tools that create guarantees', bullets: ['Program order orders actions within one thread.', 'A volatile write happens-before a later read of the same field.', 'A monitor unlock happens-before a later lock of that monitor.', 'Thread start() happens-before actions in the started thread, and thread completion happens-before a successful join().', 'Atomic classes and concurrent collections provide their own documented atomicity and visibility guarantees.'] },
      { heading: 'What volatile does not do', paragraphs: ['volatile++ is still a read followed by a write. Multiple threads can interleave those operations. Use AtomicInteger or a lock for compound updates.'] },
    ],
  },
  {
    slug: 'rule-engine',
    title: 'Rule Engine',
    category: 'design-patterns',
    summary: 'Evaluate independent business rules against a context while keeping orchestration out of the rules themselves.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['strategy', 'business logic', 'composition'],
    takeaway: 'The engine owns evaluation order. Each rule owns one decision.',
    diagram: ['Context', 'Sort by priority', 'Check condition', 'Execute matching action'],
    codeLabel: 'RuleEngine.java',
    codeNote: 'This version executes every matching rule, from highest priority to lowest.',
    code: `import java.util.Comparator;\nimport java.util.List;\n\ninterface Rule<C> {\n    int priority();\n    boolean matches(C context);\n    void execute(C context);\n}\n\nfinal class RuleEngine<C> {\n    void evaluate(List<Rule<C>> rules, C context) {\n        rules.stream()\n            .sorted(Comparator.comparingInt(Rule<C>::priority).reversed())\n            .filter(rule -> rule.matches(context))\n            .forEach(rule -> rule.execute(context));\n    }\n}`,
    sections: [
      { heading: 'The simple model', paragraphs: ['A rule has a condition and an action. The engine decides which rules run and in what order.', 'This prevents one giant method from becoming a museum of business exceptions.'] },
      { heading: 'Decisions to make', bullets: ['Should evaluation stop after the first match?', 'What happens when two rules have the same priority?', 'Should a failed action stop the engine?', 'Are conditions pure, or can they call external services?'] },
      { heading: 'Good fit', paragraphs: ['Use this when rules change independently or new rules are added regularly. For a short, stable if/else chain, this is probably more furniture than house.'] },
    ],
  },
  {
    slug: 'singleton',
    title: 'Singleton',
    category: 'design-patterns',
    summary: 'Expose one shared instance, while understanding why global state and hidden dependencies make this pattern controversial.',
    level: 'Intermediate',
    minutes: 7,
    tags: ['global state', 'enum', 'dependency injection'],
    takeaway: 'A singleton controls construction. It does not automatically make shared state safe.',
    codeLabel: 'AppConfig.java',
    codeNote: 'The enum form is safe from ordinary reflection and serialization attacks.',
    code: `public enum AppConfig {\n    INSTANCE;\n\n    public String environment() {\n        return "local";\n    }\n}`,
    sections: [
      { heading: 'Why it exists', paragraphs: ['Some resources should be shared, such as a process-wide configuration or a metrics registry. A singleton ensures a single instance inside one class loader.'] },
      { heading: 'Ways to bypass the idea', bullets: ['Reflection can create another instance for ordinary classes.', 'Serialization can create another object unless readResolve() is used.', 'Cloning can produce a second object.', 'Separate class loaders or separate JVMs create separate singleton instances.'] },
      { heading: 'My default', paragraphs: ['Prefer dependency injection. Let the application composition root create one object and pass it where needed. The dependency remains visible and tests remain less theatrical.'] },
    ],
  },
  {
    slug: 'strategy-pattern',
    title: 'Strategy',
    category: 'design-patterns',
    summary: 'Select one behavior from a family of interchangeable algorithms without changing the code that uses it.',
    level: 'Foundations',
    minutes: 7,
    tags: ['behavioral', 'composition', 'polymorphism'],
    takeaway: 'Put the changing algorithm behind an interface, then inject the choice into the code that needs it.',
    diagram: ['Checkout', 'ShippingCostStrategy', 'Standard shipping or express shipping'],
    codeLabel: 'StrategyDemo.java',
    codeNote: 'The application chooses a strategy once, then Checkout uses the same interface without knowing the concrete shipping algorithm.',
    code: `interface ShippingCostStrategy {\n    int calculateCents(int weightGrams);\n}\n\nfinal class StandardShipping implements ShippingCostStrategy {\n    public int calculateCents(int weightGrams) {\n        return 500 + weightGrams / 1000 * 50;\n    }\n}\n\nfinal class ExpressShipping implements ShippingCostStrategy {\n    public int calculateCents(int weightGrams) {\n        return 1200 + weightGrams / 1000 * 100;\n    }\n}\n\nfinal class Checkout {\n    private final ShippingCostStrategy shipping;\n\n    Checkout(ShippingCostStrategy shipping) {\n        this.shipping = shipping;\n    }\n\n    int shippingCostCents(int weightGrams) {\n        return shipping.calculateCents(weightGrams);\n    }\n}\n\nfinal class StrategyDemo {\n    public static void main(String[] args) {\n        int packageWeightGrams = 2000;\n\n        // Application code chooses the behavior at the composition boundary.\n        ShippingCostStrategy strategy = new ExpressShipping();\n        Checkout checkout = new Checkout(strategy);\n\n        System.out.println(checkout.shippingCostCents(packageWeightGrams));\n        // Express: 1200 + (2000 / 1000 * 100) = 1400 cents.\n    }\n}`,
    links: [
      { label: 'Java Comparator API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/Comparator.html' },
      { label: 'Java functional interfaces', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/function/package-summary.html' },
    ],
    sections: [
      { heading: 'The problem', paragraphs: ['Checkout needs a shipping cost, but the formula changes by delivery option. Putting every formula in Checkout creates a growing conditional block.', 'The Strategy pattern moves each algorithm into its own class. Checkout depends on the stable ShippingCostStrategy interface.'] },
      { heading: 'How the usage works', bullets: ['Create the concrete strategy, such as ExpressShipping.', 'Pass it to Checkout through the constructor.', 'Checkout calls calculateCents(...) without checking which strategy was selected.', 'To add another option, add another strategy instead of editing Checkout.'] },
      { heading: 'Good fit and traps', paragraphs: ['Use Strategy when the behavior varies independently of the object using it, or when the behavior needs isolated tests. Do not create a class for every two-line variation if a small, stable expression is clearer.'] },
    ],
  },
  {
    slug: 'factory-method-pattern',
    title: 'Factory Method',
    category: 'design-patterns',
    summary: 'Let subclasses decide which product to create while the shared workflow stays in the base class.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['creational', 'polymorphism', 'extension point'],
    takeaway: 'The base class owns the process. The concrete creator owns the product choice.',
    diagram: ['ReportService.publish()', 'createExporter()', 'CSV exporter or JSON exporter'],
    codeLabel: 'FactoryMethodDemo.java',
    codeNote: 'This is Factory Method, not a static switch-based factory: the base workflow calls an overridable creation method.',
    code: `interface ReportExporter {\n    String export(String report);\n}\n\nfinal class CsvExporter implements ReportExporter {\n    public String export(String report) {\n        return "csv:" + report;\n    }\n}\n\nfinal class JsonExporter implements ReportExporter {\n    public String export(String report) {\n        return "{\\"report\\":\\"" + report + "\\"}";\n    }\n}\n\nabstract class ReportService {\n    // The shared workflow does not know the concrete exporter.\n    final String publish(String report) {\n        ReportExporter exporter = createExporter();\n        return exporter.export(report);\n    }\n\n    protected abstract ReportExporter createExporter();\n}\n\nfinal class CsvReportService extends ReportService {\n    protected ReportExporter createExporter() {\n        return new CsvExporter();\n    }\n}\n\nfinal class JsonReportService extends ReportService {\n    protected ReportExporter createExporter() {\n        return new JsonExporter();\n    }\n}\n\nfinal class FactoryMethodDemo {\n    public static void main(String[] args) {\n        // Configuration chooses the creator. Application code uses one API.\n        ReportService service = new JsonReportService();\n        System.out.println(service.publish("sales=42"));\n    }\n}`,
    links: [
      { label: 'Java ServiceLoader API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/ServiceLoader.html' },
      { label: 'Java interface tutorial', url: 'https://docs.oracle.com/javase/tutorial/java/IandI/createinterface.html' },
    ],
    sections: [
      { heading: 'What is being separated', paragraphs: ['The publish(...) algorithm is stable: create an exporter, then export the report. The concrete exporter is the part that varies.', 'Factory Method gives the base class a named extension point, createExporter(). Subclasses override that method to choose the product.'] },
      { heading: 'How to use it', bullets: ['Choose CsvReportService or JsonReportService at the application boundary.', 'Call publish(...) on the ReportService abstraction.', 'The base class runs the common workflow.', 'The subclass supplies the product at the exact point where it is needed.'] },
      { heading: 'Factory Method versus a simple factory', paragraphs: ['A static method containing a switch can be useful and simpler. Factory Method is useful when different creators share a workflow and each creator owns its product construction. Do not use inheritance merely to hide one constructor call.'] },
    ],
  },
  {
    slug: 'builder-pattern',
    title: 'Builder',
    category: 'design-patterns',
    summary: 'Construct a readable, validated object when there are many optional settings or constructor parameters.',
    level: 'Foundations',
    minutes: 7,
    tags: ['creational', 'immutability', 'validation'],
    takeaway: 'A Builder gives names to construction choices and can validate the complete object before it exists.',
    diagram: ['Builder setters', 'build()', 'Validated immutable UserProfile'],
    codeLabel: 'BuilderDemo.java',
    codeNote: 'The built UserProfile is immutable. The builder is mutable during construction, then disappears from the caller’s view.',
    code: `final class UserProfile {\n    private final String name;\n    private final String email;\n    private final boolean admin;\n\n    private UserProfile(Builder builder) {\n        this.name = builder.name;\n        this.email = builder.email;\n        this.admin = builder.admin;\n    }\n\n    static Builder builder() {\n        return new Builder();\n    }\n\n    @Override\n    public String toString() {\n        return "UserProfile{name='" + name + "', email='" + email\n                + "', admin=" + admin + "}";\n    }\n\n    static final class Builder {\n        private String name;\n        private String email;\n        private boolean admin;\n\n        Builder name(String name) {\n            this.name = name;\n            return this;\n        }\n\n        Builder email(String email) {\n            this.email = email;\n            return this;\n        }\n\n        Builder admin(boolean admin) {\n            this.admin = admin;\n            return this;\n        }\n\n        UserProfile build() {\n            if (name == null || name.isBlank()) {\n                throw new IllegalStateException("name is required");\n            }\n            if (email == null || !email.contains("@")) {\n                throw new IllegalStateException("valid email is required");\n            }\n            return new UserProfile(this);\n        }\n    }\n}\n\nfinal class BuilderDemo {\n    public static void main(String[] args) {\n        UserProfile profile = UserProfile.builder()\n                .name("Alice")\n                .email("alice@example.com")\n                .admin(true)\n                .build();\n\n        System.out.println(profile);\n    }\n}`,
    links: [
      { label: 'Java HttpRequest.Builder API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.net.http/java/net/http/HttpRequest.Builder.html' },
      { label: 'Java StringBuilder API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/StringBuilder.html' },
    ],
    sections: [
      { heading: 'Why not a constructor?', paragraphs: ['A constructor with several String and boolean arguments is hard to read because the call site does not explain each value. Overloaded constructors multiply the problem.', 'Builder methods name each choice and let the object validate all fields together in build().'] },
      { heading: 'How to use it', bullets: ['Start with UserProfile.builder().', 'Set required and optional values using named methods.', 'Call build() once all choices are present.', 'Keep the resulting UserProfile immutable so it cannot change behind its callers.'] },
      { heading: 'Good fit and traps', paragraphs: ['Use Builder for many optional fields, validation, or a long-lived immutable value. For two required fields and no optional behavior, a normal constructor or static factory is usually clearer. Do not let a builder silently create an invalid object.'] },
    ],
  },
  {
    slug: 'decorator-pattern',
    title: 'Decorator',
    category: 'design-patterns',
    summary: 'Add behavior by wrapping an object with the same interface instead of modifying the original implementation.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['structural', 'wrapping', 'cross-cutting concerns'],
    takeaway: 'A decorator is an object-shaped wrapper: it can do work before or after delegating to the wrapped component.',
    diagram: ['Notifier', 'LoggingNotifier', 'EmailNotifier', 'send()'],
    codeLabel: 'DecoratorDemo.java',
    codeNote: 'Both the real notifier and the decorator implement Notifier, so callers can use either one through the same interface.',
    code: `interface Notifier {\n    void send(String message);\n}\n\nfinal class EmailNotifier implements Notifier {\n    public void send(String message) {\n        System.out.println("email: " + message);\n    }\n}\n\nfinal class LoggingNotifier implements Notifier {\n    private final Notifier next;\n\n    LoggingNotifier(Notifier next) {\n        this.next = next;\n    }\n\n    public void send(String message) {\n        System.out.println("log: sending notification");\n        next.send(message); // Preserve the original behavior.\n        System.out.println("log: notification sent");\n    }\n}\n\nfinal class DecoratorDemo {\n    public static void main(String[] args) {\n        Notifier notifier = new LoggingNotifier(\n                new EmailNotifier());\n\n        // The caller sees one Notifier. The wrapper adds logging.\n        notifier.send("Order shipped");\n    }\n}`,
    links: [
      { label: 'Java FilterInputStream API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/io/FilterInputStream.html' },
      { label: 'Java InputStream API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/io/InputStream.html' },
    ],
    sections: [
      { heading: 'The shape', paragraphs: ['The decorator and the wrapped component implement the same interface. That lets a decorator stand anywhere the original object is expected.', 'LoggingNotifier adds behavior, then delegates to EmailNotifier. The email implementation remains focused on sending email.'] },
      { heading: 'Stacking behavior', bullets: ['Wrap with logging, then metrics, then retry if those concerns are independent.', 'Each wrapper delegates to the next component.', 'The order matters: logging outside retry records the whole retry operation; logging inside retry records each attempt.', 'The caller depends on Notifier, not on the wrapper chain.'] },
      { heading: 'Decorator versus inheritance', paragraphs: ['Inheritance adds behavior statically to a class hierarchy. Decorator composes behavior at runtime and allows different combinations per instance. Too many wrappers can still become hard to read, so name important stacks or use a composition method.'] },
    ],
  },
  {
    slug: 'chain-of-responsibility-pattern',
    title: 'Chain of Responsibility',
    category: 'design-patterns',
    summary: 'Pass a request through an ordered pipeline where each handler can stop it or delegate to the next handler.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['behavioral', 'pipeline', 'validation'],
    takeaway: 'Each handler owns one check. Calling next() is the explicit decision to continue the request.',
    diagram: ['Request', 'Authentication', 'Rate limit', 'Business handler', 'Response'],
    codeLabel: 'ChainDemo.java',
    codeNote: 'A handler can reject the request by returning early. A valid handler calls next.handle(request) to continue the chain.',
    code: `record Request(String user, String action) {}\n\ninterface Handler {\n    void handle(Request request);\n}\n\nabstract class BaseHandler implements Handler {\n    private final Handler next;\n\n    BaseHandler(Handler next) {\n        this.next = next;\n    }\n\n    protected void continueChain(Request request) {\n        if (next != null) {\n            next.handle(request);\n        }\n    }\n}\n\nfinal class AuthenticationHandler extends BaseHandler {\n    AuthenticationHandler(Handler next) { super(next); }\n\n    public void handle(Request request) {\n        if (request.user() == null) {\n            System.out.println("REJECTED: not authenticated");\n            return;\n        }\n        continueChain(request);\n    }\n}\n\nfinal class RateLimitHandler extends BaseHandler {\n    RateLimitHandler(Handler next) { super(next); }\n\n    public void handle(Request request) {\n        System.out.println("rate limit check passed");\n        continueChain(request);\n    }\n}\n\nfinal class BusinessHandler implements Handler {\n    public void handle(Request request) {\n        System.out.println("executed: " + request.action());\n    }\n}\n\nfinal class ChainDemo {\n    public static void main(String[] args) {\n        Handler pipeline = new AuthenticationHandler(\n                new RateLimitHandler(\n                new BusinessHandler()));\n\n        // This request passes through all three handlers.\n        pipeline.handle(new Request("alice", "refund"));\n\n        // This request stops at authentication.\n        pipeline.handle(new Request(null, "refund"));\n    }\n}`,
    links: [
      { label: 'Jakarta Servlet Filter API', url: 'https://jakarta.ee/specifications/servlet/6.0/apidocs/jakarta.servlet/jakarta/servlet/Filter.html' },
      { label: 'Java logging Filter API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.logging/java/util/logging/Filter.html' },
    ],
    sections: [
      { heading: 'The request path', paragraphs: ['The pipeline is assembled once, then every request enters at the first handler. Authentication checks the user, rate limiting checks capacity, and the business handler performs the operation.', 'A handler has two choices: stop by returning, or call continueChain(...) to pass the request onward.'] },
      { heading: 'How to use it', bullets: ['Create the final business handler first.', 'Wrap it with cross-cutting handlers in the desired order.', 'Give the application only the first Handler.', 'Send requests into the pipeline and let each handler decide whether to continue.'] },
      { heading: 'Ordering and traps', paragraphs: ['Order is behavior. Authentication before rate limiting may avoid spending limiter capacity on anonymous traffic, while rate limiting before authentication may protect the authentication endpoint itself.', 'Keep handlers focused and make rejection observable. A chain that silently drops a request is not a pattern; it is a missing log message wearing a costume.'] },
    ],
  },
  {
    slug: 'lru-cache',
    title: 'LRU Cache',
    category: 'collections-caching',
    summary: 'Keep the most recently used values and evict the one that has been idle the longest.',
    level: 'Intermediate',
    minutes: 7,
    tags: ['LinkedHashMap', 'eviction', 'O(1)'],
    takeaway: 'LRU needs fast lookup and fast reordering, which is why a hash map plus linked list is the classic shape.',
    diagram: ['get / put', 'Map lookup', 'Move to most-recent end', 'Evict oldest when full'],
    codeLabel: 'LruCache.java',
    codeNote: 'LinkedHashMap can maintain access order for us.',
    code: `import java.util.LinkedHashMap;\n\nfinal class LruCache<K, V> extends LinkedHashMap<K, V> {\n    private final int capacity;\n\n    LruCache(int capacity) {\n        super(capacity, 0.75f, true);\n        this.capacity = capacity;\n    }\n\n    @Override\n    protected boolean removeEldestEntry(Entry<K, V> eldest) {\n        return size() > capacity;\n    }\n}`,
    sections: [
      { heading: 'How it works', paragraphs: ['The map gives average O(1) lookup. The linked order tracks recent access. On every get or put, the entry moves to the recent end.'] },
      { heading: 'Complexity', bullets: ['Lookup: average O(1)', 'Insert: average O(1)', 'Eviction: O(1)', 'Space: O(capacity)'] },
      { heading: 'Production checklist', bullets: ['Decide whether the cache is thread-safe.', 'Add TTL if old data can be wrong even when recently used.', 'Measure hit rate, evictions, and memory usage.', 'Protect expensive cache misses from a stampede.'] },
    ],
  },
  {
    slug: 'lfu-cache',
    title: 'LFU Cache',
    category: 'collections-caching',
    summary: 'Evict the values used the fewest times, breaking ties with recency.',
    level: 'Advanced',
    minutes: 10,
    tags: ['frequency', 'eviction', 'cache policy'],
    takeaway: 'LFU remembers popularity, not just recent activity.',
    diagram: ['Key → node', 'Frequency → ordered keys', 'Track minimum frequency', 'Evict oldest key in that bucket'],
    codeLabel: 'LfuCache.java',
    codeNote: 'The key structures are the important part. A production version also needs a clear concurrency policy.',
    code: `final class Entry<K, V> {\n    K key;\n    V value;\n    int frequency = 1;\n}\n\n// Required indexes:\nMap<K, Entry<K, V>> entries;\nMap<Integer, LinkedHashSet<K>> keysByFrequency;\nint minimumFrequency;\n\n// On access: move the key from frequency f to f + 1.\n// On eviction: remove the oldest key in minimumFrequency.` ,
    sections: [
      { heading: 'Why it is different', paragraphs: ['LRU favors what was used recently. LFU favors what is used repeatedly. A rarely used item that was accessed just now can still be evicted by LFU.'] },
      { heading: 'The usual tie-breaker', paragraphs: ['If several entries have the same frequency, use recency inside that frequency bucket. LinkedHashSet is a useful building block for that job.'] },
      { heading: 'When to choose it', paragraphs: ['LFU can work well when a small set of hot keys should remain cached for a long time. It costs more bookkeeping and can keep historical popularity around longer than you expect.'] },
    ],
  },
  {
    slug: 'tree-traversal',
    title: 'Tree Traversal',
    category: 'data-structures',
    summary: 'Visit every node in a tree in an order that matches the problem you are solving.',
    level: 'Foundations',
    minutes: 7,
    tags: ['recursion', 'DFS', 'BFS'],
    takeaway: 'Traversal order is not decoration. It determines what information is available when you visit a node.',
    diagram: ['Root', 'Left subtree', 'Right subtree'],
    codeLabel: 'Traversal.java',
    codeNote: 'This is inorder traversal: left, root, right.',
    code: `record Node(int value, Node left, Node right) {}\n\nstatic void inOrder(Node node) {\n    if (node == null) return;\n\n    inOrder(node.left());\n    System.out.println(node.value());\n    inOrder(node.right());\n}`,
    sections: [
      { heading: 'The four common orders', bullets: ['Preorder: root, left, right.', 'Inorder: left, root, right.', 'Postorder: left, right, root.', 'Breadth-first: level by level.'] },
      { heading: 'Complexity', paragraphs: ['Every traversal visits n nodes, so time is O(n). Recursive depth-first traversal uses O(h) stack space, where h is the tree height.'] },
      { heading: 'Useful clue', paragraphs: ['Inorder traversal of a binary search tree produces sorted values. Postorder is useful when children must be processed before their parent.'] },
    ],
  },
  {
    slug: 'binary-search-tree',
    title: 'Binary Search Tree',
    category: 'data-structures',
    summary: 'Organize values so smaller items go left and larger items go right.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['BST', 'ordering', 'search'],
    takeaway: 'A balanced tree gives logarithmic operations. A lopsided tree is a linked list wearing a hat.',
    codeLabel: 'Bst.java',
    codeNote: 'This recursive insert keeps the example small. Production trees need a balancing strategy when input can be ordered.',
    code: `record Node(int value, Node left, Node right) {}\n\nstatic Node insert(Node node, int value) {\n    if (node == null) return new Node(value, null, null);\n\n    if (value < node.value()) {\n        return new Node(node.value(), insert(node.left(), value), node.right());\n    }\n\n    return new Node(node.value(), node.left(), insert(node.right(), value));\n}`,
    sections: [
      { heading: 'The invariant', paragraphs: ['Every value in the left subtree is smaller than the node. Every value in the right subtree is larger, or follows whatever duplicate policy you chose.'] },
      { heading: 'Complexity', bullets: ['Average search, insert, delete: O(log n) when balanced.', 'Worst-case search, insert, delete: O(n).', 'Height is the deciding factor.'] },
      { heading: 'Real alternatives', paragraphs: ['Use TreeMap when you need a tested ordered map. Use a red-black tree, AVL tree, or another balanced structure when you are implementing the data structure itself.'] },
    ],
  },
  {
    slug: 'binary-search',
    title: 'Binary Search',
    category: 'algorithms',
    summary: 'Find a value in sorted data by removing half of the remaining search space after each comparison.',
    level: 'Foundations',
    minutes: 6,
    tags: ['sorted data', 'O(log n)', 'search'],
    takeaway: 'Binary search is fast because it discards half the possibilities, not because it compares faster.',
    codeLabel: 'Search.java',
    codeNote: 'The input must be sorted in ascending order.',
    code: `static int binarySearch(int[] values, int target) {\n    int low = 0;\n    int high = values.length - 1;\n\n    while (low <= high) {\n        int middle = low + (high - low) / 2;\n        if (values[middle] == target) return middle;\n        if (values[middle] < target) low = middle + 1;\n        else high = middle - 1;\n    }\n\n    return -1;\n}`,
    sections: [
      { heading: 'The invariant', paragraphs: ['At every loop, if the target exists, it is between low and high. Each comparison shrinks that interval.'] },
      { heading: 'Complexity', bullets: ['Time: O(log n)', 'Space: O(1) for the iterative version', 'Input requirement: sorted data'] },
      { heading: 'Common bugs', bullets: ['Using low + high can overflow for large indexes.', 'Forgetting to move past middle causes infinite loops.', 'Searching unsorted data makes the result meaningless.'] },
    ],
  },
  {
    slug: 'binary-trees',
    title: 'Binary Trees',
    category: 'algorithms',
    summary: 'Traverse, search, and reason about recursive tree structure without losing the empty-child cases.',
    level: 'Intermediate',
    minutes: 9,
    tags: ['trees', 'recursion', 'BFS', 'DFS'],
    takeaway: 'A tree algorithm is usually a clear contract for the empty node plus a result composed from child results.',
    diagram: ['Root', 'Left subtree', 'Right subtree'],
    codeLabel: 'TreeDepth.java',
    codeNote: 'The empty subtree is the base case. Each non-empty node adds one level above the deeper child.',
    code: `record Node(int value, Node left, Node right) { }\n\nstatic int maxDepth(Node node) {\n    if (node == null) {\n        return 0;\n    }\n    return 1 + Math.max(maxDepth(node.left()), maxDepth(node.right()));\n}`,
    links: [
      { label: 'LeetCode 104: Maximum Depth of Binary Tree', url: 'https://leetcode.com/problems/maximum-depth-of-binary-tree/' },
      { label: 'Java Deque API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/Deque.html' },
    ],
    sections: [
      { heading: 'The shape', paragraphs: ['A binary tree node has at most two children. The useful abstraction is not the concrete class; it is the contract for an empty subtree and the way parent results combine child results.'] },
      { heading: 'Traversal choices', bullets: ['Preorder processes a node before its children.', 'Inorder processes left, node, right and is sorted for a valid binary search tree.', 'Postorder computes children before the parent.', 'Level order uses a queue and processes one depth at a time.'] },
      { heading: 'Common traps', paragraphs: ['Be explicit about whether depth counts nodes or edges. Also test an empty tree, a single node, and a skewed tree. The null case is where many supposedly finished solutions quietly fall over.'] },
    ],
  },
  {
    slug: 'graph-traversal',
    title: 'Graph Traversal',
    category: 'algorithms',
    summary: 'Use BFS, DFS, visited state, and graph representations to solve reachability and component problems.',
    level: 'Intermediate',
    minutes: 10,
    tags: ['graphs', 'BFS', 'DFS', 'cycles'],
    takeaway: 'The graph representation and visited-state policy are part of the algorithm, not setup noise.',
    diagram: ['Vertices', 'Edges', 'Visited frontier', 'Connected components'],
    codeLabel: 'GraphTraversal.java',
    codeNote: 'BFS is a good default for shortest paths in an unweighted graph. DFS is often clearer for exhaustive exploration.',
    code: `static int countComponents(List<List<Integer>> graph) {\n    boolean[] visited = new boolean[graph.size()];\n    int components = 0;\n    for (int start = 0; start < graph.size(); start++) {\n        if (!visited[start]) {\n            components++;\n            Deque<Integer> work = new ArrayDeque<>();\n            work.push(start);\n            while (!work.isEmpty()) {\n                int node = work.pop();\n                if (visited[node]) continue;\n                visited[node] = true;\n                for (int next : graph.get(node)) work.push(next);\n            }\n        }\n    }\n    return components;\n}`,
    links: [
      { label: 'LeetCode 200: Number of Islands', url: 'https://leetcode.com/problems/number-of-islands/' },
      { label: 'Java ArrayDeque API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/ArrayDeque.html' },
    ],
    sections: [
      { heading: 'BFS versus DFS', paragraphs: ['BFS expands a frontier by distance and gives shortest edge-count paths in unweighted graphs. DFS follows a branch deeply and is useful for components, cycle checks, and exhaustive search.'] },
      { heading: 'Representations', bullets: ['Adjacency lists are usually efficient for sparse graphs.', 'A matrix gives constant-time edge lookup but uses O(V²) space.', 'Keep direction and weights explicit when parsing input.', 'A visited set prevents duplicate work and infinite cycles.'] },
      { heading: 'Complexity', paragraphs: ['With adjacency lists, a complete BFS or DFS is O(V + E). That bound assumes each vertex and adjacency entry is processed a bounded number of times.'] },
    ],
  },
  {
    slug: 'recursion-and-backtracking',
    title: 'Recursion and Backtracking',
    category: 'algorithms',
    summary: 'Model a decision tree, stop invalid branches early, and restore mutable state before exploring the next choice.',
    level: 'Intermediate',
    minutes: 9,
    tags: ['recursion', 'backtracking', 'search', 'pruning'],
    takeaway: 'Backtracking is choose, explore, undo. Miss the undo and the algorithm starts hallucinating its own history.',
    diagram: ['Choose', 'Recurse', 'Undo', 'Choose next'],
    codeLabel: 'Parentheses.java',
    codeNote: 'Only add a closing parenthesis when the partial prefix remains valid.',
    code: `static void build(int pairs, int open, int close, StringBuilder path, List<String> out) {\n    if (path.length() == pairs * 2) {\n        out.add(path.toString());\n        return;\n    }\n    if (open < pairs) {\n        path.append('(');\n        build(pairs, open + 1, close, path, out);\n        path.deleteCharAt(path.length() - 1);\n    }\n    if (close < open) {\n        path.append(')');\n        build(pairs, open, close + 1, path, out);\n        path.deleteCharAt(path.length() - 1);\n    }\n}`,
    links: [
      { label: 'Oracle Java StringBuilder API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/StringBuilder.html' },
      { label: 'LeetCode 22: Generate Parentheses', url: 'https://leetcode.com/problems/generate-parentheses/' },
    ],
    sections: [
      { heading: 'The recursive contract', paragraphs: ['Define what one call is responsible for, then write the smallest base case that returns without recursing. The parameters should describe the remaining decision state, not a hidden global history.'] },
      { heading: 'Backtracking loop', bullets: ['Choose a candidate.', 'Recurse with the updated state.', 'Undo the choice.', 'Try the next candidate.'] },
      { heading: 'Pruning', paragraphs: ['Reject a branch as soon as it cannot produce a valid answer. Pruning is safe only when the bound is correct; optimism is not an algorithm.'] },
    ],
  },
  {
    slug: 'dynamic-programming',
    title: 'Dynamic Programming',
    category: 'algorithms',
    summary: 'Define reusable states, write transitions, initialize boundary cases, and avoid recomputing the same work.',
    level: 'Intermediate',
    minutes: 10,
    tags: ['DP', 'memoization', 'tabulation', 'optimization'],
    takeaway: 'Dynamic programming is a precise state definition plus a dependency order, not a table added for decorative seriousness.',
    diagram: ['State definition', 'Base values', 'Transition', 'Final state'],
    codeLabel: 'CoinChange.java',
    codeNote: 'The impossible sentinel is larger than any valid answer and is converted back to -1 at the boundary.',
    code: `static int minCoins(int[] coins, int target) {\n    int[] dp = new int[target + 1];\n    Arrays.fill(dp, target + 1);\n    dp[0] = 0;\n    for (int amount = 1; amount <= target; amount++) {\n        for (int coin : coins) {\n            if (coin <= amount) {\n                dp[amount] = Math.min(dp[amount], dp[amount - coin] + 1);\n            }\n        }\n    }\n    return dp[target] > target ? -1 : dp[target];\n}`,
    links: [
      { label: 'LeetCode 322: Coin Change', url: 'https://leetcode.com/problems/coin-change/' },
      { label: 'Java Arrays API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/Arrays.html' },
    ],
    sections: [
      { heading: 'State first', paragraphs: ['Write one sentence defining each state before writing the loop. For coin change, dp[amount] means the fewest coins needed to make that exact amount.'] },
      { heading: 'Two common forms', bullets: ['Memoization is top-down recursion plus a cache.', 'Tabulation fills states bottom-up in dependency order.', 'Space can be reduced only when discarded states are no longer dependencies.'] },
      { heading: 'Boundary cases', paragraphs: ['Test zero, impossible targets, duplicate denominations, and targets that require several different coins. The recurrence is usually not the hard part; the boundary contract is.'] },
    ],
  },
  {
    slug: 'sliding-window',
    title: 'Sliding Window',
    category: 'algorithms',
    summary: 'Keep one contiguous range, remove the value leaving it, and add the value entering it.',
    level: 'Intermediate',
    minutes: 7,
    tags: ['arrays', 'two pointers', 'O(n)'],
    takeaway: 'The window is a range of indexes, not a mysterious variable: [left, right]. Move both edges together and update only the two changed values.',
    diagram: ['[2, 1, 5] sum=8', 'remove 2', 'add 1', '[1, 5, 1] sum=7'],
    codeLabel: 'Window.java',
    codeNote: 'Example: values = [2, 1, 5, 1, 3, 2], windowSize = 3. The method checks [2, 1, 5], then [1, 5, 1], then [5, 1, 3], then [1, 3, 2]. The largest sum is 9.',
    code: `static int maxWindowSum(int[] values, int windowSize) {\n    if (windowSize <= 0 || windowSize > values.length) {\n        throw new IllegalArgumentException("windowSize must fit inside values");\n    }\n\n    // Parameters: values is the input array.\n    // windowSize is how many adjacent values belong to the window.\n    int windowSum = 0;\n    for (int index = 0; index < windowSize; index++) {\n        windowSum += values[index];\n    }\n\n    // First range: indexes [0, windowSize - 1].\n    int bestSum = windowSum;\n\n    // right is the index of the NEW value entering the window.\n    for (int right = windowSize; right < values.length; right++) {\n        int leavingIndex = right - windowSize;\n        int enteringIndex = right;\n\n        // Previous range: [leavingIndex, right - 1].\n        // New range:      [leavingIndex + 1, right].\n        // Remove the old left value, then add the new right value.\n        windowSum -= values[leavingIndex];\n        windowSum += values[enteringIndex];\n\n        bestSum = Math.max(bestSum, windowSum);\n    }\n\n    return bestSum;\n}\n\n// maxWindowSum(new int[] {2, 1, 5, 1, 3, 2}, 3) returns 9.`,
    sections: [
      { heading: 'Name the parameters', paragraphs: ['values is the input array. windowSize is the number of adjacent elements in each range. The code uses indexes starting at 0, so a window of size 3 covering the first values is indexes [0, 2].', 'The current window is a range from left to right. In this fixed-size example it always contains windowSize elements. When right advances by one, the left edge advances by one too.'] },
      { heading: 'Walk through one move', paragraphs: ['Use values = [2, 1, 5, 1, 3, 2] and windowSize = 3. The first range is indexes [0, 2], or [2, 1, 5], with sum 8.', 'The next range is indexes [1, 3], or [1, 5, 1]. It is the previous range minus values[0] = 2, plus values[3] = 1. The new sum is 8 - 2 + 1 = 7. The code calls these the leaving value and the entering value.', 'The following ranges are [2, 4] = [5, 1, 3], sum 9, and [3, 5] = [1, 3, 2], sum 6. The answer is therefore 9.'] },
      { heading: 'Complexity', bullets: ['Time: O(n)', 'Space: O(1) for a fixed-size numeric window', 'Works best when the problem has contiguous ranges.'] },
      { heading: 'Variable windows', paragraphs: ['For constraints such as “longest substring without duplicates”, move the right edge forward and move the left edge until the window becomes valid again.'] },
    ],
  },
  {
    slug: 'completable-future',
    title: 'CompletableFuture',
    category: 'concurrency',
    summary: 'Compose asynchronous work as a typed pipeline, choose its execution policy, and make success, failure, and blocking boundaries explicit.',
    level: 'Intermediate',
    minutes: 20,
    tags: ['async', 'pipelines', 'executors', 'errors', 'composition'],
    takeaway: 'A CompletableFuture pipeline is still a program: name the stage type, executor, failure path, side effects, and blocking boundary.',
    diagram: ['Caller', 'Submit source task', 'Future completes', 'Continue or compose', 'Recover or observe', 'Join at boundary'],
    codeLabel: 'Async.java',
    codeLanguage: 'Java 21',
    codeNote: 'The source task uses a virtual-thread executor. The synchronous thenApply stage runs as part of completion, while the explicit thenApplyAsync stage is submitted to the same named executor.',
    code: `import java.util.concurrent.CompletableFuture;\nimport java.util.concurrent.ExecutorService;\nimport java.util.concurrent.Executors;\n\nfinal class Async {\n    static String loadUserName() {\n        return "Ada";\n    }\n\n    public static void main(String[] args) {\n        try (ExecutorService executor =\n                     Executors.newVirtualThreadPerTaskExecutor()) {\n            CompletableFuture<String> displayName = CompletableFuture\n                    .supplyAsync(Async::loadUserName, executor)\n                    // Synchronous continuation: runs in the completion flow.\n                    .thenApply(String::toUpperCase)\n                    // Explicit asynchronous continuation: submit to executor.\n                    .thenApplyAsync(name -> "User: " + name, executor)\n                    // Recover only if an upstream stage failed.\n                    .exceptionally(error -> "User: UNKNOWN");\n\n            // Blocking is kept at the application boundary, not in every stage.\n            System.out.println(displayName.join());\n        }\n    }\n}`,
    links: [
      { label: 'CompletableFuture API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/CompletableFuture.html' },
      { label: 'CompletionStage API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/CompletionStage.html' },
      { label: 'ExecutorService API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/ExecutorService.html' },
      { label: 'Executors factory methods', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/Executors.html' },
      { label: 'ForkJoinPool API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/ForkJoinPool.html' },
      { label: 'JEP 444: Virtual Threads', url: 'https://openjdk.org/jeps/444' },
    ],
    sections: [
      {
        heading: 'The mental model: a value plus a completion signal',
        paragraphs: [
          'CompletableFuture<T> represents a computation that will eventually finish with either a T value, an exception, or cancellation. It is both a Future, which can be observed or waited on, and a CompletionStage, which can be connected to more work.',
          'The pipeline is not a magical background thread. Each stage has a dependency, a completion rule, and an execution policy. supplyAsync starts a source task. A continuation consumes that result. A terminal observer may produce a side effect or return control to synchronous application code.',
        ],
        tables: [{
          caption: 'The important Java types around CompletableFuture',
          columns: ['Type', 'Kind', 'What it represents', 'Key characteristic'],
          rows: [
            ['Runnable', 'Functional interface', 'A task with no result', 'run() returns void; checked exceptions must be handled inside the task.'],
            ['Callable<V>', 'Functional interface', 'A task that returns V', 'call() may throw checked exceptions.'],
            ['Supplier<T>', 'Functional interface', 'Produces T without an input', 'Used by supplyAsync; get() returns the source value.'],
            ['Function<T, U>', 'Functional interface', 'Maps T to U', 'Used by thenApply; it represents a value transformation.'],
            ['Consumer<T>', 'Functional interface', 'Consumes T without returning a value', 'Used by thenAccept for a success-side effect.'],
            ['BiFunction<T, U, V>', 'Functional interface', 'Combines two values into V', 'Used by thenCombine and handle-style mappings.'],
            ['Future<V>', 'Interface', 'A handle to one result', 'get() waits; it does not describe a fluent dependency graph.'],
            ['CompletionStage<T>', 'Interface', 'A stage in an async graph', 'Composes transformations, combinations, and recovery without exposing CompletableFuture-specific control methods.'],
            ['ScheduledExecutorService', 'Interface', 'An ExecutorService with time-based submission', 'Adds delayed and periodic execution; it does not make arbitrary work non-blocking.'],
            ['CompletableFuture<T>', 'Class', 'A Future plus CompletionStage', 'Can be completed manually, composed, cancelled, observed, or waited on.'],
            ['Executor', 'Interface', 'Where a Runnable is executed', 'Defines execute(Runnable), not how many threads or queues exist.'],
            ['ExecutorService', 'Interface', 'An Executor with lifecycle and task submission', 'Adds submit, shutdown, awaitTermination, and Future-returning methods.'],
          ],
        }],
      },
      {
        heading: 'Synchronous versus asynchronous continuations',
        paragraphs: [
          'The suffix matters. thenApply, thenAccept, thenRun, and their combination methods without Async are non-async continuations. They may run in the thread that completes the previous stage, or in the caller when the previous stage is already complete. Do not read “non-async” as “always runs on the caller thread”; read it as “no executor handoff is requested.”',
          'thenApplyAsync, thenAcceptAsync, thenRunAsync, and the other Async variants request asynchronous submission to an executor. With no executor argument they use the CompletableFuture default asynchronous execution facility, normally the common ForkJoinPool. With an Executor argument, the continuation is submitted there.',
          'A practical timeline is: the caller submits the source, the source executor runs it, the source completes, a non-async continuation can run immediately in that completion thread, and an async continuation is queued to its chosen executor. The final join or get waits only where the caller crosses back into synchronous code.',
        ],
        tables: [{
          caption: 'What changes when Async is present',
          columns: ['Call', 'Execution request', 'Typical use', 'Risk'],
          rows: [
            ['thenApply(fn)', 'Run the transformation in the completion flow', 'Cheap, non-blocking CPU transformation', 'A slow or blocking fn can delay the thread completing the previous stage.'],
            ['thenApplyAsync(fn)', 'Submit the transformation to the default async facility', 'Move work away from the completion thread', 'The common pool may be the wrong isolation or capacity for the work.'],
            ['thenApplyAsync(fn, executor)', 'Submit to the named executor', 'Make ownership and capacity explicit', 'The executor still needs sizing, shutdown, and monitoring.'],
            ['thenAccept / thenRun', 'Observe success without an executor handoff request', 'Small terminal action or notification', 'Side effects can be hard to test and may run on an unexpected completion thread.'],
            ['thenAcceptAsync / thenRunAsync', 'Schedule the side effect asynchronously', 'Isolate a slow observer or callback', 'Ordering and failure visibility need to be designed explicitly.'],
          ],
        }],
      },
      {
        heading: 'Transformers and composition',
        paragraphs: ['Choose the operation from the shape of the next step. The most common mistake is using thenApply when the function already returns a CompletableFuture, producing a nested CompletableFuture instead of a flat pipeline.'],
        tables: [{
          caption: 'Transforming, flattening, and combining stages',
          columns: ['Operation', 'Shape', 'Result', 'Use it when'],
          rows: [
            ['thenApply', 'T -> U', 'CompletionStage<U>', 'The next operation is an ordinary value transformation.'],
            ['thenCompose', 'T -> CompletionStage<U>', 'CompletionStage<U>', 'The next operation is asynchronous and must be flattened.'],
            ['thenCombine', 'T plus U -> V', 'CompletionStage<V>', 'Two independent stages must both succeed before combining their values.'],
            ['thenAcceptBoth', 'T plus U -> void', 'CompletionStage<Void>', 'Two results are needed only for a side effect.'],
            ['allOf', 'Many stages', 'CompletableFuture<Void>', 'All tasks must finish; collect their typed results separately.'],
            ['anyOf', 'Many stages', 'CompletableFuture<Object>', 'The first completion wins, whether normal or exceptional.'],
            ['applyToEither', 'T from either stage -> U', 'CompletionStage<U>', 'The first normal completion should provide the value.'],
          ],
        }],
      },
      {
        heading: 'Success handling, error handling, and both',
        paragraphs: ['Exceptional completion travels through the same dependency graph as a value, but ordinary success transformations are skipped until a stage recovers the failure. A handler changes the type and state of the stage that follows it, so its position matters.'],
        tables: [{
          caption: 'Completion handlers',
          columns: ['Operation', 'Runs on success', 'Runs on failure', 'Returns / changes the pipeline'],
          rows: [
            ['thenApply', 'Yes, transforms T to U', 'No', 'CompletionStage<U>; failure passes through.'],
            ['thenAccept', 'Yes, consumes T', 'No', 'CompletionStage<Void>; failure passes through.'],
            ['thenRun', 'Yes, ignores T', 'No', 'CompletionStage<Void>; failure passes through.'],
            ['exceptionally', 'No', 'Yes, receives Throwable', 'CompletionStage<T>; returns a fallback value and recovers the path.'],
            ['exceptionallyCompose', 'No', 'Yes, receives Throwable', 'CompletionStage<T>; starts an asynchronous fallback operation.'],
            ['handle', 'Yes, receives value', 'Yes, receives Throwable', 'CompletionStage<U>; always maps both states to a new result.'],
            ['whenComplete', 'Yes, observes value', 'Yes, observes Throwable', 'CompletionStage<T>; normally preserves the original result or failure.'],
          ],
        }],
      },
      {
        heading: 'Side effects are not transformations',
        paragraphs: ['A transformation returns the next value. A side effect changes something outside the pipeline: logging, metrics, persistence, notification, or mutation of shared state. thenAccept and whenComplete are useful observers, but they do not make an external action transactional or automatically retry-safe.'],
        tables: [{
          caption: 'Choosing a stage for an effect',
          columns: ['Need', 'Prefer', 'Why', 'Watch for'],
          rows: [
            ['Compute another value', 'thenApply / thenCompose', 'The result remains in the pipeline', 'Do not hide blocking I/O in a synchronous continuation.'],
            ['Consume a successful value', 'thenAccept', 'Makes the final success side effect explicit', 'A failure in the consumer completes the returned stage exceptionally.'],
            ['Run an action after success or failure', 'whenComplete', 'Good for metrics, tracing, or cleanup observation', 'It observes; it is not a fallback. The failure normally remains a failure.'],
            ['Recover with a default', 'exceptionally', 'Turns failure into a normal value', 'A later exceptionally will not run after this stage has recovered.'],
            ['Update shared mutable state', 'Prefer an explicit owner or synchronized boundary', 'Makes visibility and atomicity reviewable', 'Concurrent callbacks can interleave; a future chain is not a lock.'],
          ],
        }],
      },
      {
        heading: 'Executors: the execution policy is part of correctness',
        paragraphs: ['An Executor is not just a performance knob. It decides which work may compete for threads, how much work can queue, whether blocking is tolerable, and when the application can shut the resource down. The CompletableFuture common pool is convenient for small non-blocking CPU work, not a universal service executor.'],
        tables: [{
          caption: 'Common executor choices',
          columns: ['Executor', 'Best fit', 'Important behavior', 'Caution'],
          rows: [
            ['ForkJoinPool.commonPool()', 'Small non-blocking CPU tasks and library defaults', 'Shared process-wide work-stealing pool used by many APIs', 'Blocking I/O can starve unrelated work; ownership and sizing are not local to your service.'],
            ['Executors.newFixedThreadPool(n)', 'Bounded CPU or blocking work with a known concurrency limit', 'Reuses n threads and queues submitted tasks', 'An unbounded queue can hide overload and grow memory.'],
            ['Executors.newWorkStealingPool(n)', 'Independent CPU tasks that can be split and stolen', 'ForkJoinPool with a target parallelism and work-stealing queues', 'Do not use it as a blanket replacement for a bounded blocking-I/O pool.'],
            ['Executors.newSingleThreadExecutor()', 'Serialized background work or one ordered consumer', 'One worker preserves task order', 'One slow task delays everything behind it.'],
            ['Executors.newCachedThreadPool()', 'Short-lived bursts of tiny tasks with careful limits elsewhere', 'Creates threads as needed and reuses idle threads', 'Can create too many threads under sustained load.'],
            ['Executors.newSingleThreadScheduledExecutor()', 'One ordered delayed or periodic worker', 'A single scheduled worker preserves serial execution', 'A long task delays later scheduled work and can create drift.'],
            ['Executors.newScheduledThreadPool(n)', 'Delayed actions and periodic scheduling', 'Runs scheduled tasks using a pool', 'It is a scheduler, not automatically a safe retry policy. Bound repeats and handle failures.'],
            ['Executors.newThreadPerTaskExecutor(factory)', 'One platform thread per task when explicit thread creation is required', 'Creates a fresh thread for each task and does not pool them', 'Can be expensive at high concurrency; virtual threads are usually the better Java 21 choice for blocking tasks.'],
            ['Executors.newVirtualThreadPerTaskExecutor()', 'Many concurrent blocking tasks in Java 21+', 'Creates a new virtual thread per submitted task', 'Not a license for unlimited downstream calls; still bound connections, rate, memory, and concurrency.'],
            ['new ThreadPoolExecutor(...)', 'Production policies needing explicit queue, rejection, keep-alive, or thread factory behavior', 'Makes capacity and overload policy configurable', 'More knobs means more responsibility; document and measure the policy.'],
          ],
        }],
      },
      {
        heading: 'get() versus join()',
        paragraphs: ['Both methods wait for the future and therefore block the calling thread. The main difference is how failure is reported and which checked-exception contract the caller must handle. Neither method makes the pipeline asynchronous; they are boundary operations that turn a future back into a synchronous wait.'],
        tables: [{
          caption: 'Waiting at the boundary',
          columns: ['Method', 'Failure signal', 'Interrupted?', 'Typical choice'],
          rows: [
            ['get()', 'Throws checked ExecutionException for computation failure', 'Throws checked InterruptedException and should preserve interrupt status when handled', 'Use when an API already has a checked-exception contract or interruption must be handled explicitly.'],
            ['join()', 'Throws unchecked CompletionException for computation failure', 'Does not expose InterruptedException in its signature', 'Use when the surrounding code wants a concise unchecked boundary and has a policy for failure.'],
            ['get(timeout, unit)', 'Same checked failure types plus TimeoutException', 'Yes, checked InterruptedException', 'Use when a synchronous wait needs a hard timeout.'],
          ],
        }],
      },
      {
        heading: 'A practical checklist',
        bullets: [
          'Name the executor for every workload that is blocking, latency-sensitive, or operationally important.',
          'Use thenApply for a value transformation, thenCompose for an async transformation, and thenCombine or allOf for independent work.',
          'Keep blocking get or join at an explicit boundary. Never sprinkle waits inside the pipeline to make it feel synchronous.',
          'Choose a recovery policy: fallback, retry, translation, compensation, or fail-fast. Do not make exceptionally a decorative last line.',
          'Treat callbacks as concurrent code. Shared state still needs ownership, atomicity, and visibility guarantees.',
          'Shut down application-owned executors. A future completing successfully does not shut down the executor that ran it.',
        ],
      },
    ],
  },
  {
    slug: 'race-condition',
    title: 'Race Condition',
    category: 'concurrency',
    summary: 'A bug where the result depends on the timing or interleaving of concurrent operations.',
    level: 'Foundations',
    minutes: 7,
    tags: ['threads', 'atomicity', 'synchronization'],
    takeaway: 'If multiple threads update shared state, make the operation atomic or remove the shared state.',
    codeLabel: 'Counter.java',
    codeNote: 'AtomicInteger makes increment a single atomic operation.',
    code: `import java.util.concurrent.atomic.AtomicInteger;\n\nfinal class Counter {\n    private final AtomicInteger value = new AtomicInteger();\n\n    void increment() {\n        value.incrementAndGet();\n    }\n\n    int value() {\n        return value.get();\n    }\n}`,
    sections: [
      { heading: 'Why it happens', paragraphs: ['value++ looks like one operation but is actually read, add, and write. Two threads can read the same old value and overwrite each other.'] },
      { heading: 'Ways out', bullets: ['Use AtomicInteger for simple atomic counters.', 'Use synchronized or Lock for a multi-step invariant.', 'Use immutable messages and ownership instead of shared mutable state.', 'Use concurrent collections when the collection itself is shared.'] },
      { heading: 'Testing warning', paragraphs: ['A race can disappear when you add logging or a debugger. Stress tests and deliberate scheduling are more useful than one passing run.'] },
    ],
  },
  {
    slug: 'kafka',
    title: 'Kafka',
    category: 'backend-tools',
    summary: 'A distributed append-only log for publishing, storing, and consuming streams of records.',
    level: 'Intermediate',
    minutes: 10,
    tags: ['events', 'partitions', 'consumer groups'],
    takeaway: 'Ordering is guaranteed within a partition, not across an entire topic.',
    diagram: ['Producer', 'Topic', 'Partitions', 'Consumer group', 'Offset'],
    codeLabel: 'KafkaExample.java',
    codeNote: 'The real client needs a broker and the kafka-clients dependency. The future local lab should start both with one click.',
    code: `var producer = new KafkaProducer<String, String>(properties);\n\nproducer.send(new ProducerRecord<>(\n    "orders",\n    "order-42",\n    "created"\n));\n\nproducer.flush();\nproducer.close();`,
    sections: [
      { heading: 'The mental model', paragraphs: ['A topic is split into partitions. Producers append records. Consumers read records and track offsets. A consumer group divides partitions among its members.'] },
      { heading: 'The details that matter', bullets: ['Use a stable key when related events must stay ordered.', 'Consumer groups scale by adding consumers up to the partition count.', 'Retention means records can remain after they were consumed.', 'Retries and duplicate delivery require idempotent consumers.'] },
      { heading: 'Local lab idea', paragraphs: ['A Kafka page should eventually offer Start, Stop, Reset, Produce, and Consume actions against a local broker. The browser should talk to a small local control service, not manage Docker directly.'] },
    ],
  },
  {
    slug: 'redis',
    title: 'Redis under the hood',
    category: 'data-services',
    summary: 'Trace Redis from key hashing and single-threaded command execution through cluster shards, replicas, persistence, scripting, notifications, and failure handling.',
    level: 'Advanced',
    minutes: 18,
    tags: ['Redis', 'hash slots', 'transactions', 'Lua', 'replication', 'failover', 'observability'],
    domain: 'Data',
    takeaway: 'Redis is fast because the hot command path is simple. Production correctness comes from understanding slot ownership, asynchronous replication, persistence choices, and which event mechanism you actually need.',
    diagram: ['Client topology map', 'CRC16(key) % 16384', 'Hash slot', 'Primary shard', 'Replica', 'AOF / RDB', 'Metrics and logs'],
    codeLabel: 'RedisInternalsExample.java',
    codeLanguage: 'Java / Redis CLI',
    codeNote: 'The Java example uses Lettuce. The CLI commands assume a Redis Cluster is available on localhost:7000.',
    code: `import io.lettuce.core.RedisClient;\nimport io.lettuce.core.ScriptOutputType;\n\nvar client = RedisClient.create("redis://localhost:6379");\ntry (var connection = client.connect()) {\n    var redis = connection.sync();\n\n    // Hash tags keep related keys in one slot: both contain {42}.\n    redis.set("user:{42}:balance", "100");\n    redis.set("user:{42}:status", "active");\n\n    // MULTI queues commands. EXEC runs the queue without another\n    // client interleaving commands between these two operations.\n    redis.multi();\n    redis.decrby("user:{42}:balance", 5);\n    redis.set("user:{42}:last-change", "purchase");\n    redis.exec();\n\n    // A Lua script is one atomic server-side command.\n    String script = "return redis.call('INCRBY', KEYS[1], ARGV[1])";\n    Object remaining = redis.eval(\n        script,\n        ScriptOutputType.INTEGER,\n        new String[] {"inventory:{sku-7}:stock"},\n        "1"\n    );\n\n    // Pub/Sub is live notification, not a durable work queue.\n    redis.publish("cache-events", "user-42-updated");\n    System.out.println(remaining);\n} finally {\n    client.shutdown();\n}\n\n# Inspect the slot and cluster topology from a shell:\nredis-cli -p 7000 cluster keyslot user:{42}:balance\nredis-cli -p 7000 cluster nodes\nredis-cli -p 7000 info replication\nredis-cli -p 7000 slowlog get 10`,
    links: [
      { label: 'Redis Cluster scaling and resharding', url: 'https://redis.io/docs/latest/operate/oss_and_stack/management/scaling' },
      { label: 'Redis Cluster specification', url: 'https://redis.io/docs/latest/operate/oss_and_stack/reference/cluster-spec' },
      { label: 'Transactions and WATCH', url: 'https://redis.io/docs/latest/develop/using-commands/transactions' },
      { label: 'Redis scripting and functions', url: 'https://redis.io/docs/latest/develop/interact/programmability/' },
      { label: 'Redis replication', url: 'https://redis.io/docs/latest/operate/oss_and_stack/management/replication/' },
      { label: 'Redis persistence', url: 'https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/' },
      { label: 'Keyspace notifications', url: 'https://redis.io/docs/latest/develop/pubsub/keyspace-notifications/' },
      { label: 'Redis Streams', url: 'https://redis.io/docs/latest/develop/data-types/streams' },
      { label: 'Redis latency diagnosis', url: 'https://redis.io/docs/latest/operate/oss_and_stack/management/optimization/latency/' },
      { label: 'Redis memory optimization', url: 'https://redis.io/docs/latest/operate/oss_and_stack/management/optimization/memory-optimization' },
    ],
    sections: [
      { heading: '1. What happens after a command arrives', paragraphs: ['A client sends a RESP command over a TCP connection. Redis parses it, resolves the command implementation, looks up the key in its in-memory data structures, executes the command, and writes the reply to the client output buffer.', 'The normal command path is event-loop driven and deliberately avoids a lock around every ordinary command. That is why a small GET or INCR is cheap, and why one accidentally expensive command such as KEYS, a huge SORT, or a large collection rewrite can delay unrelated clients.'] },
      { heading: '2. Slotting: how Redis Cluster finds a shard', paragraphs: ['Redis Cluster divides the keyspace into exactly 16,384 hash slots. For an ordinary key, the slot is CRC16(key) modulo 16,384. Each primary owns a subset of slots, so the client topology map can send the request directly to the node that owns the key.', 'Hash tags change the input to the hash. Redis hashes the text between the first { and the following }, so user:{42}:profile and user:{42}:cart land together. This enables a multi-key transaction or script, but it also creates a hotspot when one tag receives most of the traffic.', 'During resharding, slots move between primaries. A client can receive MOVED and refresh its topology, or ASK while a slot is in transit. Horizontal expansion is therefore a controlled slot migration, not a global table copy.'] },
      { heading: '3. Transactions: atomic does not mean rollback', paragraphs: ['MULTI starts a queue. Commands are normally replied to with QUEUED, and EXEC runs the queued commands sequentially. No other client command is interleaved during that execution window. This gives isolation for the command sequence.', 'Redis transactions do not provide SQL-style rollback. A command can fail at execution time while earlier commands remain applied. WATCH provides optimistic compare-and-set behavior: if a watched key changes before EXEC, EXEC aborts and the application must reread, recompute, and retry.', 'In Cluster mode every key used by a transaction must map to the same slot, usually by using a deliberate hash tag. WAIT can wait for replicas to acknowledge prior writes, but it does not turn Redis replication into a consensus commit protocol.'] },
      { heading: '4. Scripting and server-side functions', paragraphs: ['EVAL and EVALSHA run Lua on the Redis server. The script sees KEYS and ARGV supplied by the client, and Redis executes the script atomically relative to other commands. Use scripts for a short read-check-write operation that must happen beside the data.', 'Redis Functions package reusable server-side functions and can be loaded into the server. In a cluster, the script or function must obey the same key-slot rules as a transaction. A long script blocks the command path, so move expensive computation to an application worker instead of hiding it in Lua.', 'Pass every accessed key in KEYS. Do not construct arbitrary key names from user input inside a script; explicit keys let Redis Cluster route and validate the operation.'] },
      { heading: '5. Shards, replicas, and horizontal scale', bullets: ['A primary shard owns slots and accepts the normal write path.', 'A replica asynchronously receives the primary replication stream and can be promoted during failover.', 'Adding primaries increases capacity only after slots are assigned to the new nodes.', 'Replicas improve availability and can serve carefully chosen read traffic, but stale reads are possible.', 'A cluster client must understand MOVED, ASK, reconnects, and topology refresh. A generic single-node client is not enough.', 'Multi-key commands, transactions, and scripts are naturally single-shard operations unless all keys share a slot.'] },
      { heading: '6. Is Redis data compressed?', paragraphs: ['Redis does not automatically compress every arbitrary string value for you. Its memory saving comes mainly from compact internal encodings: small hashes and sorted sets can use listpack-style representations, and integer sets have specialized storage. When a value crosses configured thresholds, Redis promotes it to a more general representation.', 'At rest, RDB snapshots are compact binary files and AOF is an append-only command history with rewrite support. Neither choice should be described as transparent application-value compression. If payload compression matters, compress at the client or choose a module/data type that owns that representation, then measure CPU, memory, and latency.', 'Memory fragmentation is a separate problem from payload size. Track used_memory, RSS, allocator fragmentation, active defragmentation, evictions, and the memory overhead of keys and data structures.'] },
      { heading: '7. Notifications: Pub/Sub, keyspace events, or Streams?', bullets: ['Pub/Sub is live fan-out. If a subscriber is disconnected, its message is gone; there is no acknowledgement or replay.', 'Keyspace notifications publish events such as expired or deleted keys through Pub/Sub. In Cluster mode they are node-specific, so a consumer that needs every event must subscribe to every relevant node.', 'Sharded Pub/Sub keeps a channel inside one cluster shard and reduces cluster-bus fan-out, but it is still ephemeral.', 'Streams persist entries and provide consumer groups, pending entries, acknowledgements, and replay. Use Streams or Kafka when the event is work that must survive a consumer outage.'] },
      { heading: '8. Monitoring and logging', bullets: ['INFO exposes sections for memory, persistence, replication, CPU, command statistics, latency, errors, and cluster state.', 'SLOWLOG records commands whose execution time crosses the configured threshold. It measures server execution, not network time.', 'LATENCY HISTOGRAM, LATENCY DOCTOR, and latency monitoring help distinguish slow commands, fork pauses, disk I/O, expiry work, and environment noise.', 'MONITOR shows every request in real time and is expensive. Use it briefly for diagnosis, never as a default production telemetry pipeline.', 'Track p50/p95/p99 latency, ops/sec, blocked clients, rejected connections, hit rate, evictions, expired keys, replication lag, full resyncs, AOF/RDB status, fork time, and cluster slot health.', 'Redis logs startup, persistence, replication, failover, and serious runtime events. Ship logs with the instance identity and role, but do not put payloads or credentials into logs.'] },
      { heading: '9. Failure, failover, and persistence', paragraphs: ['Replication is asynchronous. A primary can acknowledge a write before a replica has it, so a primary failure can lose recently acknowledged writes unless the application chooses stronger operational safeguards. Redis Cluster or Sentinel detects failures and promotes a replica; the application must reconnect and refresh topology.', 'RDB is a point-in-time snapshot. It restarts quickly and is convenient for backup, but writes after the last snapshot can be lost. AOF records write operations and can fsync every second, every write, or according to the operating system. Every-second fsync is a latency/durability tradeoff, not magic zero-loss storage.', 'A serious failure plan includes replicas, persistence, off-host backups, restore drills, disk-full alerts, replication-lag alerts, and a defined policy for cache bypass versus request failure. For a cache, rebuilding from the source may be correct. For a ledger, Redis may be the wrong system of record.'] },
      { heading: 'Interview answer in one minute', paragraphs: ['Say: “A Redis Cluster key maps to one of 16,384 CRC16 slots, and a primary owns a slot range. Replicas asynchronously copy the primary and can be promoted, so acknowledged-write durability depends on replication and persistence settings. MULTI/EXEC serializes a command queue but does not roll back; WATCH gives optimistic CAS. Lua and Functions are atomic server-side code, but they block the command path and must use same-slot keys. Pub/Sub is lossy, Streams are replayable. I monitor INFO, SLOWLOG, LATENCY, replication offsets, evictions, persistence, and failover health.”'] },
      { heading: 'Local lab idea', paragraphs: ['A useful Redis lab should show a key’s computed slot, the primary and replica owning it, a deliberate MOVED response, a same-slot transaction, a cross-slot failure, a Lua atomic update, a missed Pub/Sub event, a replayed Stream entry, and the INFO/SLOWLOG evidence for each experiment.'] },
    ],
  },
  {
    slug: 'junit',
    title: 'JUnit',
    category: 'testing-engineering',
    summary: 'Write small executable examples that tell you whether a unit behaves as intended.',
    level: 'Foundations',
    minutes: 6,
    tags: ['tests', 'assertions', 'TDD'],
    takeaway: 'A good unit test names one behavior and fails for one useful reason.',
    codeLabel: 'LruCacheTest.java',
    codeNote: 'The example assumes JUnit Jupiter is on the test classpath.',
    code: `import static org.junit.jupiter.api.Assertions.assertEquals;\nimport org.junit.jupiter.api.Test;\n\nclass CalculatorTest {\n    @Test\n    void addsTwoNumbers() {\n        int result = 2 + 3;\n\n        assertEquals(5, result);\n    }\n}`,
    sections: [
      { heading: 'What belongs in a unit test', paragraphs: ['Arrange the input, act once, and assert the observable behavior. Keep the test independent from networks, clocks, and other tests.'] },
      { heading: 'Useful habits', bullets: ['Name the behavior, not the implementation detail.', 'Prefer one reason to fail per test.', 'Use parameterized tests when the same rule has several examples.', 'Test edge cases where the implementation is most likely to lie.'] },
      { heading: 'Test pyramid reminder', paragraphs: ['Keep most tests fast and focused. Use integration tests for real boundaries, not as a substitute for understanding the unit.'] },
    ],
  },
  {
    slug: 'testcontainers',
    title: 'Testcontainers',
    category: 'testing-engineering',
    summary: 'Run real dependencies in disposable containers during tests instead of mocking their behavior into fiction.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['integration tests', 'Docker', 'databases'],
    takeaway: 'Use mocks for your code. Use containers when the behavior belongs to the dependency.',
    codeLabel: 'PostgresTest.java',
    codeNote: 'This needs Docker and the Testcontainers PostgreSQL module.',
    code: `@Testcontainers\nclass PostgresTest {\n    @Container\n    static PostgreSQLContainer<?> postgres =\n        new PostgreSQLContainer<>("postgres:16");\n\n    @Test\n    void databaseIsReachable() {\n        assertTrue(postgres.isRunning());\n    }\n}`,
    sections: [
      { heading: 'What it gives you', paragraphs: ['Your test talks to a real PostgreSQL, Kafka, Redis, or Elasticsearch process started for the test run. This catches protocol and configuration mistakes that mocks cannot.'] },
      { heading: 'Tradeoffs', bullets: ['Tests are slower than pure unit tests.', 'The machine running tests needs Docker.', 'Images should be pinned and cached in CI.', 'Containers need cleanup and sensible resource limits.'] },
      { heading: 'Local lab connection', paragraphs: ['The same service catalog that powers interactive local examples can provide the images and health checks used by integration tests. One definition, two consumers.'] },
    ],
  },
  {
    slug: 'rate-limiter',
    title: 'Rate Limiter',
    category: 'system-design',
    summary: 'Limit how many operations a caller can perform during a period of time.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['tokens', 'traffic', 'distributed systems'],
    takeaway: 'A rate limit is a policy. Choose what happens at the boundary, not just the counter algorithm.',
    diagram: ['Request', 'Check allowance', 'Allow or reject', 'Refill over time'],
    codeLabel: 'TokenBucket.java',
    codeNote: 'The demo creates one limiter, sends named requests through tryAcquire(...), and uses a fixed clock so the refill behavior is visible. A distributed limiter needs shared state and a clock strategy.',
    code: `import java.time.Duration;\nimport java.time.Instant;\n\nfinal class TokenBucket {\n    private final int capacity;\n    private final int refillTokens;\n    private final Duration refillEvery;\n    private int tokens;\n    private Instant lastRefill;\n\n    TokenBucket(int capacity, int refillTokens,\n                Duration refillEvery, Instant start) {\n        if (capacity <= 0 || refillTokens <= 0 || refillEvery.isZero()\n                || refillEvery.isNegative()) {\n            throw new IllegalArgumentException("Invalid limiter settings");\n        }\n        this.capacity = capacity;\n        this.refillTokens = refillTokens;\n        this.refillEvery = refillEvery;\n        this.tokens = capacity; // Start full: an initial burst is allowed.\n        this.lastRefill = start;\n    }\n\n    synchronized boolean tryAcquire(Instant now) {\n        refill(now);\n        if (tokens == 0) {\n            return false; // No token means reject this request.\n        }\n        tokens--; // One request consumes one token.\n        return true;\n    }\n\n    synchronized int availableTokens(Instant now) {\n        refill(now);\n        return tokens;\n    }\n\n    private void refill(Instant now) {\n        if (now.isBefore(lastRefill)) {\n            throw new IllegalArgumentException("Clock moved backwards");\n        }\n        long completedRefills = Duration.between(lastRefill, now).toMillis()\n                / refillEvery.toMillis();\n        if (completedRefills == 0) {\n            return;\n        }\n\n        long tokensToAdd = Math.min((long) capacity,\n                completedRefills * refillTokens);\n        tokens = (int) Math.min(capacity, tokens + tokensToAdd);\n        lastRefill = lastRefill.plus(refillEvery.multipliedBy(completedRefills));\n    }\n}\n\n// Usage: every request asks the same limiter before calling the service.\nfinal class TokenBucketDemo {\n    public static void main(String[] args) {\n        Instant start = Instant.parse("2026-01-01T10:00:00Z");\n        TokenBucket limiter = new TokenBucket(\n                3,                      // capacity: maximum stored tokens\n                1,                      // refillTokens: tokens added each period\n                Duration.ofSeconds(10), // refillEvery: one token each 10 seconds\n                start);                  // clock time when the bucket starts full\n\n        request(limiter, "request-1", start);\n        request(limiter, "request-2", start.plusSeconds(1));\n        request(limiter, "request-3", start.plusSeconds(2));\n        request(limiter, "request-4", start.plusSeconds(3));\n        // The first three requests consume the three initial tokens.\n        // request-4 is rejected because no token is left.\n\n        request(limiter, "request-5", start.plusSeconds(10));\n        // Ten seconds have passed: one token was refilled, so this is allowed.\n    }\n\n    private static void request(TokenBucket limiter, String name, Instant at) {\n        boolean allowed = limiter.tryAcquire(at);\n        System.out.printf("%s -> %s, tokens left=%d%n",\n                name, allowed ? "ALLOWED" : "REJECTED",\n                limiter.availableTokens(at));\n    }\n}`,
    sections: [
      { heading: 'How to use it', paragraphs: ['Create one TokenBucket for the caller or resource you want to protect. Before doing the real work, call limiter.tryAcquire(now). If it returns true, continue to the service. If it returns false, reject or delay the request.', 'The demo sends five named requests through the same limiter. Requests 1 to 3 use the three tokens available at the start. Request 4 is rejected. At 10 seconds, one token has returned, so request 5 is allowed.'] },
      { heading: 'The constructor parameters', bullets: ['capacity = 3: the maximum number of unused tokens stored. This controls the largest immediate burst.', 'refillTokens = 1: how many tokens are added after each refill period.', 'refillEvery = 10 seconds: how often tokens are added. Together with refillTokens, this controls the long-term rate.', 'start: the initial clock time. The example starts full, so the first three requests can pass immediately.'] },
      { heading: 'What tryAcquire does', paragraphs: ['tryAcquire first calculates how many complete refill periods have passed, then adds those tokens without exceeding capacity. It checks the balance: zero tokens means REJECTED; one or more tokens means ALLOWED and removes exactly one token.', 'A token represents permission for one operation. The limiter does not call the downstream service itself. It only answers the question: “May this request continue?”'] },
      { heading: 'Common algorithms', bullets: ['Fixed window is simple but can burst at window boundaries.', 'Sliding window is smoother but needs more bookkeeping.', 'Token bucket allows a burst up to capacity while controlling the average rate.', 'Leaky bucket drains work at a steady rate instead of allowing a stored burst.'] },
      { heading: 'Production concern', paragraphs: ['This example keeps state in one JVM. With multiple application instances, each instance would have its own bucket unless the state is shared through a service such as Redis. Decide whether the limit is per user, API key, IP address, or service, and return a useful response such as Retry-After when rejecting a request.'] },
    ],
  },
  {
    slug: 'circuit-breaker',
    title: 'Circuit Breaker',
    category: 'system-design',
    summary: 'Stop calling a failing dependency when recent failures cross a threshold, then test recovery with one probe.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['resilience', 'timeouts', 'fallbacks'],
    takeaway: 'A circuit breaker is a small state machine around a real dependency call: count recent outcomes, calculate the failure rate, and fail fast when the threshold is crossed.',
    diagram: ['CLOSED: calls flow', 'Failure rate ≥ threshold', 'OPEN: fail fast', 'Timer expires', 'HALF_OPEN: one probe'],
    codeLabel: 'CircuitState.java',
    codeNote: 'The demo wraps the dependency call, records success or failure, prints the calculated rate, and shows each state transition. It is intentionally single-threaded so the policy is visible; production code also needs atomic state changes and a timeout around the dependency.',
    code: `import java.time.Duration;\nimport java.time.Instant;\nimport java.util.ArrayDeque;\nimport java.util.Deque;\nimport java.util.concurrent.Callable;\n\nfinal class Circuit {\n    enum State { CLOSED, OPEN, HALF_OPEN }\n\n    private final int windowSize;\n    private final int minimumCalls;\n    private final double failureRateThreshold;\n    private final Duration openDuration;\n    private final Deque<Boolean> recentOutcomes = new ArrayDeque<>();\n    private State state = State.CLOSED;\n    private Instant openedAt;\n\n    // windowSize: how many recent calls are used in the rate calculation.\n    // minimumCalls: do not open before this many calls have completed.\n    // failureRateThreshold: 0.50 means 50% failures or more opens the circuit.\n    // openDuration: how long OPEN rejects calls before allowing one probe.\n    Circuit(int windowSize, int minimumCalls,\n            double failureRateThreshold, Duration openDuration) {\n        this.windowSize = windowSize;\n        this.minimumCalls = minimumCalls;\n        this.failureRateThreshold = failureRateThreshold;\n        this.openDuration = openDuration;\n    }\n\n    String call(Instant now, Callable<String> dependency) {\n        if (!allowsRequest(now)) {\n            // OPEN: do not call the dependency. Return a fallback immediately.\n            return "fallback (OPEN: dependency was not called)";\n        }\n\n        try {\n            String result = dependency.call();\n            recordSuccess(now);\n            return result;\n        } catch (Exception failure) {\n            recordFailure(now);\n            return "fallback (dependency failed)";\n        }\n    }\n\n    private boolean allowsRequest(Instant now) {\n        if (state == State.OPEN) {\n            if (now.isBefore(openedAt.plus(openDuration))) {\n                return false;\n            }\n            // The wait ended. Allow the next call as a recovery probe.\n            System.out.println("OPEN -> HALF_OPEN");\n            state = State.HALF_OPEN;\n        }\n        return true;\n    }\n\n    private void recordSuccess(Instant now) {\n        if (state == State.HALF_OPEN) {\n            // The probe worked: the dependency recovered. Close and reset history.\n            state = State.CLOSED;\n            openedAt = null;\n            recentOutcomes.clear();\n            return;\n        }\n        recordOutcome(true);\n    }\n\n    private void recordFailure(Instant now) {\n        if (state == State.HALF_OPEN) {\n            // The probe failed: start the OPEN wait again.\n            open(now);\n            return;\n        }\n        recordOutcome(false);\n        if (recentOutcomes.size() >= minimumCalls\n                && failureRate() >= failureRateThreshold) {\n            open(now);\n        }\n    }\n\n    private void recordOutcome(boolean success) {\n        recentOutcomes.addLast(success);\n        if (recentOutcomes.size() > windowSize) {\n            recentOutcomes.removeFirst();\n        }\n    }\n\n    private void open(Instant now) {\n        state = State.OPEN;\n        openedAt = now;\n    }\n\n    double failureRate() {\n        if (recentOutcomes.isEmpty()) return 0.0;\n        long failures = recentOutcomes.stream().filter(success -> !success).count();\n        return (double) failures / recentOutcomes.size();\n    }\n\n    String status() {\n        return String.format("state=%s, samples=%d, failureRate=%.0f%%",\n                state, recentOutcomes.size(), failureRate() * 100);\n    }\n}\n\n// Usage: four calls, then a recovery probe.\nfinal class CircuitDemo {\n    public static void main(String[] args) {\n        Circuit circuit = new Circuit(4, 4, 0.50, Duration.ofSeconds(10));\n        Instant t0 = Instant.parse("2026-01-01T10:00:00Z");\n\n        System.out.println(circuit.call(t0, () -> "OK"));\n        System.out.println(circuit.status()); // CLOSED, 1 sample, 0%\n        System.out.println(circuit.call(t0.plusSeconds(1), () -> { throw new RuntimeException(); }));\n        System.out.println(circuit.call(t0.plusSeconds(2), () -> { throw new RuntimeException(); }));\n        System.out.println(circuit.call(t0.plusSeconds(3), () -> { throw new RuntimeException(); }));\n        System.out.println(circuit.status()); // OPEN, 4 samples, 75%\n\n        System.out.println(circuit.call(t0.plusSeconds(4), () -> "NOT CALLED"));\n        // Still OPEN: this call fails fast and does not execute the lambda.\n\n        System.out.println(circuit.call(t0.plusSeconds(14), () -> "RECOVERED"));\n        System.out.println(circuit.status()); // CLOSED, 0 samples, 0%\n    }\n}`,
    sections: [
      { heading: 'How to use it', paragraphs: ['Create one Circuit object for the dependency you want to protect, then pass the real dependency call into circuit.call(...). The circuit decides whether the call is allowed, records the outcome, and returns a fallback when the dependency fails or the circuit is open.', 'In the example, the first call succeeds. The next three calls fail. After the fourth completed call, the circuit has seen 3 failures out of 4 samples, so it opens. The later lambda marked “NOT CALLED” proves that OPEN prevents the dependency call.'] },
      { heading: 'The parameters', bullets: ['windowSize = 4: keep only the four most recent completed outcomes for the rate.', 'minimumCalls = 4: do not make a trip decision after only one unlucky request.', 'failureRateThreshold = 0.50: open when failures divided by samples is at least 50%.', 'openDuration = 10 seconds: remain OPEN for 10 seconds before allowing a probe.'] },
      { heading: 'How the states change', paragraphs: ['CLOSED is the normal state. Calls reach the dependency, and each success or failure enters the rolling history.', 'When the history has at least minimumCalls and failureRate reaches the threshold, CLOSED changes to OPEN. OPEN rejects calls immediately and does not touch the dependency.', 'After openDuration, the next allowed call changes OPEN to HALF_OPEN. That call is the recovery probe. A successful probe changes HALF_OPEN to CLOSED and clears the old failure history. A failed probe changes HALF_OPEN back to OPEN and starts the wait again.'] },
      { heading: 'How the rate is calculated', paragraphs: ['The rate is not a percentage of all requests ever made. It is calculated from the current rolling history: failureRate = number of failed outcomes / number of stored outcomes.', 'For the example, the four outcomes are success, failure, failure, failure. The calculation is 3 / 4 = 0.75, or 75%. Because 75% is greater than the 50% threshold, the circuit opens. A call rejected while OPEN is not added to the history because the dependency was never attempted.'] },
      { heading: 'It is not a retry policy', paragraphs: ['A circuit breaker decides whether a call is attempted. A retry policy decides whether a failed call is attempted again. Put a small, bounded retry policy inside the protected call only when the extra attempts are safe; otherwise retries can make the failure rate and outage worse.'] },
      { heading: 'Production notes', bullets: ['Use a real timeout so a hung dependency becomes a recorded failure.', 'Use atomic state transitions and limit concurrent HALF_OPEN probes when multiple threads share the circuit.', 'Measure failure rate, open duration, probe results, fallback rate, latency, and timeout count.', 'Choose the window and threshold from dependency behavior; 50% and four calls are teaching values, not universal defaults.'] },
    ],
  },
  {
    slug: 'virtual-threads',
    title: 'Virtual Threads',
    category: 'concurrency',
    summary: 'Use Java 21 virtual threads for large numbers of mostly waiting tasks, while keeping CPU work and downstream resources explicitly bounded.',
    level: 'Intermediate',
    minutes: 19,
    tags: ['Java 21', 'I/O', 'Project Loom', 'thread per task', 'Semaphore', 'structured concurrency'],
    domain: 'JVM concurrency',
    takeaway: 'A virtual thread is cheap to block, not a faster processor. Create one per task, then limit the database, API, memory, or CPU resource that the task uses.',
    diagram: ['task starts on virtual thread', 'JVM mounts it on a carrier', 'blocking I/O parks the virtual thread', 'carrier runs another task', 'I/O completes and task resumes'],
    codeLabel: 'VirtualThreadsDemo.java',
    codeLanguage: 'Java 21',
    codeNote: 'This example shows four usage styles: direct start, a named Thread.Builder, an ExecutorService returning Futures, and CompletableFuture. The Semaphore allows many virtual threads to exist while permitting only two inventory calls at once.',
    code: `import java.time.Duration;\nimport java.util.List;\nimport java.util.concurrent.CompletableFuture;\nimport java.util.concurrent.ExecutorService;\nimport java.util.concurrent.Executors;\nimport java.util.concurrent.Future;\nimport java.util.concurrent.Semaphore;\n\npublic final class VirtualThreadsDemo {\n    // This represents a scarce downstream resource, such as two database connections.\n    private static final Semaphore INVENTORY_LIMIT = new Semaphore(2);\n\n    private static String blockingLookup(String id) {\n        try {\n            Thread.sleep(Duration.ofMillis(100)); // Stand in for JDBC or HTTP I/O.\n            return id + " -> available";\n        } catch (InterruptedException interrupted) {\n            Thread.currentThread().interrupt();\n            throw new IllegalStateException("lookup interrupted", interrupted);\n        }\n    }\n\n    private static String boundedInventoryLookup(String sku) {\n        try {\n            INVENTORY_LIMIT.acquire();\n            try {\n                return blockingLookup(sku);\n            } finally {\n                INVENTORY_LIMIT.release(); // Always return the permit.\n            }\n        } catch (InterruptedException interrupted) {\n            Thread.currentThread().interrupt();\n            throw new IllegalStateException("waiting for inventory capacity interrupted", interrupted);\n        }\n    }\n\n    public static void main(String[] args) throws Exception {\n        // 1. One independent task, with no executor object to manage.\n        Thread direct = Thread.startVirtualThread(() ->\n                System.out.println("direct: " + Thread.currentThread().isVirtual()));\n        direct.join();\n\n        // 2. A builder is useful when naming or configuring virtual threads.\n        Thread named = Thread.ofVirtual()\n                .name("report-worker")\n                .start(() -> System.out.println(\n                        "named: " + Thread.currentThread().getName()));\n        named.join();\n\n        // 3. One virtual thread per submitted task, with Future coordination.\n        try (ExecutorService executor =\n                     Executors.newVirtualThreadPerTaskExecutor()) {\n            List<Future<String>> results = List.of(\n                    executor.submit(() -> boundedInventoryLookup("sku-1")),\n                    executor.submit(() -> boundedInventoryLookup("sku-2")),\n                    executor.submit(() -> boundedInventoryLookup("sku-3")),\n                    executor.submit(() -> boundedInventoryLookup("sku-4"))\n            );\n\n            for (Future<String> result : results) {\n                System.out.println("future: " + result.get());\n            }\n\n            // 4. CompletableFuture is still useful when composing stages.\n            CompletableFuture<String> profile = CompletableFuture\n                    .supplyAsync(() -> blockingLookup("user-42"), executor);\n            System.out.println("future composition: " + profile.join());\n        }\n    }\n}`,
    links: [
      { label: 'JEP 444: Virtual Threads', url: 'https://openjdk.org/jeps/444' },
      { label: 'Oracle virtual threads guide', url: 'https://docs.oracle.com/en/java/javase/21/core/virtual-threads.html' },
      { label: 'Thread API in Java 21', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Thread.html' },
      { label: 'Thread.Builder.OfVirtual API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Thread.Builder.OfVirtual.html' },
      { label: 'Executors API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/Executors.html' },
      { label: 'Semaphore API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/Semaphore.html' },
      { label: 'StructuredTaskScope API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/StructuredTaskScope.html' },
    ],
    sections: [
      { heading: 'Why virtual threads exist', tables: [{ caption: 'Platform threads and virtual threads solve different costs', columns: ['Question', 'Platform thread', 'Virtual thread'], rows: [['What is expensive?', 'Each thread maps closely to an operating-system thread, so stacks and scheduling capacity limit how many are practical.', 'The JVM schedules many virtual threads over a smaller set of carrier platform threads.'], ['What happens during blocking I/O?', 'The platform thread can sit waiting while it owns an operating-system thread.', 'The virtual thread can park while waiting, allowing its carrier to run another virtual thread when the operation is supported.'], ['Best use', 'CPU work, explicit thread-pool policies, or code that must avoid virtual-thread pinning.', 'Many independent tasks that spend much of their lifetime waiting on I/O.'], ['What does not improve automatically?', 'A platform thread does not make a remote service faster.', 'A virtual thread does not add CPU cores, database connections, network bandwidth, or downstream quota.']] }], paragraphs: ['A platform thread is the JVM representation most closely tied to an operating-system thread. Operating-system threads are useful but relatively expensive, so applications traditionally put tasks into a fixed pool and queue excess work.', 'A virtual thread is a lightweight Java Thread scheduled by the JVM. When the task reaches supported blocking I/O, the JVM can park the virtual thread and let its carrier platform thread run another virtual thread. When the I/O completes, the parked task becomes runnable again.', 'The benefit is simpler code at higher concurrency. You can write ordinary blocking-style code such as read, wait, and continue, without dedicating one expensive platform thread to every task. The benefit is efficient waiting, not faster computation.'] },
      { heading: 'Read the example from top to bottom', bullets: ['Thread.startVirtualThread(...) is the shortest form for starting one independent task. The returned Thread can be joined, interrupted, or inspected.', 'Thread.ofVirtual().name(...).start(...) uses the builder API. Use it when names, an uncaught-exception handler, or thread-local inheritance policy should be explicit.', 'Executors.newVirtualThreadPerTaskExecutor() creates a new virtual thread for each submitted task. It returns Future objects, so the caller can wait for results or cancel work.', 'CompletableFuture.supplyAsync(..., executor) uses the same virtual-thread executor as the execution mechanism. CompletableFuture describes coordination; it does not itself make a task virtual.', 'The example deliberately starts four inventory tasks but allows only two inventory calls at once. The tasks are cheap to create, while the simulated downstream capacity remains two.'] },
      { heading: 'What does “bound the resource” mean?', paragraphs: ['Suppose an application receives 10,000 requests and each request needs one database lookup. With virtual threads, creating 10,000 waiting tasks may be reasonable. Opening 10,000 database connections is not. The database, connection pool, memory, and network become the limits.', 'A Semaphore is a counter representing permission to enter a scarce section. In the example, new Semaphore(2) creates two permits. A task calls acquire() before the inventory call. The first two tasks take the permits and call the dependency. Later tasks wait at acquire() without consuming two extra database connections. Each task calls release() in finally so a success, exception, or cancellation does not leak capacity.', 'A semaphore limits concurrent use. It does not limit requests per second. If a partner allows 100 requests per minute, use a rate limiter for the time-based rule, and use a semaphore as well if there is also a maximum number of in-flight requests.'] },
      { heading: 'Why not use a fixed pool of virtual threads?', paragraphs: ['The old pattern uses a fixed pool of 20 worker threads to limit a dependency to 20 concurrent calls. That works when the workers are expensive platform threads, but it mixes two unrelated concerns: how tasks execute and how much of a downstream resource the application may consume.', 'With virtual threads, use one virtual thread per task and put the limit next to the scarce resource. Use a database connection pool with maximumPoolSize = 20 for database capacity. Use a Semaphore(20) when the dependency has an in-flight limit that is not already enforced by its client pool. Use a rate limiter when the rule is requests per second or minute.', 'This keeps limits independent. You might allow 1,000 virtual tasks, 20 database calls, 50 HTTP requests in flight, and 100 API requests per second. One fixed worker pool cannot express those four different policies cleanly.'] },
      { heading: 'CPU-bound work still needs a bounded pool', paragraphs: ['Virtual threads do not create more CPU cores. If 10,000 tasks perform expensive JSON compression, image processing, encryption, or a large algorithm, they compete for the same processors. Creating a virtual thread for every CPU-heavy task does not make that work finish sooner and can add scheduling and memory pressure.', 'Use a bounded platform-thread executor or a ForkJoinPool for CPU-bound stages. The pool size should reflect available processors and the workload. Keep the virtual-thread approach for tasks that mostly wait, then hand CPU-heavy work to the bounded CPU executor.'] },
      { heading: 'Other resource limits', tables: [{ caption: 'Choose the limiter that matches the real constraint', columns: ['Constraint', 'Example', 'Useful control'], rows: [['Database connections', 'Only 20 JDBC connections are available.', 'Configure the connection pool to 20; a Semaphore can protect additional per-operation capacity.'], ['Concurrent HTTP calls', 'The partner rejects more than 50 in-flight requests.', 'Semaphore(50) around the remote call.'], ['Request rate', 'The partner allows 100 requests per second.', 'A token-bucket or fixed-rate limiter. A semaphore alone is not enough.'], ['CPU capacity', 'Compression uses all processor cores.', 'Bounded platform-thread executor or ForkJoinPool.'], ['Memory', 'Each task holds a large response buffer.', 'Bound input concurrency, buffer sizes, queue sizes, and response limits.'], ['Application shutdown', 'Tasks must finish or be cancelled with the request.', 'Track task ownership and propagate interruption or cancellation.']] }], paragraphs: ['The rule is simple: identify what can run out, then limit that thing. Virtual threads remove one bottleneck, the cost of one blocked platform thread. They do not remove the other bottlenecks in the request path.'] },
      { heading: 'Server request-per-task pattern', paragraphs: ['A virtual-thread server can accept a request and run the blocking handler in one virtual thread per request. This is a good migration path for applications built around JDBC, blocking HTTP clients, filesystem calls, or ordinary try/catch code.', 'The server still needs bounded connection pools, request body limits, timeouts, authentication limits, rate limits, and shutdown handling. “One virtual thread per request” means one cheap execution context per request. It does not mean one unlimited request into every dependency.'] },
      { heading: 'Structured concurrency', paragraphs: ['Virtual threads are the execution primitive. Structured concurrency is the coordination model for related child tasks. A request can fork several child operations, wait for them as a group, cancel sibling work when one fails, and keep the child lifetime inside the request scope.', 'StructuredTaskScope was a preview API in Java 21, so production code must check the JDK version and preview-status rules before using it. The important design idea is stable: child tasks should have an owner, a bounded lifetime, and a clear failure and cancellation policy.'] },
      { heading: 'Pinning and blocking compatibility', bullets: ['In Java 21, a virtual thread can pin its carrier while executing native code or blocking inside certain synchronized regions. A short synchronized section is not automatically a disaster; long or frequent pinning defeats the scalability benefit.', 'Prefer java.util.concurrent locks when they fit the design, keep critical sections short, and use Java Flight Recorder or the documented diagnostics to investigate pinning rather than guessing.', 'A blocking library is not automatically unsafe. The relevant questions are whether it blocks, whether the JDK can park the virtual thread during that wait, whether it pins a carrier, and whether the downstream resource is bounded.', 'Interruptible operations should preserve the interrupt flag or propagate cancellation. A task that ignores interruption can outlive the request that created it.'] },
      { heading: 'ThreadLocal, naming, and observability', paragraphs: ['Virtual threads support ThreadLocal, but thousands or millions of task-local values can consume meaningful memory. Do not use ThreadLocal as a hidden pool for a database connection or other expensive resource. Acquire and release that resource explicitly.', 'Name virtual threads when a name helps logs or diagnostics, but avoid putting sensitive identifiers into thread names. Record request IDs through the application context or logging framework according to its supported propagation model.', 'Measure active tasks, waiting time, dependency latency, timeout count, rejected permits, pool utilization, memory, and carrier pinning. A thread dump or trace should let you distinguish “many tasks waiting on the database” from “CPU is saturated.”'] },
      { heading: 'Common mistakes', bullets: ['Expecting virtual threads to speed up CPU-bound code. They primarily improve the cost of waiting.', 'Submitting all work to an unbounded virtual-thread executor without limiting databases, APIs, queues, memory, or request rates.', 'Using a Semaphore to model a time-based rate limit. A semaphore controls simultaneous work; a rate limiter controls starts over time.', 'Acquiring a permit without releasing it in finally. One exception can permanently reduce capacity.', 'Putting long synchronized or native sections around blocking calls and then wondering why carrier threads are pinned.', 'Using ThreadLocal to cache a JDBC connection per virtual thread. Use a connection pool and close or return the connection explicitly.', 'Treating Future.get() or CompletableFuture.join() as harmless everywhere. Waiting is readable at a deliberate boundary, but blocking inside an event loop or reactive pipeline can break that runtime model.'] },
      { heading: 'When to use it', bullets: ['Many concurrent request or task handlers that spend most of their time waiting on JDBC, HTTP, files, or other blocking I/O.', 'Thread-per-request or thread-per-task designs where callback-heavy code would make ownership and error handling harder to understand.', 'Fan-out work where each child has a clear owner, deadline, cancellation rule, and downstream capacity limit.', 'Existing synchronous libraries that behave correctly when interrupted and do not pin carriers for long periods.'] },
      { heading: 'When not to use it', bullets: ['A CPU-heavy batch stage that needs explicit processor-level parallelism.', 'A dependency whose capacity is unbounded only in theory. Add a connection, concurrency, rate, memory, and timeout policy first.', 'A framework or runtime that requires event-loop non-blocking behavior and has a better native async API.', 'As a replacement for cancellation, timeouts, bulkheads, rate limits, or observability. Virtual threads improve one execution model; they are not a resilience strategy.'] },
    ],
  },
  {
    slug: 'reactor',
    title: 'Project Reactor',
    category: 'concurrency',
    summary: 'Understand how publishers, subscribers, subscriptions, demand, and backpressure turn Java pipelines into asynchronous flows.',
    level: 'Advanced',
    minutes: 18,
    tags: ['reactive streams', 'Publisher', 'Subscriber', 'Subscription', 'Flux', 'Mono', 'backpressure', 'Kafka Streams', 'Flink'],
    domain: 'Reactive and event streaming',
    takeaway: 'Publication sends signals downstream. Subscription connects the graph, carries demand upstream, and owns cancellation. No subscription means no data flow.',
    diagram: ['source Publisher', 'operators transform signals', 'Subscription carries request(n) upstream', 'onNext / onError / onComplete downstream', 'Subscriber consumes or cancels'],
    codeLabel: 'ReactorDemo.java',
    codeLanguage: 'Java 17+ with reactor-core 3.x',
    codeNote: 'Add the reactor-core dependency before compiling. The first pipeline is assembled without running; subscribe starts it. BaseSubscriber then requests one item at a time so demand is visible.',
    code: `import org.reactivestreams.Subscription;\nimport reactor.core.publisher.BaseSubscriber;\nimport reactor.core.publisher.Flux;\n\npublic final class ReactorDemo {\n    public static void main(String[] args) {\n        Flux<Integer> pipeline = Flux.range(1, 5)\n                .doOnRequest(demand ->\n                        System.out.println("upstream demand=" + demand))\n                .map(number -> number * 10)\n                .filter(number -> number >= 30);\n\n        // Assembly only describes the computation. No values print here.\n        System.out.println("pipeline assembled");\n\n        // subscribe connects the Publisher to a Subscriber and starts flow.\n        pipeline.subscribe(value ->\n                System.out.println("unbounded value=" + value));\n\n        // A custom Subscriber can make demand and cancellation explicit.\n        pipeline.subscribe(new BaseSubscriber<Integer>() {\n            @Override\n            protected void hookOnSubscribe(Subscription subscription) {\n                request(1); // Ask upstream for one matching value.\n            }\n\n            @Override\n            protected void hookOnNext(Integer value) {\n                System.out.println("bounded value=" + value);\n                request(1); // Pull the next value after processing this one.\n            }\n\n            @Override\n            protected void hookOnComplete() {\n                System.out.println("bounded stream complete");\n            }\n        });\n    }\n}`,
    links: [
      { label: 'Reactor reference guide', url: 'https://projectreactor.io/docs/core/release/reference/index.html' },
      { label: 'Reactor Core API', url: 'https://projectreactor.io/docs/core/release/api/index.html' },
      { label: 'Reactive Streams specification', url: 'https://www.reactive-streams.org/' },
      { label: 'Reactive Streams Publisher API', url: 'https://www.reactive-streams.org/reactive-streams-1.0.4-javadoc/org/reactivestreams/Publisher.html' },
      { label: 'Reactive Streams Subscriber API', url: 'https://www.reactive-streams.org/reactive-streams-1.0.4-javadoc/org/reactivestreams/Subscriber.html' },
      { label: 'Reactive Streams Subscription API', url: 'https://www.reactive-streams.org/reactive-streams-1.0.4-javadoc/org/reactivestreams/Subscription.html' },
      { label: 'Java Stream API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/stream/Stream.html' },
      { label: 'Apache Kafka Streams', url: 'https://kafka.apache.org/documentation/streams/' },
      { label: 'Apache Flink streaming concepts', url: 'https://flink.apache.org/docs/latest/' },
    ],
    sections: [
      { heading: 'The vocabulary', tables: [{ caption: 'The Reactive Streams contract', columns: ['Part', 'Responsibility'], rows: [['Publisher', 'Produces a sequence of values, errors, or completion signals. It publishes according to demand rather than having unlimited permission to flood the consumer.'], ['Subscriber', 'Receives onSubscribe, then onNext values, followed by exactly one terminal onError or onComplete signal.'], ['Subscription', 'The one-to-one lifecycle between one Publisher and one Subscriber. It exposes request(n) for demand and cancel() for stopping and cleanup.'], ['Operator', 'Wraps an upstream Publisher and returns another Publisher. map, filter, flatMap, buffer, retry, and publishOn change the signal flow.']] }], paragraphs: ['Reactor implements the Reactive Streams model. Flux represents zero to many values, while Mono represents zero or one value. Neither is the value itself. Each is a description of a possible signal sequence.', 'Publication is the downstream movement of signals. Subscription is the act that connects the publisher to the subscriber and creates the lifecycle through which demand, data, errors, completion, and cancellation travel.'] },
      { heading: 'Publication: what moves downstream', paragraphs: ['A Publisher can emit three kinds of downstream signals: onNext(value) for data, onError(error) for terminal failure, and onComplete() for successful termination. onError and onComplete end the sequence, so no later onNext signal is valid.', 'Think of publication as the source announcing what happened. A database query may publish rows, an HTTP client may publish a response body, and a timer may publish ticks. The source does not decide the whole application behavior; operators and the final subscriber do.'] },
      { heading: 'Subscription: how the flow starts', bullets: ['The application builds a pipeline by calling operators. This is assembly time, and it normally performs no I/O and emits no values.', 'subscribe(...) connects the final Subscriber to the upstream Publisher. Reactor then invokes onSubscribe with a Subscription.', 'The Subscriber uses Subscription.request(n) to express how many elements it can handle. This demand travels upstream and enables backpressure.', 'The Publisher sends up to the requested number of onNext signals, then eventually sends onComplete or onError.', 'Subscription.cancel() stops demand and asks the source to release resources. In Reactor, the Disposable returned by subscribe is the convenient cancellation handle for lambda-based subscriptions.'] },
      { heading: 'The subtle Reactor detail', paragraphs: ['The lambda subscribe forms are convenient and, in the common cases, request an effectively unbounded amount. That is why the first subscription in the example is labelled unbounded. Use BaseSubscriber or an operator such as limitRate when you need explicit demand control.', 'Do not call subscribe deep inside every service method. A library or web handler should usually return the Flux or Mono and let the outer framework own the subscription, cancellation, context, and response lifecycle. Manual subscription is appropriate at an application boundary, a command-line entry point, or a deliberate side-effect boundary.'] },
      { heading: 'Java Streams to Reactor', tables: [{ caption: 'Same vocabulary family, different runtime contract', columns: ['Dimension', 'Java Stream', 'Reactor'], rows: [['Primary shape', 'A finite or source-backed computation over elements.', 'An asynchronous sequence represented by Mono or Flux.'], ['Direction', 'The terminal operation pulls/traverses the source as part of the pipeline execution.', 'The Publisher sends signals, while Subscriber demand travels upstream as request(n).'], ['Start signal', 'A terminal operation such as collect, count, or forEach.', 'subscribe, or a framework subscribing on your behalf.'], ['Time', 'Usually completes during the terminal call.', 'May span time, threads, network responses, and multiple signals.'], ['Failure and cancellation', 'Exceptions propagate through the terminal operation; cancellation is not the central API contract.', 'onError terminates the sequence; cancel stops demand and enables resource cleanup.'], ['Backpressure', 'No Reactive Streams request protocol.', 'Demand is part of the contract across asynchronous boundaries.']] }], paragraphs: ['Java Streams and Reactor share a declarative pipeline vocabulary: source, intermediate operations, terminal consumption, laziness, and transformations such as map and filter. The evolution is not that Java Stream became Reactor. Reactor keeps the pipeline idea but adds time, asynchronous boundaries, signals, cancellation, and demand management.', 'A useful mental progression is: Java Stream asks “how do I transform this available data?” Reactor asks “how do I compose data that may arrive later, fail, be cancelled, or arrive faster than the consumer can process?”'] },
      { heading: 'Where Apache fits', tables: [{ caption: 'Do not collapse these into one product', columns: ['Technology', 'What it is', 'Where the data lives and runs'], rows: [['Project Reactor', 'A JVM library for composing asynchronous, non-blocking signal pipelines.', 'Inside an application process. It commonly handles HTTP, database, and messaging flows.'], ['Apache Kafka Streams', 'A client library and DSL for building applications that read Kafka topics, transform records, maintain local state, and write topics.', 'Inside your application instances, with Kafka providing durable input, output, partitions, and state-related recovery.'], ['Apache Flink DataStream', 'A distributed processing engine for stateful computations over bounded and unbounded streams.', 'In a Flink job running under a cluster runtime, with managed state, event-time concepts, checkpoints, and parallel operators.']] }], paragraphs: ['Kafka Streams and Flink are not simply later versions of Reactor. Reactor primarily defines an in-process asynchronous composition model. Kafka Streams adds Kafka-native partitioned logs, record keys, materialized state, and application topology management. Flink adds a distributed execution engine for long-running stateful jobs, including cluster scheduling, checkpoints, event time, and large-scale parallelism.', 'They can be combined. A Reactor application can consume or publish through a Kafka client, and a service can hand events to Kafka Streams or Flink. The boundary is the important part: Reactor controls local application signals; Kafka or Flink controls durable distributed stream processing.'] },
      { heading: 'Backpressure, schedulers, and blocking work', bullets: ['Backpressure is a demand protocol, not merely a queue size. It prevents a fast producer from forcing an asynchronous consumer to buffer without limit.', 'publishOn changes the execution context for downstream operators. subscribeOn changes where subscription and upstream work begin. Neither operator automatically makes blocking code non-blocking.', 'Schedulers are execution policies. Use boundedElastic or a dedicated bounded executor for unavoidable blocking work, and keep the boundary visible.', 'Use timeout, retryWhen, rate limiting, circuit breaking, and fallback deliberately. A retry can multiply load against an already failing dependency.', 'Prefer cancellation-aware sources. A client that ignores cancellation can continue consuming sockets, buffers, or remote work after the caller has gone away.'] },
      { heading: 'Hot, cold, and shared publication', paragraphs: ['A cold Publisher starts its work for each Subscriber. A cold HTTP request or Flux.defer is often the right model when each caller needs an independent operation.', 'A hot Publisher produces independently of any particular Subscriber. Late subscribers may miss earlier signals. Sinks and share or replay operators make sharing explicit, but they also require decisions about buffering, replay, ownership, and termination.', 'Do not use a hot stream when you actually need durable delivery. Kafka topics, queues, and Flink checkpoints solve different durability and recovery problems than an in-memory Reactor pipeline.'] },
      { heading: 'Common mistakes', bullets: ['Confusing assembly with execution: map and filter configure a graph; subscribe or a framework boundary starts it.', 'Calling block() inside a reactive request path: it consumes a thread and can deadlock or destroy the concurrency model.', 'Assuming subscribe means bounded demand: convenient lambda subscribers commonly request unbounded demand.', 'Using publishOn or subscribeOn as performance decoration without identifying which work should move and why.', 'Treating Flux as a collection: it can represent an infinite or time-based sequence and should not be forced into memory.', 'Mixing Reactor, Kafka Streams, and Flink terminology as if Publisher, KStream, and DataStream were interchangeable types.'] },
      { heading: 'When it earns its complexity', paragraphs: ['Use Reactor when asynchronous I/O, streaming responses, cancellation, or backpressure are first-class requirements and the surrounding stack supports the model. Spring WebFlux and Reactor Netty are common examples.', 'Use Java Streams for local data transformations over an already available source. Use Kafka Streams when the problem is Kafka-native record topology and local state. Use Flink when the problem needs a distributed, stateful stream-processing engine. The right choice follows the data boundary, durability requirement, state model, and operational ownership, not the fashionable API of the month.'] },
    ],
  },
  {
    slug: 'vertx',
    title: 'Eclipse Vert.x',
    category: 'concurrency',
    summary: 'Build reactive JVM services around event loops, verticles, asynchronous APIs, and an in-process event bus.',
    level: 'Advanced',
    minutes: 9,
    tags: ['event loop', 'reactive', 'non-blocking', 'verticles'],
    takeaway: 'Vert.x keeps event-loop code fast by making blocking work somebody else’s problem, preferably a worker pool.',
    diagram: ['Event loop', 'Verticle', 'Async operation', 'Handler callback'],
    codeLabel: 'HttpServer.java',
    codeNote: 'Do not run blocking database or filesystem calls on the event loop. Use an async client or executeBlocking with care.',
    code: `import io.vertx.core.Vertx;\n\nvar vertx = Vertx.vertx();\nvertx.createHttpServer()\n    .requestHandler(request ->\n        request.response().end("hello from Vert.x"))\n    .listen(8080)\n    .onSuccess(server -> System.out.println("listening on 8080"));`,
    links: [
      { label: 'Vert.x documentation', url: 'https://vertx.io/docs/' },
      { label: 'Vert.x Core manual', url: 'https://vertx.io/docs/vertx-core/java/' },
      { label: 'Vert.x GitHub repository', url: 'https://github.com/eclipse-vertx/vert.x' },
    ],
    sections: [
      { heading: 'The runtime model', paragraphs: ['Vert.x runs handlers on a small set of event-loop threads. A handler should do a little work, start an asynchronous operation, and return so the loop can serve other events.', 'Verticles are deployable units that own behavior and state. The event bus lets verticles communicate without reaching directly into each other’s object graph.'] },
      { heading: 'The non-blocking contract', bullets: ['Never block an event-loop thread with JDBC, filesystem calls, long locks, or CPU-heavy transforms.', 'Prefer Vert.x async clients for HTTP, databases, messaging, and files.', 'Use worker verticles or executeBlocking for operations that cannot be made asynchronous.', 'Keep event-loop starvation visible with metrics and blocked-thread checks.'] },
      { heading: 'When to choose it', paragraphs: ['Vert.x is a strong fit for high-concurrency network services, gateways, and polyglot reactive applications on the JVM. It gives you more runtime decisions than a conventional blocking framework, so the team must understand event loops before production explains them.'] },
    ],
  },
  {
    slug: 'cloudflare-anycast-cdn',
    title: 'Cloudflare Anycast CDN',
    category: 'edge-delivery',
    summary: 'Use Cloudflare’s shared Anycast IPs as a global network front door for cached content, APIs, and beacon uploads.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['CDN', 'Anycast', 'BGP', 'beacons'],
    domain: 'Systems',
    takeaway: 'Anycast gets the request to a Cloudflare edge. The edge can then serve a cached response, run a Worker, or forward the request to your origin.',
    diagram: ['DNS proxy enabled', 'Cloudflare Anycast IP', 'BGP-selected edge', 'Cache, Worker, or origin'],
    codeLabel: 'beacon.js',
    codeLanguage: 'JavaScript / Beacon API',
    codeNote: 'sendBeacon sends a small asynchronous POST and returns whether the browser queued it. The endpoint should respond quickly, tolerate duplicates, and never depend on a client response body.',
    code: `const endpoint = 'https://beacon.example.com/v1/collect';\n\nconst payload = new URLSearchParams({\n  event: 'checkout_completed',\n  session_id: 'demo-session',\n  at: new Date().toISOString(),\n});\n\nconst queued = navigator.sendBeacon(endpoint, payload);\nif (!queued) {\n  void fetch(endpoint, { method: 'POST', body: payload, keepalive: true });\n}`,
    links: [
      { label: 'Cloudflare Developer Documentation', url: 'https://developers.cloudflare.com/' },
      { label: 'Cloudflare CDN architecture', url: 'https://developers.cloudflare.com/reference-architecture/architectures/cdn' },
      { label: 'How Anycast works', url: 'https://www.cloudflare.com/learning/cdn/glossary/anycast-network/' },
      { label: 'Cloudflare IP addresses', url: 'https://developers.cloudflare.com/fundamentals/concepts/cloudflare-ip-addresses/' },
      { label: 'Cloudflare cache responses', url: 'https://developers.cloudflare.com/cache/concepts/cache-responses/' },
      { label: 'Cloudflare Cache documentation', url: 'https://developers.cloudflare.com/cache/' },
      { label: 'Cache Rules', url: 'https://developers.cloudflare.com/cache/how-to/cache-rules/' },
      { label: 'Purge cached content', url: 'https://developers.cloudflare.com/cache/how-to/purge-cache/' },
      { label: 'Beacon API reference', url: 'https://developer.mozilla.org/en-US/docs/Web/API/Navigator/sendBeacon' },
    ],
    sections: [
      { heading: 'What Anycast changes', paragraphs: ['You proxy a hostname through Cloudflare. DNS then returns an address from Cloudflare’s shared Anycast ranges instead of exposing your origin address directly.', 'Cloudflare announces those IP prefixes from many points of presence. The Internet routes packets toward one of those announcements, so the request enters Cloudflare near the user in routing terms. Anycast is not a GPS lookup and does not guarantee the physically closest datacenter.'] },
      { heading: 'BGP and the nearest edge', paragraphs: ['BGP is the control plane that exchanges reachability between autonomous systems. Cloudflare’s routers announce that they can reach a prefix; transit providers and peers propagate those announcements; the user’s ISP selects a route using its local policy, local preference, AS-path length, and other tie-breakers.', 'There is no per-user setting that says “send this visitor to Frankfurt.” The selected path lands at the Cloudflare location that wins the ISP’s route decision. Cloudflare can influence the result with its own routing policy, announcements, traffic engineering, and internal capacity management, but congestion or topology changes can still make BGP-nearest different from latency-nearest.'] },
      { heading: 'CDN as a beacon upload endpoint', paragraphs: ['A beacon host such as beacon.example.com can use the same proxied Anycast front door, but its job is ingress rather than cache delivery. The browser sends a small POST with navigator.sendBeacon, often when the document becomes hidden or the user is leaving a page. Cloudflare routes the POST to a Worker, queue, or origin ingestion service.', 'POST beacons should normally bypass the object cache. The edge can authenticate, rate-limit, normalize, sample, or enqueue the event, then return 204 quickly. The ingestion pipeline must handle retries, duplicates, out-of-order events, payload limits, privacy rules, and backpressure. Anycast reduces the network distance to the first receiver; it does not make the event durable by itself.'] },
      { heading: 'How cached content gets updated', paragraphs: ['A cache is a copy of an HTTP response. It is kept only when the request and response are eligible, and it is considered fresh only for its configured lifetime, called the TTL. On a cache hit, the edge serves its copy without calling the origin.', 'Imagine the browser requests /app.js at 10:00. The edge has no copy, so it asks the origin, stores the response, and serves it. You replace the file at the origin at 10:05, but the edge may still serve the old copy until its TTL ends. Changing the origin file does not reach into every edge and edit an already-stored response.', 'A versioned URL changes the address when the content changes: the old HTML references /app.js, while the new HTML references /app.8f3c.js. The new URL is a cache miss and therefore fetches the new file. Existing users can finish loading the old URL, and new users get the new one. The filename is usually generated from the file contents, so a changed file naturally gets a changed name.', 'If the URL cannot change, revalidation asks the origin whether its copy is still valid using ETag or Last-Modified, while purging removes selected cached copies before their TTL. stale-while-revalidate can serve the old copy briefly while the edge fetches a fresh one.'] },
      { heading: 'Design checklist', bullets: ['Use a proxied DNS record for the public hostname and lock down the origin to Cloudflare traffic where possible.', 'Keep beacon and content hostnames or paths separate so event POSTs cannot accidentally become public cached assets.', 'Return 204 quickly from the beacon receiver and enqueue work behind it.', 'Set explicit Cache-Control headers and version static assets when possible.', 'Use targeted purge for urgent replacements instead of purging everything by habit.', 'Measure CF-Cache-Status, origin fetches, cache age, beacon acceptance, queue lag, duplicates, and dropped events.'] },
    ],
  },
  {
    slug: 'apache-pulsar',
    title: 'Apache Pulsar',
    category: 'backend-tools',
    summary: 'A cloud-native messaging and streaming platform that separates brokers from durable message storage.',
    level: 'Advanced',
    minutes: 10,
    tags: ['messaging', 'pub-sub', 'BookKeeper', 'multi-tenancy'],
    domain: 'Distributed',
    takeaway: 'Pulsar’s broker and storage layers scale independently, which is powerful and operationally non-trivial.',
    diagram: ['Producer', 'Broker', 'Topic', 'BookKeeper ledger', 'Subscription cursor'],
    codeLabel: 'Producer.java',
    codeNote: 'The Java client needs the pulsar-client dependency and a broker listening on the service URL.',
    code: `import org.apache.pulsar.client.api.PulsarClient;\nimport org.apache.pulsar.client.api.Schema;\n\ntry (var client = PulsarClient.builder()\n        .serviceUrl("pulsar://localhost:6650")\n        .build();\n     var producer = client.newProducer(Schema.STRING)\n        .topic("orders")\n        .create()) {\n    producer.send("order-created");\n}`,
    links: [
      { label: 'Pulsar concepts overview', url: 'https://pulsar.apache.org/docs/concepts-overview/' },
      { label: 'Pulsar Java client', url: 'https://pulsar.apache.org/docs/client-libraries/java/' },
      { label: 'Pulsar architecture overview', url: 'https://pulsar.apache.org/docs/en/next/concepts-architecture-overview/' },
    ],
    sections: [
      { heading: 'The architecture', paragraphs: ['Producers publish to topics and consumers attach through subscriptions. Pulsar brokers handle client connections and dispatch, while Apache BookKeeper stores message ledgers durably. This separation lets serving and storage scale on different curves.', 'Topics live inside tenants and namespaces, giving multi-tenant deployments a first-class place for ownership, policy, quotas, and isolation.'] },
      { heading: 'Subscription choices', bullets: ['Exclusive gives one active consumer.', 'Failover keeps a standby consumer ready.', 'Shared distributes messages among consumers without preserving global order.', 'Key_Shared routes messages with the same key consistently while sharing work.', 'Acknowledgments and redelivery make idempotent processing part of the application design.'] },
      { heading: 'Pulsar versus a plain log', paragraphs: ['Pulsar is attractive when geo-replication, multi-tenancy, tiered storage, and flexible subscription semantics matter. Kafka is often simpler when the team wants one dominant partitioned-log model and already has that operating muscle. Compare failure modes, ordering needs, retention, and tooling, not logos.'] },
    ],
  },
  {
    slug: 'apache-flink',
    title: 'Apache Flink',
    category: 'backend-tools',
    summary: 'Run stateful computations over bounded and unbounded streams with event time, checkpoints, and fault tolerance.',
    level: 'Advanced',
    minutes: 10,
    tags: ['stream processing', 'state', 'event time', 'checkpoints'],
    domain: 'Distributed',
    takeaway: 'Flink is a stateful stream processor first. Batch jobs are a bounded-stream case, not a separate personality.',
    diagram: ['Source', 'Key by entity', 'Stateful operator', 'Checkpoint', 'Sink'],
    codeLabel: 'WordCount.java',
    codeNote: 'This teaching example uses an in-memory source. Production jobs normally read from Kafka, Pulsar, files, or another connector.',
    code: `var env = StreamExecutionEnvironment.getExecutionEnvironment();\n\nvar events = env.fromElements("java", "flink", "java");\nevents.map(String::toUpperCase)\n    .print();\n\nenv.execute("uppercase-events");`,
    links: [
      { label: 'Flink documentation', url: 'https://flink.apache.org/documentation/' },
      { label: 'Flink architecture', url: 'https://flink.apache.org/what-is-flink/flink-architecture/' },
      { label: 'Flink concepts overview', url: 'https://nightlies.apache.org/flink/flink-docs-master/docs/concepts/overview/' },
    ],
    sections: [
      { heading: 'The processing model', paragraphs: ['Flink builds a dataflow graph from sources, transformations, and sinks. Operators can be keyed so each entity gets local state, and parallel subtasks process the graph across a cluster.', 'Event time lets a job reason about when an event happened rather than only when it arrived. Watermarks express progress through out-of-order data and make windowing a correctness decision, not merely a timer.'] },
      { heading: 'State and recovery', bullets: ['Managed state lets operators remember aggregates, timers, sessions, and other per-key facts.', 'Checkpoints capture a consistent view of operator state and positions so a failed job can recover.', 'Exactly-once state consistency does not automatically make an external side effect exactly once. The sink needs a compatible transaction or idempotency design.', 'Backpressure propagates through the graph when a downstream operator cannot keep up.'] },
      { heading: 'When it fits', paragraphs: ['Use Flink for long-running pipelines, event-driven applications, streaming ETL, and analytics that need state, time semantics, and recovery. A simple stateless transformation may not need a distributed runtime with this much machinery.'] },
    ],
  },
  {
    slug: 'aerospike',
    title: 'Aerospike under the hood',
    category: 'data-services',
    summary: 'Trace Aerospike from the smart client partition map through 4096 logical partitions, master/replica succession lists, storage engines, transactions, UDFs, change notifications, and cluster recovery.',
    level: 'Advanced',
    minutes: 20,
    tags: ['Aerospike', 'partitions', 'replication', 'strong consistency', 'UDF', 'XDR', 'operations'],
    domain: 'Data',
    takeaway: 'Aerospike does not use Redis-style slots. A namespace has 4096 logical partitions, the client routes directly to the partition owner, and the cluster continuously reassigns master and replica copies as membership changes.',
    diagram: ['Aerospike smart client', 'RIPEMD160 digest', 'Partition 0..4095', 'Master copy', 'Replica succession list', 'SSD / memory', 'Metrics / XDR'],
    codeLabel: 'AerospikeInternalsExample.java',
    codeLanguage: 'Java / Aerospike policies',
    codeNote: 'The Java client needs aerospike-client and an Aerospike server listening on localhost:3000. The generation policy makes the concurrency decision visible.',
    code: `import com.aerospike.client.AerospikeClient;\nimport com.aerospike.client.Key;\nimport com.aerospike.client.Bin;\nimport com.aerospike.client.Operation;\nimport com.aerospike.client.policy.CommitLevel;\nimport com.aerospike.client.policy.GenerationPolicy;\nimport com.aerospike.client.policy.WritePolicy;\n\nvar client = new AerospikeClient("localhost", 3000);\nvar key = new Key("demo", "users", "user-42");\n\n// One record is the natural atomic boundary.\nvar write = new WritePolicy(client.writePolicyDefault);\nwrite.commitLevel = CommitLevel.COMMIT_ALL;\nclient.operate(write, key,\n    Operation.add(new Bin("login-count", 1)),\n    Operation.put(new Bin("last-state", "active"))\n);\n\nvar record = client.get(null, key);\nvar compareAndSet = new WritePolicy(client.writePolicyDefault);\ncompareAndSet.generationPolicy = GenerationPolicy.EXPECT_GEN_EQUAL;\ncompareAndSet.generation = record.generation;\nclient.put(compareAndSet, key, new Bin("state", "verified"));\n\nclient.close();\n\n# Inspect partition ownership and node health with Aerospike tools:\nasadm -e "show distribution"\nasadm -e "show statistics like cluster_size"\nasinfo -v statistics\nasloglatency -h`,
    links: [
      { label: 'Aerospike architecture overview', url: 'https://aerospike.com/docs/database/learn/architecture/' },
      { label: 'Data distribution and partitions', url: 'https://aerospike.com/docs/database/learn/architecture/clustering/data-distribution/' },
      { label: 'Aerospike consistency modes', url: 'https://aerospike.com/docs/database/learn/architecture/clustering/consistency-modes' },
      { label: 'Aerospike Java transactions', url: 'https://aerospike.com/docs/develop/client/java/usage/multi/transactions/' },
      { label: 'Aerospike Java policies', url: 'https://aerospike.com/docs/develop/client/java/policies/' },
      { label: 'Aerospike Lua UDFs', url: 'https://aerospike.com/docs/database/advanced/udf/' },
      { label: 'Storage compression', url: 'https://aerospike.com/docs/database/manage/namespace/storage/compression' },
      { label: 'Change notifications', url: 'https://aerospike.com/docs/connectors/streaming/common/change-notification' },
      { label: 'Aerospike logging', url: 'https://aerospike.com/docs/database/8.0.0/manage/logging/logs' },
      { label: 'Prometheus monitoring stack', url: 'https://aerospike.com/docs/database/observe/monitor/configure/prometheus/' },
      { label: 'Aerospike latency histograms', url: 'https://aerospike.com/docs/database/observe/latency' },
    ],
    sections: [
      { heading: '1. What happens after a request arrives', paragraphs: ['The Aerospike Java client is cluster-aware. It maintains a partition map, opens connection pools to nodes, hashes the record key to a digest, and sends a record operation directly to the node that currently owns the master copy of that partition.', 'During a cluster change, the client can learn a newer partition map and retry or reroute the request. This is the important contrast with a dumb proxy: the client participates in data location and can usually reach the owner in one network hop.'] },
      { heading: '2. Partitions instead of Redis slots', paragraphs: ['Each namespace is divided into 4096 logical partitions. Aerospike hashes the user key into a fixed digest and uses 12 bits to select the partition. The partition is the unit of ownership and migration; it is not a user-visible shard name that an application must manage.', 'Each partition has a succession list. The first node is the master copy, and subsequent nodes hold replica copies according to the namespace replication factor. This list changes as nodes join, leave, or recover.', 'A record is therefore located by namespace plus digest-derived partition, not by a Redis-style 16,384-slot calculation. The application should not invent its own manual shard map.'] },
      { heading: '3. How Aerospike expands horizontally', bullets: ['Add a peer node with compatible cluster configuration and the cluster membership protocol incorporates it.', 'The data migration module redistributes partitions so the new node receives a balanced share of masters and replicas.', 'The client learns the new partition map and routes new requests to the new owners.', 'During migration, Aerospike keeps replica coverage as far as the current cluster state permits and moves data without requiring an application-level re-sharding job.', 'Remove or repair nodes through controlled cluster operations; watch migration queues, unavailable partitions, storage pressure, and client timeouts while capacity changes.'] },
      { heading: '4. Shards, masters, and replicas', paragraphs: ['Aerospike uses a shared-nothing peer cluster. There is no permanent global master node. For one partition, one node is the master and zero or more nodes are replicas. A namespace replication-factor of 2 means two copies of every partition: one master and one replica.', 'In normal operation, writes go to the master and replication follows the namespace and write policy. With synchronous replication or strong-consistency settings, the server waits for the required replica acknowledgements before reporting success. With availability-oriented settings, the system can continue serving through more failure modes but may expose stale reads or lose writes during a partition.', 'Rack awareness places copies in different racks or availability zones. Replication factor without failure-domain separation is not a complete disaster-recovery design.'] },
      { heading: '5. Transactions and concurrency control', paragraphs: ['The ordinary atomic boundary is one record. Operations in one operate() call can update multiple bins and are applied as a single record transaction. A generation policy provides optimistic concurrency: read generation N, write only if the record is still generation N, and retry when another writer won.', 'Aerospike also supports multi-record transaction workflows in compatible server and client versions. They lock the participating records and can return an in-doubt outcome when the network fails after the server may have applied the operation. The application must use the documented transaction policy and recovery path, not blindly retry every timeout.', 'Commit level is a deliberate tradeoff. Commit to master gives lower write coordination. Commit all waits for replica participation and gives a stronger acknowledgement story at additional latency.'] },
      { heading: '6. Scripting and server-side UDFs', paragraphs: ['Aerospike UDFs are Lua modules registered with the cluster and executed by the server. A record UDF can read and update one record inside the record transaction flow. A stream UDF can process records returned from a query or secondary-index operation.', 'Use native bin operations, List/Map operations, expressions, or operate() first. UDFs add execution overhead and operational surface area. A record UDF can lock the record and block other writes, so a long script is a production concurrency problem, not just a slow function.', 'UDF modules are cluster-managed artifacts. Version and deploy them like code, observe their execution, and keep Lua inputs bounded.'] },
      { heading: '7. Is Aerospike data compressed?', paragraphs: ['Aerospike can compress records at the storage layer using none, LZ4, Snappy, or Zstandard, configured per namespace. The server decompresses data as needed to perform operations and recompresses it when persisting. This is storage compression, not a promise that every in-memory object has the same compressed representation.', 'Compression trades write CPU and latency for lower storage use. Existing records can remain compressed even after configuration changes until they are updated, so measure the namespace compression ratio and actual device savings.', 'Aerospike also gives you a storage choice: memory, SSD, persistent memory, and storage-backed memory designs. Capacity planning must include the primary index, record data, replication factor, migration headroom, and stop-write thresholds.'] },
      { heading: '8. Notifications and change capture', paragraphs: ['Aerospike is not a Redis Pub/Sub server. It does not provide a general ephemeral channel that every application can subscribe to for arbitrary messages.', 'For record changes, Aerospike change notification uses the XDR machinery to publish inserts, updates, touches, and deletes to outbound connectors or an XDR proxy. Aerospike Connect can convert those notifications to HTTP, Kafka, Elasticsearch, or other downstream integration formats.', 'Treat change notification as change capture, not as a perfectly ordered durable event log. Notifications can be grouped or reordered across connectors, and the downstream system needs an idempotency and reconciliation strategy. Use Kafka or Pulsar when you need retained topics, consumer groups, replay, and schema governance.'] },
      { heading: '9. Monitoring and logging', bullets: ['asadm gives cluster-wide operational views such as node health, namespace statistics, distribution, migrations, and stop-write state.', 'asinfo exposes node-specific statistics and configuration details. Use it when the cluster-wide view needs a single-node drill-down.', 'asloglatency reads Aerospike latency histograms and helps separate client, transaction, storage, fabric, and migration bottlenecks.', 'The Prometheus exporter translates Aerospike metrics for Prometheus and Grafana. Alert on unavailable partitions, migration backlog, stop-writes, storage and memory availability, client errors, transaction timeouts, replication health, and XDR lag.', 'Logs are emitted through contexts and sinks such as console, file, journald, or syslog. Keep normal production severity controlled; detail-level logs can include record keys and create enormous volume.', 'For a useful incident timeline, correlate client timeout logs, server transaction histograms, cluster membership changes, migration events, storage pressure, and downstream connector lag.'] },
      { heading: '10. Failure, failover, and the fail-store story', paragraphs: ['When a node fails, the cluster changes the succession lists and promotes an available replica to master where the configured consistency mode permits it. The smart client learns the new map and reroutes requests. This is automatic recovery, not a reason to skip capacity headroom or failure-domain planning.', 'In strong-consistency mode, committed writes are protected by replica coordination and partitions can become unavailable rather than accepting conflicting writes. In availability-oriented mode, the cluster favors serving requests during partitions, which can mean stale reads or conflict resolution later.', 'Persistence depends on the namespace storage engine and edition. SSD-backed namespaces persist data on devices, while in-memory designs may require storage-backed configuration or replica refill after a restart. When storage or memory crosses stop-write thresholds, user writes can fail while reads and some internal recovery work continue. That is the database protecting itself, not a random outage.', 'Backups, XDR to another datacenter, RF and rack placement, stop-write alerts, restore drills, and a tested client retry policy are the failure plan. A timeout can be in-doubt: log the key and operation identity safely, reconcile, and only retry according to the policy.'] },
      { heading: 'Interview answer in one minute', paragraphs: ['Say: “Aerospike is a shared-nothing peer cluster. Each namespace has 4096 logical partitions, and the key digest maps a record to one partition. The smart client knows the partition map and sends the request directly to the master. Replica copies follow a succession list and the replication factor. One record is the normal atomic transaction boundary; generation policies provide optimistic compare-and-set, and commit level controls replica acknowledgement. Lua UDFs execute server-side but can lock records and are slower than native operations. Storage can be memory or persistent devices, with optional LZ4, Snappy, or Zstandard compression. Change notifications use XDR and connectors, not Redis-style Pub/Sub. I monitor asadm, asinfo, asloglatency, Prometheus metrics, migrations, stop-writes, and XDR lag.”'] },
      { heading: 'Local lab idea', paragraphs: ['A useful Aerospike lab should show a key’s partition, the succession list, RF2 master/replica placement, a generation conflict, commit-to-master versus commit-all, a short Lua UDF, compression ratio, migration progress after adding a node, stop-write thresholds, and a change notification delivered to a test connector.'] },
    ],
  },
  {
    slug: 'clickhouse',
    title: 'ClickHouse',
    category: 'data-services',
    summary: 'A column-oriented SQL database built for fast analytical scans, aggregation, compression, and high-ingestion workloads.',
    level: 'Intermediate',
    minutes: 9,
    tags: ['OLAP', 'columnar', 'MergeTree', 'vectorized execution'],
    domain: 'Data',
    takeaway: 'ClickHouse wins when queries read and aggregate columns over lots of rows, not when every request updates one customer record.',
    codeLabel: 'events.sql',
    codeLanguage: 'SQL',
    codeNote: 'The ORDER BY expression defines the primary sort key for MergeTree storage and should match common filtering patterns.',
    code: `CREATE TABLE events (\n    tenant_id UInt32,\n    occurred_at DateTime,\n    event_name LowCardinality(String),\n    value Float64\n) ENGINE = MergeTree\nORDER BY (tenant_id, occurred_at);\n\nSELECT event_name, sum(value)\nFROM events\nWHERE tenant_id = 42\nGROUP BY event_name;`,
    links: [
      { label: 'What is ClickHouse?', url: 'https://clickhouse.com/docs/get-started/about/intro' },
      { label: 'ClickHouse distinctive features', url: 'https://clickhouse.com/docs/get-started/about/distinctive-features' },
      { label: 'ClickHouse architecture overview', url: 'https://clickhouse.com/docs/development/architecture' },
    ],
    sections: [
      { heading: 'Why columns matter', paragraphs: ['Analytical queries often read a few columns across many rows. Columnar storage keeps each column together, so the engine can avoid loading unrelated fields and apply compression and vectorized operations efficiently.', 'ClickHouse stores MergeTree data in sorted parts that are merged in the background. The sort key and data-skipping structures are therefore part of query design, not a final tuning garnish.'] },
      { heading: 'Strengths', bullets: ['Fast scans and aggregations over large datasets.', 'SQL interface with vectorized execution and aggressive pruning.', 'Compression that benefits repeated or low-cardinality values.', 'Parallel execution across shards and replicas when deployed as a cluster.', 'Materialized views and specialized table engines for common workloads.'] },
      { heading: 'The tradeoff', paragraphs: ['ClickHouse is optimized for analytical access patterns. High-frequency row-by-row updates, strict transactional workflows, and arbitrary point lookups are not its happy place. Model events append-first, choose keys around real queries, and separate serving OLTP from analytical workloads when both need to be excellent.'] },
    ],
  },
  {
    slug: 'apache-pinot',
    title: 'Apache Pinot',
    category: 'data-services',
    summary: 'A distributed real-time OLAP datastore for low-latency, high-concurrency queries over fresh event data.',
    level: 'Advanced',
    minutes: 9,
    tags: ['real-time OLAP', 'segments', 'low latency', 'stream ingestion'],
    domain: 'Data',
    takeaway: 'Pinot is for analytics people ask while the event stream is still warm, often through a user-facing product surface.',
    codeLabel: 'query.sql',
    codeLanguage: 'SQL / Pinot',
    codeNote: 'The query is sent to a Pinot broker, which routes it to the servers holding the relevant segments.',
    code: `SELECT region, COUNT(*) AS orders\nFROM orders\nWHERE __time >= ago('PT1H')\nGROUP BY region\nORDER BY orders DESC\nLIMIT 10;`,
    links: [
      { label: 'Apache Pinot introduction', url: 'https://docs.pinot.apache.org' },
      { label: 'Pinot Java query client', url: 'https://docs.pinot.apache.org/build-with-pinot/connectors-clients-apis/client-libraries/java' },
      { label: 'Pinot client libraries', url: 'https://docs.pinot.apache.org/build-with-pinot/connectors-clients-apis/client-libraries' },
    ],
    sections: [
      { heading: 'The serving model', paragraphs: ['Pinot ingests from streaming or batch sources, creates segments, and serves queries through brokers and servers. The system is shaped around fresh data, high query concurrency, and predictable low latency for dashboards, product analytics, and user-facing exploration.', 'A broker routes a query. Servers scan the relevant segments using indexes, dictionaries, sorted columns, and other structures chosen for the table schema.'] },
      { heading: 'Pinot versus ClickHouse', bullets: ['Pinot emphasizes real-time ingestion and interactive serving over event data.', 'ClickHouse is often the more flexible choice for broad ad-hoc analytical queries and simpler single-system deployments.', 'Pinot’s table configuration and component topology are more specialized, which can pay off when freshness and concurrency are hard requirements.', 'Both reward schema design around actual filters, groupings, cardinality, and retention.'] },
      { heading: 'Questions to answer first', paragraphs: ['How fresh must data be before it is queryable? What is the p95 or p99 query target under peak concurrency? Which dimensions are filtered most often? Can late events arrive? The answers determine ingestion mode, indexes, partitioning, retention, and whether Pinot is the right tool rather than merely an impressive one.'] },
    ],
  },
  {
    slug: 'apache-iceberg',
    title: 'Apache Iceberg',
    category: 'lakehouse-formats',
    summary: 'An open table format that brings schemas, snapshots, time travel, and reliable evolution to huge datasets in object storage.',
    level: 'Advanced',
    minutes: 10,
    tags: ['lakehouse', 'object storage', 'time travel', 'schema evolution'],
    domain: 'Data',
    takeaway: 'Iceberg is the table layer over files in cold or warm object storage. It is not the file format and not the query engine.',
    diagram: ['Catalog', 'Table metadata', 'Snapshot', 'Manifest list', 'Data files'],
    codeLabel: 'table.sql',
    codeLanguage: 'Spark SQL',
    codeNote: 'Syntax varies slightly by engine. This example shows the table-oriented operations Iceberg makes possible.',
    code: `CREATE TABLE prod.db.events (\n    event_id STRING,\n    occurred_at TIMESTAMP,\n    payload STRING\n) USING iceberg;\n\nALTER TABLE prod.db.events ADD COLUMN source STRING;\n\nSELECT * FROM prod.db.events\nFOR SYSTEM_VERSION AS OF 123456789;`,
    links: [
      { label: 'Iceberg documentation', url: 'https://iceberg.apache.org/docs/latest/' },
      { label: 'Iceberg table specification', url: 'https://iceberg.apache.org/spec/' },
      { label: 'Iceberg project site', url: 'https://iceberg.apache.org/' },
    ],
    sections: [
      { heading: 'Cold storage, made table-like', paragraphs: ['Object storage is cheap and durable, but a directory full of files is not a table contract. Iceberg adds metadata and snapshots so engines can find the right files, evolve schemas safely, and read a consistent table state.', 'That makes it useful for historical or cold data that should remain queryable without loading everything into a serving database. Compute can come and go while the table and its files remain.'] },
      { heading: 'The metadata chain', bullets: ['A catalog points readers to the current table metadata.', 'A snapshot describes one committed table state.', 'Manifest lists and manifests identify data files and their partition and column statistics.', 'Readers use those statistics to prune files before scanning them.', 'Expiration and compaction are maintenance work, not automatic permission to forget old data.'] },
      { heading: 'Why it is not Parquet', paragraphs: ['Parquet stores the rows and columns inside a file. Iceberg tracks a collection of files as a table and adds transactions, snapshots, partition evolution, and schema management. They commonly appear together: Iceberg as the table abstraction, Parquet as the data-file format.'] },
    ],
  },
  {
    slug: 'parquet',
    title: 'Apache Parquet',
    category: 'lakehouse-formats',
    summary: 'A column-oriented file format that uses row groups, column chunks, compression, and statistics for efficient analytical reads.',
    level: 'Intermediate',
    minutes: 8,
    tags: ['columnar', 'file format', 'compression', 'predicate pushdown'],
    domain: 'Data',
    takeaway: 'Parquet is a well-organized file, not a database. Its layout helps readers skip data they do not need.',
    diagram: ['File footer', 'Row groups', 'Column chunks', 'Pages', 'Encoded values'],
    codeLabel: 'query.sql',
    codeLanguage: 'SQL / DuckDB',
    codeNote: 'DuckDB can query Parquet directly. The example highlights column projection and a filter that can prune row groups.',
    code: `SELECT customer_id, sum(amount) AS total\nFROM read_parquet('events/*.parquet')\nWHERE event_date >= DATE '2026-01-01'\nGROUP BY customer_id;`,
    links: [
      { label: 'Parquet documentation', url: 'https://parquet.apache.org/docs/_print' },
      { label: 'Parquet concepts', url: 'https://parquet.apache.org/docs/concepts' },
      { label: 'Parquet file format', url: 'https://parquet.apache.org/docs/file-format/_print' },
    ],
    sections: [
      { heading: 'The file shape', paragraphs: ['A Parquet file is divided into row groups. Each row group has a column chunk for every column, and chunks are divided into pages that carry encoded values and metadata.', 'A reader can load only the requested columns and use statistics such as minimum and maximum values to skip row groups that cannot match a filter. That is why column selection and file organization matter so much.'] },
      { heading: 'Compression is a layout decision', bullets: ['Values in one column tend to have similar types and patterns, which makes encoding and compression effective.', 'Dictionary encoding can work well for repeated values such as status or country.', 'Row-group size trades metadata and parallelism against scan efficiency.', 'Too many tiny files create overhead for object stores and query engines.', 'Partition directories can help, but over-partitioning creates a different kind of mess.'] },
      { heading: 'Parquet in a data platform', paragraphs: ['Use Parquet for portable analytical files exchanged among engines such as Spark, Flink, Trino, DuckDB, and Python tools. Add a table format such as Iceberg when you need snapshots, schema evolution, and a reliable multi-file table contract.'] },
    ],
  },
  {
    slug: 'openshift',
    title: 'OpenShift',
    category: 'kubernetes',
    summary: 'Understand the OpenShift layer around Kubernetes: Projects, Routes, image workflows, Operators, and platform security.',
    level: 'Foundations',
    minutes: 10,
    tags: ['Projects', 'Routes', 'Operators', 'S2I', 'SCC'],
    domain: 'OpenShift',
    takeaway: 'OpenShift keeps Kubernetes APIs and workload concepts while adding opinionated platform workflows, security controls, and operator-managed services.',
    diagram: ['developer or pipeline', 'OpenShift API', 'Project and RBAC', 'Build or image workflow', 'Deployment and Service', 'Route'],
    codeLabel: 'openshift-app.yaml',
    codeLanguage: 'YAML',
    codeNote: 'The examples use placeholders only. A Route exposes a Service, while a BuildConfig or external pipeline can produce the image consumed by the workload.',
    code: `apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: api\n  namespace: demo\nspec:\n  replicas: 2\n  selector:\n    matchLabels:\n      app: api\n  template:\n    metadata:\n      labels:\n        app: api\n    spec:\n      containers:\n        - name: api\n          image: image-registry.openshift-image-registry.svc:5000/demo/api:latest\n          ports:\n            - containerPort: 8080\n---\napiVersion: route.openshift.io/v1\nkind: Route\nmetadata:\n  name: api\n  namespace: demo\nspec:\n  to:\n    kind: Service\n    name: api\n  tls:\n    termination: edge`,
    links: [
      { label: 'OpenShift documentation', url: 'https://docs.openshift.com/container-platform/latest/welcome/index.html' },
      { label: 'Projects', url: 'https://docs.openshift.com/container-platform/latest/applications/projects/working-with-projects.html' },
      { label: 'Routes', url: 'https://docs.openshift.com/container-platform/latest/networking/routes/route-configuration.html' },
      { label: 'Security context constraints', url: 'https://docs.openshift.com/container-platform/latest/authentication/managing-security-context-constraints.html' },
    ],
    sections: [
      { heading: 'OpenShift and Kubernetes', paragraphs: ['OpenShift is a Kubernetes platform, not a separate container orchestration model. Kubernetes objects such as Pods, Deployments, Services, RBAC, and namespaces remain important, while OpenShift adds platform APIs and opinionated operational defaults.'] },
      { heading: 'Projects and access', bullets: ['A Project is a tenant-oriented namespace boundary with collaboration and policy conventions.', 'RBAC still determines which users and service accounts can act on API resources.', 'Quotas and LimitRanges make resource consumption explicit within a Project.', 'The OpenShift OAuth layer authenticates users; authorization is still a separate RBAC decision.'] },
      { heading: 'Build and image workflows', bullets: ['BuildConfig and Source-to-Image can turn source code into an image through a builder workflow.', 'ImageStreams track image metadata and can trigger downstream actions when tags change.', 'The integrated registry is a platform service whose access and storage need operational configuration.', 'External CI systems can also build and publish images; OpenShift does not require S2I for every workload.'] },
      { heading: 'Networking and security', bullets: ['A Route exposes a Service through the OpenShift router and can select edge, re-encrypt, or passthrough TLS behavior.', 'SecurityContextConstraints constrain privileged operations, host access, capabilities, and related Pod security choices.', 'Use the least-privileged service account and avoid treating a Secret object as a substitute for a full secret-management strategy.'] },
      { heading: 'Operators and upgrades', paragraphs: ['Operators reconcile custom resources and encode operational knowledge for databases, messaging systems, and platform components. ClusterOperator status is a key health signal during installation and upgrades; degraded operators should be investigated before proceeding.'] },
    ],
  },
  {
    slug: 'kubernetes-components',
    title: 'Kubernetes components',
    category: 'kubernetes',
    summary: 'See how the control plane, worker nodes, API objects, and container runtime cooperate to keep workloads running.',
    level: 'Foundations',
    minutes: 9,
    tags: ['control plane', 'worker node', 'architecture', 'kubelet'],
    domain: 'Kubernetes',
    takeaway: 'The API server is the front door, controllers reconcile desired state, the scheduler chooses nodes, and kubelets run the assigned Pods.',
    image: { src: '/doc-images/image1.png', alt: 'Docker and Kubernetes topic map from the uploaded document', caption: 'The uploaded document’s Docker-to-Kubernetes topic map, used as the starting point for this section.' },
    diagram: ['kubectl or client', 'API server', 'etcd', 'controllers and scheduler', 'kubelet and runtime', 'Pods'],
    codeLabel: 'cluster-components.yaml',
    codeLanguage: 'YAML',
    codeNote: 'This is a teaching map, not a manifest. Kubernetes components are processes; the YAML below is the kind of object they reconcile.',
    code: `apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: web\n  template:\n    metadata:\n      labels:\n        app: web\n    spec:\n      containers:\n        - name: web\n          image: example/web:1.0`,
    links: [
      { label: 'Kubernetes components', url: 'https://kubernetes.io/docs/concepts/overview/components/' },
      { label: 'Cluster architecture', url: 'https://kubernetes.io/docs/concepts/architecture/' },
    ],
    sections: [
      { heading: 'The control plane', bullets: ['kube-apiserver exposes the Kubernetes API and validates requests.', 'etcd stores the cluster state that the API server reads and writes.', 'The scheduler chooses a suitable node for newly created Pods.', 'Controller managers run reconciliation loops, such as Deployment and Job controllers.'] },
      { heading: 'The worker node', bullets: ['kubelet watches the API for Pods assigned to its node and keeps their containers running.', 'The container runtime starts and stops containers through the Container Runtime Interface.', 'The node network implementation gives Pods connectivity; kube-proxy or an integrated proxy routes Service traffic.', 'A node reports status back through the API server.'] },
      { heading: 'The reconciliation loop', paragraphs: ['A manifest describes desired state. Controllers compare that state with what exists, then create, update, or remove dependent objects until the observed state converges. Kubernetes is not a script that runs once; it is a set of continuous repair loops.'] },
    ],
  },
  {
    slug: 'kubernetes-object-catalog',
    title: 'Kubernetes object catalog',
    category: 'kubernetes',
    summary: 'Map the main built-in object families so a manifest tells you what owns the workload, how it is reached, and where its data lives.',
    level: 'Foundations',
    minutes: 10,
    tags: ['objects', 'api resources', 'manifests', 'kinds'],
    domain: 'Kubernetes',
    takeaway: 'A Kubernetes object has a kind, metadata, and a spec that expresses desired state; its status records what the cluster observes.',
    diagram: ['metadata', 'spec: desired state', 'controller', 'status: observed state'],
    codeLabel: 'object-families.yaml',
    codeLanguage: 'YAML',
    codeNote: 'These are representative object headers. The full examples live in the focused subjects below.',
    code: `# Workloads\nkind: Pod\nkind: Deployment\nkind: StatefulSet\nkind: DaemonSet\nkind: Job\nkind: CronJob\n\n# Networking\nkind: Service\nkind: Ingress\nkind: NetworkPolicy\n\n# Configuration and identity\nkind: ConfigMap\nkind: Secret\nkind: ServiceAccount\n\n# Storage and policy\nkind: PersistentVolume\nkind: PersistentVolumeClaim\nkind: StorageClass\nkind: ResourceQuota\nkind: LimitRange\nkind: PodDisruptionBudget\n\n# Access control and extension\nkind: Role\nkind: RoleBinding\nkind: ClusterRole\nkind: ClusterRoleBinding\nkind: CustomResourceDefinition`,
    links: [
      { label: 'Kubernetes API reference', url: 'https://kubernetes.io/docs/reference/kubernetes-api/' },
      { label: 'Objects in Kubernetes', url: 'https://kubernetes.io/docs/concepts/overview/working-with-objects/' },
      { label: 'Object management', url: 'https://kubernetes.io/docs/concepts/overview/working-with-objects/object-management/' },
    ],
    sections: [
      { heading: 'Workload objects', paragraphs: ['Pod is the smallest deployable unit. Deployment manages interchangeable Pods for stateless applications. StatefulSet gives Pods stable identity and storage relationships. DaemonSet places a Pod on selected nodes. Job and CronJob run finite work once or on a schedule.'] },
      { heading: 'The supporting objects', bullets: ['Service gives a changing set of Pods a stable network identity.', 'ConfigMap and Secret provide configuration data to workloads; Secret data still needs access control and careful handling.', 'PersistentVolume, PersistentVolumeClaim, and StorageClass model storage supply, demand, and provisioning.', 'Role, RoleBinding, ClusterRole, and ClusterRoleBinding define API permissions.', 'ResourceQuota, LimitRange, and PodDisruptionBudget constrain resource usage or protect availability.'] },
      { heading: 'How to read a manifest', paragraphs: ['Start with kind and metadata.name. Then read spec as the desired state and inspect labels, selectors, references, and ownership. A resource that exists is not necessarily healthy; use status and events to see what controllers observed.'] },
    ],
  },
  {
    slug: 'kubernetes-workloads',
    title: 'Kubernetes workload objects',
    category: 'kubernetes',
    summary: 'Choose between Pods, Deployments, StatefulSets, DaemonSets, Jobs, and CronJobs based on identity, replacement, and completion behavior.',
    level: 'Intermediate',
    minutes: 10,
    tags: ['Pod', 'Deployment', 'StatefulSet', 'Job', 'CronJob'],
    domain: 'Kubernetes',
    takeaway: 'Use the highest-level workload object that matches the behavior you need; hand-written Pods are rarely the right production abstraction.',
    diagram: ['Deployment', 'ReplicaSet', 'interchangeable Pods'],
    codeLabel: 'workloads.yaml',
    codeLanguage: 'YAML',
    codeNote: 'The Deployment owns a ReplicaSet, and the ReplicaSet keeps the requested number of Pods alive.',
    code: `apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: api\nspec:\n  replicas: 2\n  selector:\n    matchLabels:\n      app: api\n  template:\n    metadata:\n      labels:\n        app: api\n    spec:\n      containers:\n        - name: api\n          image: example/api:1.0\n          ports:\n            - containerPort: 8080\n---\napiVersion: batch/v1\nkind: CronJob\nmetadata:\n  name: nightly-report\nspec:\n  schedule: "0 2 * * *"\n  jobTemplate:\n    spec:\n      template:\n        spec:\n          restartPolicy: Never\n          containers:\n            - name: report\n              image: example/report:1.0`,
    links: [
      { label: 'Workload management', url: 'https://kubernetes.io/docs/concepts/workloads/controllers/' },
      { label: 'Pods', url: 'https://kubernetes.io/docs/concepts/workloads/pods/' },
      { label: 'Deployments', url: 'https://kubernetes.io/docs/concepts/workloads/controllers/deployment/' },
    ],
    sections: [
      { heading: 'Which object fits?', bullets: ['Pod: one tightly coupled unit, mainly useful as a low-level building block or debugging target.', 'Deployment: stateless replicas that can be replaced and rolled out.', 'StatefulSet: stable ordinal identity, ordered operations, and per-Pod storage relationships.', 'DaemonSet: one Pod on every matching node, such as logging or node monitoring.', 'Job: run-to-completion work.', 'CronJob: create Jobs on a schedule.'] },
      { heading: 'Selectors are the join key', paragraphs: ['A controller’s selector must match the labels in its Pod template. If the selector and template labels disagree, the controller cannot manage the Pods you think it manages. Keep labels deliberate and avoid reusing a selector for unrelated workloads.'] },
      { heading: 'Replacement is normal', paragraphs: ['Pods are disposable. A node failure, rollout, eviction, or image update can replace them. Put durable identity in a StatefulSet or external system, and put durable data in storage rather than inside the writable container filesystem.'] },
    ],
  },
  {
    slug: 'kubernetes-service-networking',
    title: 'Service and networking objects',
    category: 'kubernetes',
    summary: 'Expose a changing set of Pods through a stable name, then add HTTP routing and traffic policy only where the application needs it.',
    level: 'Foundations',
    minutes: 9,
    tags: ['Service', 'EndpointSlice', 'Ingress', 'NetworkPolicy', 'DNS'],
    domain: 'Kubernetes',
    takeaway: 'A Service is a stable endpoint backed by Pods selected through labels; it is not the application process itself.',
    diagram: ['client', 'Service: api', 'EndpointSlice', 'selected Pods'],
    codeLabel: 'service-networking.yaml',
    codeLanguage: 'YAML',
    codeNote: 'The Service selects Pods labelled app: api. The Ingress routes external HTTP traffic to the Service; it needs an ingress controller or Gateway implementation.',
    code: `apiVersion: v1\nkind: Service\nmetadata:\n  name: api\nspec:\n  selector:\n    app: api\n  ports:\n    - name: http\n      port: 80\n      targetPort: 8080\n  type: ClusterIP\n---\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: api\nspec:\n  rules:\n    - host: api.example.test\n      http:\n        paths:\n          - path: /\n            pathType: Prefix\n            backend:\n              service:\n                name: api\n                port:\n                  number: 80`,
    links: [
      { label: 'Services', url: 'https://kubernetes.io/docs/concepts/services-networking/service/' },
      { label: 'Ingress', url: 'https://kubernetes.io/docs/concepts/services-networking/ingress/' },
      { label: 'NetworkPolicy', url: 'https://kubernetes.io/docs/concepts/services-networking/network-policies/' },
    ],
    sections: [
      { heading: 'Service types', bullets: ['ClusterIP is the default and exposes the service inside the cluster.', 'NodePort opens a port on each node and is mostly a building block for other exposure methods.', 'LoadBalancer asks an integrated cloud or platform provider for an external load balancer.', 'ExternalName returns a DNS alias and does not select Pods.'] },
      { heading: 'What happens after selection', paragraphs: ['Kubernetes maintains EndpointSlice objects for the Pods that match the Service selector. A service proxy routes traffic to ready endpoints. Because Pods can be replaced, clients should call the Service DNS name instead of a Pod IP.'] },
      { heading: 'External HTTP routing', paragraphs: ['Ingress describes HTTP and HTTPS routing rules. It does not implement the proxy by itself; an ingress controller must watch the object and configure the data plane. Gateway API is the newer, more expressive option for many platforms.'] },
    ],
  },
  {
    slug: 'kubernetes-persistent-storage',
    title: 'PersistentVolume, PVC, and StorageClass',
    category: 'kubernetes',
    summary: 'Connect a Pod to storage that survives Pod replacement by separating the storage supply, the application request, and the mounting point.',
    level: 'Foundations',
    minutes: 9,
    tags: ['PersistentVolume', 'PVC', 'StorageClass', 'volumeMounts'],
    domain: 'Kubernetes',
    takeaway: 'A PVC is the application’s request for storage; it is not the storage device. Kubernetes binds it to a PV, often provisioned through a StorageClass.',
    diagram: ['Pod', 'PVC request', 'PV binding', 'StorageClass provisioner', 'persistent backend'],
    codeLabel: 'storage.yaml',
    codeLanguage: 'YAML',
    codeNote: 'In many clusters, omitting storageClassName lets the default StorageClass dynamically provision a matching PV.',
    code: `apiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: database-data\nspec:\n  accessModes:\n    - ReadWriteOnce\n  resources:\n    requests:\n      storage: 10Gi\n---\napiVersion: v1\nkind: Pod\nmetadata:\n  name: database\nspec:\n  containers:\n    - name: database\n      image: example/database:1.0\n      volumeMounts:\n        - name: data\n          mountPath: /var/lib/database\n  volumes:\n    - name: data\n      persistentVolumeClaim:\n        claimName: database-data`,
    links: [
      { label: 'Persistent volumes', url: 'https://kubernetes.io/docs/concepts/storage/persistent-volumes/' },
      { label: 'Storage Classes', url: 'https://kubernetes.io/docs/concepts/storage/storage-classes/' },
      { label: 'Volume snapshots', url: 'https://kubernetes.io/docs/concepts/storage/volume-snapshots/' },
    ],
    sections: [
      { heading: 'The three storage roles', bullets: ['PersistentVolume represents storage available to the cluster.', 'PersistentVolumeClaim describes what a workload requests: capacity, access mode, and sometimes a class.', 'StorageClass describes how a provisioner dynamically creates storage for matching claims.'] },
      { heading: 'Mounting the claim', paragraphs: ['The Pod references the PVC under volumes, then a container mounts that named volume at a path. The claim is the stable Kubernetes reference; the Pod can be replaced without changing the application’s requested storage.'] },
      { heading: 'Access modes are constraints', paragraphs: ['ReadWriteOnce, ReadOnlyMany, and ReadWriteMany describe how a volume may be attached and mounted. The actual support depends on the storage backend. Do not choose an access mode because it sounds convenient; check what the provisioner implements.'] },
      { heading: 'PVC mistakes', bullets: ['A claim can remain Pending when no PV or StorageClass can satisfy it.', 'Storage is not automatically a backup. Backups and restore testing are separate responsibilities.', 'Deleting a claim can have data-loss consequences depending on the PV reclaim policy.', 'A database usually needs a StatefulSet and operational backup strategy, not only a PVC.'] },
    ],
  },
  {
    slug: 'kubernetes-config-identity-policy',
    title: 'Config, identity, and policy objects',
    category: 'kubernetes',
    summary: 'Keep configuration, workload identity, API permissions, and resource guardrails explicit instead of hiding them inside container images.',
    level: 'Intermediate',
    minutes: 10,
    tags: ['ConfigMap', 'Secret', 'ServiceAccount', 'RBAC', 'ResourceQuota'],
    domain: 'Kubernetes',
    takeaway: 'Configuration and permissions are separate concerns: ConfigMap supplies ordinary settings, Secret protects sensitive data, and RBAC controls API access.',
    diagram: ['ConfigMap or Secret', 'Pod env or volume', 'ServiceAccount', 'RoleBinding', 'API permissions'],
    codeLabel: 'config-rbac.yaml',
    codeLanguage: 'YAML',
    codeNote: 'The placeholder value is intentionally not a real credential. Put sensitive values into an approved secret-management workflow, not source control.',
    code: `apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: api-config\ndata:\n  LOG_LEVEL: info\n---\napiVersion: v1\nkind: Secret\nmetadata:\n  name: api-runtime\nstringData:\n  DATABASE_URL: REPLACE_AT_DEPLOY_TIME\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  name: pod-reader\nrules:\n  - apiGroups: [""]\n    resources: ["pods"]\n    verbs: ["get", "list"]\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: api-pod-reader\nsubjects:\n  - kind: ServiceAccount\n    name: api\nroleRef:\n  kind: Role\n  name: pod-reader\n  apiGroup: rbac.authorization.k8s.io`,
    links: [
      { label: 'ConfigMaps', url: 'https://kubernetes.io/docs/concepts/configuration/configmap/' },
      { label: 'Secrets', url: 'https://kubernetes.io/docs/concepts/configuration/secret/' },
      { label: 'RBAC authorization', url: 'https://kubernetes.io/docs/reference/access-authn-authz/rbac/' },
      { label: 'Resource quotas', url: 'https://kubernetes.io/docs/concepts/policy/resource-quotas/' },
    ],
    sections: [
      { heading: 'Configuration delivery', bullets: ['ConfigMap is for non-confidential configuration.', 'Secret is a Kubernetes API object intended for sensitive values, but access controls, encryption at rest, and external secret management still matter.', 'Environment variables are convenient; mounted files can be better for larger or rotated configuration.', 'Changing a ConfigMap or Secret does not guarantee every process reloads it. The application must support reload or the workload must restart.'] },
      { heading: 'Identity and permissions', paragraphs: ['A Pod can use a ServiceAccount identity when it calls the Kubernetes API. A Role grants permissions in one namespace, and a RoleBinding attaches that grant to a subject. ClusterRole and ClusterRoleBinding operate at cluster scope or can be reused across namespaces.'] },
      { heading: 'Guardrails', bullets: ['ResourceQuota limits aggregate namespace consumption.', 'LimitRange supplies defaults and per-object limits.', 'PodDisruptionBudget describes how many replicas should remain available during voluntary disruptions.', 'NetworkPolicy controls allowed traffic when the installed network implementation enforces it.'] },
    ],
  },
  {
    slug: 'kubernetes-yaml-helm-templates',
    title: 'YAML manifests and Helm templates',
    category: 'kubernetes',
    summary: 'Understand the YAML data structure first, then use Helm’s Go-template expressions to generate environment-specific Kubernetes manifests.',
    level: 'Intermediate',
    minutes: 11,
    tags: ['YAML', 'Helm', 'templates', 'values', 'kubectl'],
    domain: 'Kubernetes',
    takeaway: 'YAML is the manifest data format; Helm templates are a preprocessing language that renders YAML before kubectl or a deployment tool sends it to the API server.',
    diagram: ['values.yaml', 'Helm template', 'rendered YAML', 'kubectl apply', 'Kubernetes API'],
    codeLabel: 'deployment.yaml.tpl',
    codeLanguage: 'YAML / Helm',
    codeNote: 'The left side is Helm template syntax. Helm renders it into ordinary YAML; Kubernetes never evaluates {{ ... }} itself.',
    code: `apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: {{ include "app.fullname" . }}\n  labels:\n    app.kubernetes.io/name: {{ .Chart.Name | quote }}\nspec:\n  replicas: {{ .Values.replicaCount }}\n  selector:\n    matchLabels:\n      app.kubernetes.io/name: {{ .Chart.Name | quote }}\n  template:\n    metadata:\n      labels:\n        app.kubernetes.io/name: {{ .Chart.Name | quote }}\n    spec:\n      containers:\n        - name: {{ .Chart.Name }}\n          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"\n          {{- if .Values.service.port }}\n          ports:\n            - name: http\n              containerPort: {{ .Values.service.port }}\n          {{- end }}`,
    links: [
      { label: 'Helm template guide', url: 'https://helm.sh/docs/chart_template_guide/' },
      { label: 'Helm built-in objects', url: 'https://helm.sh/docs/chart_template_guide/builtin_objects/' },
      { label: 'kubectl apply', url: 'https://kubernetes.io/docs/reference/kubectl/generated/kubectl_apply/' },
      { label: 'YAML specification', url: 'https://yaml.org/spec/' },
    ],
    sections: [
      { heading: 'YAML before templates', bullets: ['Indentation represents nesting; use spaces consistently.', 'Mappings are key-value pairs, while sequences use a dash.', 'Strings that look like numbers, booleans, or special values may need quotes.', 'A multi-document file separates objects with ---.', 'Kubernetes validates the rendered object against the API schema, not your editor’s intent.'] },
      { heading: 'Helm expression anatomy', bullets: ['{{ ... }} evaluates an expression and prints its result.', '{{- and -}} trim surrounding whitespace, which helps keep rendered YAML tidy.', '.Values reads values supplied by values.yaml, a parent chart, or the command line.', '.Chart and .Release expose chart and release metadata.', 'Pipelines pass a value through functions, such as .Chart.Name | quote.', 'if, with, and range conditionally render or repeat sections.'] },
      { heading: 'Render before applying', paragraphs: ['Use helm template to inspect the exact YAML before it reaches the cluster, then use a schema-aware linter or kubectl apply --dry-run=server when a cluster is available. Never debug a generated manifest only from the template source; read the rendered output.'] },
      { heading: 'Helm is not Kubernetes', paragraphs: ['Helm packages and renders charts. Kubernetes stores and reconciles the resulting objects. This separation explains many confusing errors: a template can render invalid YAML, valid YAML with the wrong fields, or valid objects whose selectors and references do not connect.'] },
    ],
  },
  {
    slug: 'docker-kubernetes-map',
    title: 'Docker and Kubernetes: from image to workload',
    category: 'containers-git-architecture',
    summary: 'Use the uploaded document’s Docker-to-Kubernetes map to connect images and containers with Pods, Deployments, and Services.',
    level: 'Foundations',
    minutes: 8,
    tags: ['Docker', 'Kubernetes', 'images', 'containers'],
    domain: 'Containers',
    takeaway: 'Docker packages and runs a container image; Kubernetes schedules and reconciles Pods that contain those containers.',
    image: { src: '/doc-images/image1.png', alt: 'Docker and Kubernetes topic map', caption: 'Source image from look4.docx: a compact map from Docker fundamentals to Kubernetes objects.' },
    diagram: ['Dockerfile', 'image', 'container', 'Pod', 'Deployment', 'Service'],
    codeLabel: 'deployment.yaml',
    codeLanguage: 'YAML',
    codeNote: 'The image is built elsewhere and referenced by the Kubernetes Deployment. Kubernetes does not build the image from this manifest.',
    code: `apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: hello\nspec:\n  replicas: 2\n  selector:\n    matchLabels:\n      app: hello\n  template:\n    metadata:\n      labels:\n        app: hello\n    spec:\n      containers:\n        - name: hello\n          image: example/hello:1.0\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: hello\nspec:\n  selector:\n    app: hello\n  ports:\n    - port: 80\n      targetPort: 8080`,
    links: [
      { label: 'Docker overview', url: 'https://docs.docker.com/get-started/overview/' },
      { label: 'Kubernetes workloads', url: 'https://kubernetes.io/docs/concepts/workloads/' },
    ],
    sections: [
      { heading: 'The vocabulary bridge', bullets: ['Image: immutable-ish package layers identified by a digest and optionally a tag.', 'Container: a running process created from an image.', 'Pod: Kubernetes scheduling unit containing one or more closely coupled containers.', 'Deployment: controller that manages interchangeable Pod replicas.', 'Service: stable network identity for selected Pods.'] },
      { heading: 'What changes in Kubernetes', paragraphs: ['With plain Docker, you often issue lifecycle commands directly against one host. Kubernetes turns the desired workload into API objects and lets controllers, schedulers, nodes, and networking components keep the running system close to that description.'] },
    ],
  },
  {
    slug: 'docker-components',
    title: 'Docker components and image push flow',
    category: 'containers-git-architecture',
    summary: 'Read the Docker client, daemon, image cache, containers, and registry as separate pieces in the build and run path.',
    level: 'Foundations',
    minutes: 8,
    tags: ['Docker client', 'Docker daemon', 'registry', 'image cache'],
    domain: 'Containers',
    takeaway: 'The Docker CLI is a client. The daemon performs the work, using local image data or pulling layers from a registry.',
    images: [
      { src: '/doc-images/image2.png', alt: 'Docker components overview', caption: 'Source image from look4.docx: Docker client, Docker host, containers, image cache, and registry.' },
      { src: '/doc-images/image3.png', alt: 'Docker components and image push flow', caption: 'Source image from look4.docx: Docker client, daemon, containers, image cache, and registry interactions.' },
    ],
    diagram: ['Docker CLI', 'Docker daemon', 'local cache', 'container', 'registry'],
    codeLabel: 'terminal',
    codeLanguage: 'Docker CLI',
    codeNote: 'Use a concrete tag for repeatable deployment. The registry name and image tag below are illustrative, not credentials.',
    code: `docker build -t example/hello:1.0 .\ndocker run --name hello -p 8080:8080 example/hello:1.0\ndocker push example/hello:1.0\n\n# On another host, the daemon can pull the same tag.\ndocker pull example/hello:1.0`,
    links: [
      { label: 'Docker architecture', url: 'https://docs.docker.com/get-started/docker-overview/' },
      { label: 'Docker CLI reference', url: 'https://docs.docker.com/reference/cli/docker/' },
      { label: 'Image distribution', url: 'https://docs.docker.com/engine/reference/commandline/push/' },
    ],
    sections: [
      { heading: 'The pieces', bullets: ['Docker client sends commands through the Docker API.', 'Docker daemon manages images, containers, networks, and volumes.', 'The local image cache avoids downloading layers that are already available.', 'A registry stores and serves image manifests and layers.', 'A container is a process with an isolated filesystem and runtime settings.'] },
      { heading: 'Build, run, push, pull', paragraphs: ['Build turns a Dockerfile and build context into image layers. Run creates a container from the image. Push uploads a manifest and missing layers to a registry. Pull downloads the layers needed by another daemon. Kubernetes usually performs the pull on a worker node rather than on your laptop.'] },
    ],
  },
  {
    slug: 'docker-container-lifecycle',
    title: 'Container lifecycle',
    category: 'containers-git-architecture',
    summary: 'Separate running, paused, stopped, and removed states so Docker commands describe an actual state transition rather than a vague restart.',
    level: 'Foundations',
    minutes: 7,
    tags: ['Docker', 'lifecycle', 'stop', 'start', 'remove'],
    domain: 'Containers',
    takeaway: 'Stopping a container keeps its writable container metadata; removing it deletes the container object, while the image can remain available.',
    image: { src: '/doc-images/image4.png', alt: 'Docker container lifecycle diagram', caption: 'Source image from look4.docx: running, paused, stopped, and removed container states.' },
    diagram: ['created', 'running', 'paused', 'stopped', 'removed'],
    codeLabel: 'terminal',
    codeLanguage: 'Docker CLI',
    codeNote: 'These commands operate on the named container. The image is a separate object and is not removed by docker rm.',
    code: `docker run --name demo example/hello:1.0\ndocker pause demo\ndocker unpause demo\ndocker stop demo\ndocker start demo\ndocker rm demo`,
    links: [
      { label: 'Docker start', url: 'https://docs.docker.com/reference/cli/docker/container/start/' },
      { label: 'Docker stop', url: 'https://docs.docker.com/reference/cli/docker/container/stop/' },
      { label: 'Docker remove', url: 'https://docs.docker.com/reference/cli/docker/container/rm/' },
    ],
    sections: [
      { heading: 'State transitions', bullets: ['run creates and starts a container.', 'pause freezes the container’s processes without removing it.', 'stop asks the process to exit gracefully before forceful termination.', 'kill sends a signal immediately and is not the same as graceful stop.', 'start starts an existing stopped container.', 'rm removes the container object; use -f only when you intentionally want to force removal.'] },
      { heading: 'Container data is not automatically durable', paragraphs: ['The writable layer belongs to the container lifecycle. If the container is removed, changes in that layer disappear. Use a named volume, bind mount, or an external data service when data must outlive the container.'] },
    ],
  },
  {
    slug: 'docker-images-layers-dockerfile',
    title: 'Docker images, layers, and Dockerfile instructions',
    category: 'containers-git-architecture',
    summary: 'Understand why Dockerfiles produce layered images and how instruction order affects cache reuse and rebuild time.',
    level: 'Foundations',
    minutes: 9,
    tags: ['Dockerfile', 'layers', 'cache', 'FROM', 'COPY'],
    domain: 'Containers',
    takeaway: 'Each Dockerfile instruction contributes to an image history; stable, expensive steps should appear before frequently changing application code.',
    images: [
      { src: '/doc-images/image5.png', alt: 'Container image layers', caption: 'Source image from look4.docx: configuration, application code, dependencies, runtime dependencies, and base image layers.' },
      { src: '/doc-images/image7.png', alt: 'Dockerfile instructions and layered image history', caption: 'Source image from look4.docx: comments, instructions, arguments, and the resulting image layer chain.' },
    ],
    diagram: ['base image', 'dependency layer', 'application layer', 'runtime container'],
    codeLabel: 'Dockerfile',
    codeLanguage: 'Dockerfile',
    codeNote: 'Copy dependency descriptors before source files when the dependency set changes less often. This makes the dependency layer cacheable.',
    code: `FROM eclipse-temurin:21-jre\nWORKDIR /app\n\n# Copy stable inputs first so dependency work can be cached.\nCOPY build/libs/app.jar app.jar\n\n# The final image runs one foreground process.\nENTRYPOINT ["java", "-jar", "app.jar"]`,
    links: [
      { label: 'Dockerfile reference', url: 'https://docs.docker.com/reference/dockerfile/' },
      { label: 'Docker build cache', url: 'https://docs.docker.com/build/cache/' },
      { label: 'Image layers', url: 'https://docs.docker.com/get-started/docker-concepts/building-images/understanding-image-layers/' },
    ],
    sections: [
      { heading: 'Important instructions', bullets: ['FROM chooses the base image and starts the build.', 'RUN executes a build-time command and records its result in a layer.', 'COPY adds files from the build context.', 'ENV records environment defaults in the image configuration.', 'CMD supplies a default command; ENTRYPOINT defines the executable behavior more strongly.'] },
      { heading: 'Layer cache is a build optimization', paragraphs: ['If an instruction and its inputs have not changed, Docker can reuse the previous result. A small source change should not force a fresh dependency download. Keep build contexts small and avoid copying secrets into image layers because deleted files can remain in history.'] },
    ],
  },
  {
    slug: 'docker-registries-tags',
    title: 'Container registries and image tags',
    category: 'containers-git-architecture',
    summary: 'Treat a registry as the distribution point for images and use tags for human navigation while digests provide immutable identity.',
    level: 'Foundations',
    minutes: 7,
    tags: ['registry', 'tags', 'digest', 'CI/CD'],
    domain: 'Containers',
    takeaway: 'Tags are movable names; digests identify exact image content. Production rollouts should make the resolved image identity observable.',
    image: { src: '/doc-images/image6.png', alt: 'Container registry repository list', caption: 'Source image from look4.docx: repository listing in a container registry.' },
    diagram: ['build', 'tag', 'push', 'registry', 'pull by deployment'],
    codeLabel: 'terminal',
    codeLanguage: 'Docker CLI',
    codeNote: 'The example uses a public-looking placeholder repository. Authenticate through your local registry workflow; never put credentials in a Dockerfile or manifest.',
    code: `docker build -t example/api:1.4.2 .\ndocker push example/api:1.4.2\n\n# Inspect the immutable digest after push.\ndocker buildx imagetools inspect example/api:1.4.2`,
    links: [
      { label: 'Docker image tags', url: 'https://docs.docker.com/reference/cli/docker/image/tag/' },
      { label: 'Docker image push', url: 'https://docs.docker.com/reference/cli/docker/image/push/' },
      { label: 'OCI image specification', url: 'https://github.com/opencontainers/image-spec' },
    ],
    sections: [
      { heading: 'Tagging strategy', bullets: ['A semantic version such as 1.4.2 communicates release intent.', 'latest is a moving alias, not a release guarantee.', 'A commit or build tag helps trace an image back to source.', 'A digest is the strongest content identity and is useful for reproducibility.'] },
      { heading: 'Registry responsibilities', paragraphs: ['Registries store manifests and layers, expose repositories, and often provide vulnerability scanning and retention policies. CI/CD commonly builds and pushes the image, while the deployment system pulls it onto the target nodes.'] },
    ],
  },
  {
    slug: 'git-clone-versus-fork',
    title: 'Git clone versus fork',
    category: 'containers-git-architecture',
    summary: 'Distinguish a local clone from a server-side fork so collaboration diagrams match the remotes developers actually control.',
    level: 'Foundations',
    minutes: 6,
    tags: ['Git', 'clone', 'fork', 'pull request'],
    domain: 'Git',
    takeaway: 'Clone copies a repository to your local machine; fork creates a separate server-side repository under another owner.',
    image: { src: '/doc-images/image8.png', alt: 'Git clone versus fork comparison', caption: 'Source image from look4.docx: clone gives multiple developers local copies, while fork adds a separate remote repository.' },
    diagram: ['upstream repository', 'fork or clone', 'local branch', 'push', 'pull request'],
    codeLabel: 'terminal',
    codeLanguage: 'Git',
    codeNote: 'The remote names are conventional. Check the actual remote URLs with git remote -v before pushing.',
    code: `git clone https://example.test/team/project.git\ncd project\ngit switch -c improve-readme\ngit add README.md\ngit commit -m "Improve README"\ngit push -u origin improve-readme`,
    links: [
      { label: 'Git clone documentation', url: 'https://git-scm.com/docs/git-clone' },
      { label: 'GitHub fork documentation', url: 'https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/about-permissions-and-visibility-of-forks' },
    ],
    sections: [
      { heading: 'Clone', paragraphs: ['Clone creates a local working copy and usually configures origin to point at the repository you cloned. Multiple developers can clone the same upstream repository, each with local branches and a path to push changes.'] },
      { heading: 'Fork', paragraphs: ['A fork creates another server-side repository, often under a developer’s account or organization. The developer clones the fork, pushes branches there, and opens a pull request back to upstream. The fork is useful when the developer does not have write permission to the original repository.'] },
      { heading: 'The common confusion', bullets: ['A clone is local; a fork is hosted.', 'A fork may later be cloned locally.', 'origin and upstream are remote names, not magic server roles.', 'Pull requests compare branches across repositories or within one repository.'] },
    ],
  },
  {
    slug: 'metadata-and-node-lifecycle',
    title: 'Metadata graphs and node lifecycle',
    category: 'containers-git-architecture',
    summary: 'Use the document’s metadata diagrams to reason about relationships, state transitions, and why a graph model can explain an evolving domain.',
    level: 'Intermediate',
    minutes: 9,
    tags: ['metadata', 'graph', 'state machine', 'lifecycle'],
    domain: 'Architecture',
    takeaway: 'A metadata model becomes understandable when relationships and state transitions are explicit rather than hidden in ad hoc fields.',
    images: [
      { src: '/doc-images/image9.png', alt: 'Metadata relationship graph', caption: 'Source image from look4.docx: marketplace, category, attribute, and node metadata relationships.' },
      { src: '/doc-images/image10.png', alt: 'Metadata and node lifecycle graph', caption: 'Source image from look4.docx: metadata relationships leading into a node lifecycle and related entities.' },
    ],
    diagram: ['marketplace', 'category', 'attribute name', 'attribute value', 'node state transitions'],
    codeLabel: 'NodeState.java',
    codeNote: 'The state transition method makes legal lifecycle changes visible and rejects accidental jumps.',
    code: `enum NodeState { DRAFT, ACTIVE, ARCHIVED }\n\nfinal class Node {\n    private NodeState state = NodeState.DRAFT;\n\n    void publish() {\n        if (state != NodeState.DRAFT) {\n            throw new IllegalStateException("only drafts can be published");\n        }\n        state = NodeState.ACTIVE;\n    }\n\n    void archive() {\n        if (state != NodeState.ACTIVE) {\n            throw new IllegalStateException("only active nodes can be archived");\n        }\n        state = NodeState.ARCHIVED;\n    }\n\n    NodeState state() {\n        return state;\n    }\n}`,
    links: [
      { label: 'Domain-driven design reference', url: 'https://martinfowler.com/bliki/DomainDrivenDesign.html' },
      { label: 'Java enum API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Enum.html' },
    ],
    sections: [
      { heading: 'Relationship model', paragraphs: ['The diagram separates a marketplace, category, attribute name, and attribute value. That separation lets one category reuse a vocabulary while individual nodes carry selected values. In code, these relationships should have explicit ownership and validation rules.'] },
      { heading: 'Lifecycle model', bullets: ['Draft is editable and not yet visible to consumers.', 'Active is published and participates in normal reads.', 'Archived is retained for history but excluded from ordinary discovery.', 'Transitions should be named operations, not arbitrary string assignments.', 'Persist both the current state and the events or audit data needed to explain how it changed.'] },
    ],
  },
  {
    slug: 'service-component-architecture',
    title: 'Service and component architecture',
    category: 'containers-git-architecture',
    summary: 'Turn the uploaded component diagrams into a practical rule: name boundaries, inputs, outputs, and the owner of each transition.',
    level: 'Intermediate',
    minutes: 10,
    tags: ['architecture', 'services', 'orchestration', 'validation'],
    domain: 'Architecture',
    takeaway: 'A useful service diagram explains who owns a decision, what data crosses a boundary, and which component can retry or reject the work.',
    images: [
      { src: '/doc-images/image11.png', alt: 'Service component architecture overview', caption: 'Source image from look4.docx: a broader service and node-management component map.' },
      { src: '/doc-images/image12.png', alt: 'Service component architecture diagram with orchestrator and validators', caption: 'Source image from look4.docx: component relationships among metadata creation, validation, orchestration, and external services.' },
    ],
    diagram: ['request', 'creator', 'validator', 'orchestrator', 'external system', 'result'],
    codeLabel: 'ValidationService.java',
    codeNote: 'The orchestration layer coordinates components; each validator owns one kind of validation and returns an explicit result.',
    code: `record ValidationResult(boolean valid, String reason) {}\n\ninterface Validator<T> {\n    ValidationResult validate(T value);\n}\n\nfinal class ValidationService<T> {\n    private final java.util.List<Validator<T>> validators;\n\n    ValidationService(java.util.List<Validator<T>> validators) {\n        this.validators = validators;\n    }\n\n    ValidationResult validate(T value) {\n        for (Validator<T> validator : validators) {\n            ValidationResult result = validator.validate(value);\n            if (!result.valid()) {\n                return result;\n            }\n        }\n        return new ValidationResult(true, "accepted");\n    }\n}`,
    links: [
      { label: 'Microsoft microservices architecture', url: 'https://learn.microsoft.com/en-us/azure/architecture/guide/architecture-styles/microservices' },
      { label: 'Martin Fowler: microservices', url: 'https://martinfowler.com/articles/microservices.html' },
    ],
    sections: [
      { heading: 'Read a diagram in four questions', bullets: ['Who receives the request?', 'Which component owns each validation or transformation?', 'Where is state persisted, and who publishes the next event?', 'What happens when an external dependency is slow or unavailable?'] },
      { heading: 'Orchestrator versus choreography', paragraphs: ['An orchestrator calls or commands other components and owns the workflow sequence. In choreography, components react to events and the overall flow emerges from subscriptions. Neither is automatically better; choose based on coupling, observability, and how often the workflow changes.'] },
      { heading: 'Keep boundaries honest', paragraphs: ['A service boundary is not proven by a box in a diagram. The code should make the boundary visible through an interface, request/response contract, event, or client. If every component shares the same database tables and internal classes, the boxes are mostly decorative.'] },
    ],
  },
  {
    slug: 'asset-fund-data-model',
    title: 'Asset and fund data model',
    category: 'containers-git-architecture',
    summary: 'Extract the document’s asset, fund, transaction, and holding entities into a small relational model with explicit cardinality.',
    level: 'Intermediate',
    minutes: 9,
    tags: ['data model', 'asset', 'fund', 'transaction', 'holdings'],
    domain: 'Data modeling',
    takeaway: 'Model facts once, connect them with keys, and keep transaction history separate from the current holding snapshot.',
    image: { src: '/doc-images/image13.png', alt: 'Asset and fund relational data model', caption: 'Source image from look4.docx: an entity relationship diagram for funds, assets, transactions, holdings, and asset lots.' },
    diagram: ['Fund', 'AssetHolding', 'Asset', 'Transaction', 'AssetLot', 'TaxRecord'],
    codeLabel: 'schema.sql',
    codeLanguage: 'SQL',
    codeNote: 'The schema keeps transactions as an event history and holdings as a current or derived position view.',
    code: `CREATE TABLE fund (\n    fund_id BIGINT PRIMARY KEY,\n    name VARCHAR(200) NOT NULL\n);\n\nCREATE TABLE asset (\n    asset_id BIGINT PRIMARY KEY,\n    ticker VARCHAR(20) NOT NULL,\n    name VARCHAR(200) NOT NULL\n);\n\nCREATE TABLE transaction (\n    transaction_id BIGINT PRIMARY KEY,\n    fund_id BIGINT NOT NULL REFERENCES fund(fund_id),\n    asset_id BIGINT NOT NULL REFERENCES asset(asset_id),\n    quantity DECIMAL(20, 6) NOT NULL,\n    transaction_type VARCHAR(30) NOT NULL,\n    occurred_at TIMESTAMP NOT NULL\n);`,
    links: [
      { label: 'PostgreSQL CREATE TABLE', url: 'https://www.postgresql.org/docs/current/sql-createtable.html' },
      { label: 'PostgreSQL foreign keys', url: 'https://www.postgresql.org/docs/current/ddl-constraints.html' },
    ],
    sections: [
      { heading: 'History versus snapshot', paragraphs: ['A transaction records something that happened, such as a buy, sell, dividend, or transfer. A holding is a current or derived answer to “what does the fund own now?” Keeping both concepts distinct prevents a mutable snapshot from erasing the audit trail.'] },
      { heading: 'Keys and cardinality', bullets: ['One fund can have many transactions.', 'One asset can appear in many transactions across funds.', 'A join table or holding entity connects a fund to an asset.', 'An asset lot can preserve acquisition-level quantity and cost basis.', 'Tax records should reference the transaction or lot they explain.'] },
      { heading: 'Queries should state their time', paragraphs: ['A current holding query and an as-of-date portfolio query have different requirements. Decide whether holdings are maintained incrementally, recomputed from transactions, or materialized periodically, then test the choice against corrections and late-arriving events.'] },
    ],
  },
  {
    slug: 'jvm-internals-and-gc',
    title: 'JVM internals and garbage collection',
    category: 'jvm-internals',
    summary: 'Connect Java source code to class loading, JIT compilation, heap allocation, garbage collection, and production evidence.',
    level: 'Advanced',
    minutes: 15,
    tags: ['JVM', 'class loading', 'JIT', 'heap', 'garbage collection', 'profiling'],
    domain: 'JVM',
    takeaway: 'The JVM is an adaptive runtime: it loads classes, allocates objects, compiles hot code, and reclaims memory according to observed behavior rather than one fixed execution plan.',
    diagram: ['.java source', 'bytecode', 'class loader', 'interpreter', 'JIT compiled code', 'heap and GC'],
    codeLabel: 'JvmObservation.java',
    codeLanguage: 'Java',
    codeNote: 'The allocation count is deterministic. Garbage collection timing is not, so System.gc() is shown only as a non-binding hint, never as a correctness mechanism.',
    code: `import java.util.ArrayList;\nimport java.util.List;\n\nfinal class JvmObservation {\n    public static void main(String[] args) {\n        Runtime runtime = Runtime.getRuntime();\n        long before = runtime.totalMemory() - runtime.freeMemory();\n\n        List<byte[]> allocations = new ArrayList<>();\n        int allocationCount = 100;\n        int bytesPerObject = 1024 * 1024;\n\n        for (int i = 0; i < allocationCount; i++) {\n            allocations.add(new byte[bytesPerObject]);\n        }\n\n        long after = runtime.totalMemory() - runtime.freeMemory();\n        System.out.println("objects allocated: " + allocations.size());\n        System.out.println("used memory increased: " + (after > before));\n        System.out.println("max heap bytes: " + runtime.maxMemory());\n\n        allocations.clear();\n        System.gc(); // Hint only. The JVM may ignore it.\n        System.out.println("references released: " + allocations.isEmpty());\n    }\n}`,
    links: [
      { label: 'Java Virtual Machine Specification', url: 'https://docs.oracle.com/javase/specs/jvms/se21/html/' },
      { label: 'Java garbage collection tuning guide', url: 'https://docs.oracle.com/en/java/javase/21/gctuning/' },
      { label: 'Java Flight Recorder', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/jdk.jfr/module-summary.html' },
    ],
    sections: [
      { heading: 'From source to running code', paragraphs: ['javac compiles source into bytecode. A class loader loads classes when they are needed, the verifier checks the bytecode, and the runtime links and initializes the class. The JVM can interpret bytecode first, then compile hot methods with the JIT when profiling shows that optimized native code is worthwhile.'] },
      { heading: 'Memory areas that matter', bullets: ['The heap stores ordinary objects and is managed by garbage collection.', 'Each thread has its own stack frames for method calls and local variables.', 'Metaspace stores class metadata outside the traditional heap and can still exhaust native memory.', 'Code caches hold JIT-compiled machine code.', 'Direct buffers and native libraries consume memory outside the Java heap.'] },
      { heading: 'How garbage collection should be explained', paragraphs: ['Garbage collection identifies objects that are no longer reachable from live roots and reclaims or compacts their storage according to the selected collector. Generational collectors exploit the fact that many objects die young, but a large live set, allocation burst, promotion pressure, pinned regions, or long references can still produce pauses or high CPU.', 'GC is not a deterministic delete call. System.gc() is only a request, finalization is not a cleanup strategy, and reducing heap usage does not automatically reduce pause time. Measure allocation rate, live-set size, pause distribution, CPU, and old-generation or region occupancy.'] },
      { heading: 'Interview diagnosis path', bullets: ['Separate high allocation from a genuinely large live set.', 'Check GC logs and Java Flight Recorder before changing flags.', 'Use a heap histogram or heap dump to find retained objects and the retaining path.', 'Check thread dumps and CPU profiles when the symptom is latency or high CPU rather than memory exhaustion.', 'Tune only after identifying the workload shape, target pause time, and available memory.'] },
      { heading: 'Common traps', bullets: ['Heap is not the same thing as process memory.', 'A larger heap can reduce collection frequency but increase worst-case work and memory pressure.', 'A memory leak in Java is usually unintended retention, not failure to call free().', 'A passing load test does not prove a production collector configuration is safe.'] },
    ],
  },
  {
    slug: 'collections-and-generics',
    title: 'Java collections and generics',
    category: 'collections-caching',
    summary: 'Choose collection implementations deliberately and use generics to make element types, variance, and complexity visible at compile time.',
    level: 'Intermediate',
    minutes: 13,
    tags: ['ArrayList', 'HashMap', 'ConcurrentHashMap', 'generics', 'type erasure', 'Comparator'],
    domain: 'Core Java',
    takeaway: 'The interface describes the operation you need; the implementation determines ordering, memory layout, concurrency behavior, and expected cost.',
    diagram: ['List / Set / Map', 'implementation choice', 'generic type contract', 'iteration and lookup'],
    codeLabel: 'CollectionsAndGenerics.java',
    codeLanguage: 'Java',
    codeNote: 'The example makes the caller choices visible: an ArrayList for ordered values, a HashMap for key lookup, and a bounded generic method for reading numeric values.',
    code: `import java.util.ArrayList;\nimport java.util.Comparator;\nimport java.util.HashMap;\nimport java.util.List;\nimport java.util.Map;\n\nfinal class CollectionsAndGenerics {\n    static double average(List<? extends Number> values) {\n        return values.stream()\n                .mapToDouble(Number::doubleValue)\n                .average()\n                .orElse(0.0);\n    }\n\n    public static void main(String[] args) {\n        List<Integer> scores = new ArrayList<>(List.of(72, 91, 84));\n        Map<String, Integer> byName = new HashMap<>();\n        byName.put("Ada", 91);\n        byName.put("Grace", 84);\n\n        scores.sort(Comparator.reverseOrder());\n        System.out.println("highest score: " + scores.get(0));\n        System.out.println("Grace score: " + byName.get("Grace"));\n        System.out.println("average: " + average(scores));\n    }\n}`,
    links: [
      { label: 'Java Collections Framework overview', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/package-summary.html' },
      { label: 'HashMap API and concurrency contract', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/HashMap.html' },
      { label: 'ConcurrentHashMap API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/ConcurrentHashMap.html' },
      { label: 'Java Generics tutorial', url: 'https://docs.oracle.com/javase/tutorial/java/generics/' },
    ],
    sections: [
      { heading: 'Start with the operation', paragraphs: ['Use List when position and iteration order matter, Set when uniqueness is the contract, Queue or Deque when processing order matters, and Map when a key identifies a value. The interface is the first design decision; the implementation follows from lookup, ordering, memory, and concurrency requirements.'] },
      { heading: 'The interview essentials', bullets: ['ArrayList gives fast indexed access and compact storage, but inserting near the front shifts later elements.', 'LinkedList is rarely the automatic answer: its nodes add memory and pointer overhead, and indexed access is linear.', 'HashMap provides expected constant-time get and put when hashes distribute well, but it is unsynchronized and has no stable iteration order.', 'TreeMap maintains sorted keys with logarithmic operations and needs a comparison strategy.', 'ConcurrentHashMap supports concurrent access patterns, but a sequence such as containsKey followed by put is not automatically one atomic operation.'] },
      { heading: 'Generics and variance', paragraphs: ['Generics move type errors to compilation and remove many casts. A parameterized type such as List<Integer> is not a subtype of List<Number>, because allowing a Number into it could insert a value that is not an Integer.', 'Use ? extends T when the method reads values as T, and ? super T when the method writes T values. This is the useful form of the producer extends, consumer super rule. Type erasure means most generic type arguments are not available at runtime, so do not expect new T() or a reliable instanceof List<String>.'] },
      { heading: 'Thread safety is a separate choice', paragraphs: ['Unmodifiable does not mean thread-safe, and a concurrent collection does not make an entire workflow atomic. Decide whether callers share the collection, whether compound operations need atomic methods such as computeIfAbsent, and whether a snapshot or explicit lock is the clearer contract.'] },
      { heading: 'Common traps', bullets: ['Mutable keys can become unreachable when their hashCode changes while stored in a hash map.', 'Fail-fast iterators are bug detectors, not synchronization.', 'Choosing a collection only by its Big-O label ignores allocation, locality, ordering, and contention.', 'Raw types throw away the compiler help that generics were added to provide.'] },
    ],
  },
  {
    slug: 'streams-and-modern-java',
    title: 'Streams and modern Java',
    category: 'modern-java',
    summary: 'Use streams, records, Optional, and newer language features to express data transformations without hiding cost or control flow.',
    level: 'Intermediate',
    minutes: 12,
    tags: ['streams', 'records', 'Optional', 'lambdas', 'parallel streams'],
    domain: 'Modern Java',
    takeaway: 'A stream pipeline describes a lazy computation; it does not make the work free, parallel, or automatically clearer.',
    diagram: ['source', 'filter', 'map', 'terminal operation', 'result'],
    codeLabel: 'ModernJava.java',
    codeLanguage: 'Java 17+',
    codeNote: 'The terminal operation triggers the lazy pipeline. The record makes the data shape explicit without ceremony.',
    code: `import java.util.List;\n\nrecord Order(String customer, int cents, boolean paid) {}\n\nfinal class ModernJava {\n    public static void main(String[] args) {\n        List<Order> orders = List.of(\n                new Order("Ada", 1200, true),\n                new Order("Grace", 800, false),\n                new Order("Linus", 2500, true)\n        );\n\n        // filter and map are lazy. sum() starts the traversal.\n        int paidCents = orders.stream()\n                .filter(Order::paid)\n                .mapToInt(Order::cents)\n                .sum();\n\n        System.out.println("paid cents: " + paidCents);\n        System.out.println("paid orders: " + orders.stream()\n                .filter(Order::paid)\n                .count());\n    }\n}`,
    links: [
      { label: 'Stream API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/stream/Stream.html' },
      { label: 'Record classes', url: 'https://docs.oracle.com/en/java/javase/21/language/records.html' },
      { label: 'Optional API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/Optional.html' },
    ],
    sections: [
      { heading: 'Pipeline shape', paragraphs: ['A stream has a source, zero or more intermediate operations, and usually one terminal operation. filter, map, sorted, and limit describe work; count, collect, reduce, and forEach request a result or side effect. Intermediate operations are lazy, which lets the implementation fuse work and stop early in some pipelines.'] },
      { heading: 'The cost is still real', bullets: ['A stream does not remove the cost of traversing, allocating, sorting, or collecting data.', 'Prefer primitive streams such as mapToInt when boxing would add noise or allocation.', 'Avoid side effects inside a pipeline when a normal loop makes ownership and ordering clearer.', 'Parallel streams use a shared execution facility by default and can hurt latency or saturate unrelated work.', 'A stream pipeline is a poor place to hide blocking I/O.'] },
      { heading: 'Modern data shapes', paragraphs: ['Records are concise, transparent carriers for data with generated accessors, equality, hashCode, and a useful toString. They are not a replacement for every mutable domain object.', 'Optional expresses a value that may be absent at an API boundary. It should not normally be used for fields, serialization models, or every local variable.'] },
      { heading: 'Interview follow-ups', bullets: ['What starts a lazy stream?', 'When does distinct or sorted need additional state?', 'What changes when the stream is parallel?', 'Why can a stream with side effects produce surprising results?', 'When is a loop more readable and easier to debug?'] },
    ],
  },
  {
    slug: 'java-exceptions-and-error-handling',
    title: 'Exceptions and error handling in Java',
    category: 'error-handling',
    summary: 'Design failure contracts with checked and unchecked exceptions, causes, try-with-resources, boundaries, and explicit recovery policy.',
    level: 'Intermediate',
    minutes: 12,
    tags: ['exceptions', 'checked exceptions', 'try-with-resources', 'suppressed exceptions', 'recovery'],
    domain: 'Core Java',
    takeaway: 'Good error handling preserves context, closes resources, distinguishes retryable from permanent failure, and never hides a failure behind an empty catch block.',
    diagram: ['operation', 'validate', 'catch at boundary', 'preserve cause', 'recover or report'],
    codeLabel: 'ErrorHandling.java',
    codeLanguage: 'Java',
    codeNote: 'The example closes the reader automatically and translates a low-level checked exception at an application boundary while preserving the original cause.',
    code: `import java.io.IOException;\nimport java.io.Reader;\nimport java.io.StringReader;\n\nfinal class ErrorHandling {\n    static String readFirstLine(Reader reader) {\n        try (reader) {\n            char[] buffer = new char[64];\n            int length = reader.read(buffer);\n            if (length < 0) {\n                throw new IllegalArgumentException("input is empty");\n            }\n            return new String(buffer, 0, length).lines().findFirst().orElse("");\n        } catch (IOException exception) {\n            // Add business context without discarding the root cause.\n            throw new IllegalStateException("could not read configuration", exception);\n        }\n    }\n\n    public static void main(String[] args) {\n        System.out.println(readFirstLine(new StringReader("mode=local\\n")));\n    }\n}`,
    links: [
      { label: 'Throwable API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/Throwable.html' },
      { label: 'try-with-resources tutorial', url: 'https://docs.oracle.com/javase/tutorial/essential/exceptions/tryResourceClose.html' },
      { label: 'Java exceptions tutorial', url: 'https://docs.oracle.com/javase/tutorial/essential/exceptions/' },
    ],
    sections: [
      { heading: 'Checked and unchecked exceptions', paragraphs: ['Checked exceptions are part of a method’s compile-time contract and must be caught or declared. Unchecked exceptions extend RuntimeException and usually represent programming errors, invalid arguments, violated state, or failures that the caller cannot reasonably recover from at that layer.', 'The choice is not a moral category. Choose the contract that makes the caller’s recovery decision explicit. Do not wrap every exception in RuntimeException just to make the compiler quiet.'] },
      { heading: 'Catch at the boundary that can act', bullets: ['Catch an exception where you can recover, translate it, add useful context, or decide the response.', 'Do not catch Exception broadly unless the boundary is deliberately protecting a thread, request, or process.', 'Preserve the original exception as the cause when translating layers.', 'Never log and rethrow without adding context or deciding who owns the log; duplicate logging creates noisy incidents.', 'Do not use exceptions for ordinary expected branching when a result type or validation outcome is clearer.'] },
      { heading: 'Resource cleanup', paragraphs: ['try-with-resources closes every AutoCloseable resource in reverse declaration order. If the body and close operation both fail, the close failure is attached as a suppressed exception to the primary failure. This is safer than hand-written finally blocks that can skip cleanup or overwrite the real cause.'] },
      { heading: 'Retry and recovery policy', bullets: ['Retry only failures that are transient and only when the operation is safe to repeat or has an idempotency key.', 'Use bounded attempts, backoff, and a deadline. An unbounded retry loop is an outage multiplier.', 'Translate low-level failures at an API boundary into a stable domain or transport error without leaking implementation details.', 'Return enough context for logs and metrics, but never put secrets or full sensitive payloads into exception messages.'] },
      { heading: 'Common interview traps', bullets: ['finally can replace an earlier return or exception if it returns or throws its own exception.', 'An empty catch block turns a failure into a mystery.', 'throw new Exception loses useful type information and often creates a weak API contract.', 'A stack trace is evidence, not a recovery strategy.'] },
    ],
  },
  {
    slug: 'java-rest-clients',
    title: 'REST clients in Java',
    category: 'api-clients',
    summary: 'Compare Java REST clients by blocking model, HTTP features, type safety, streaming, resilience, and framework coupling.',
    level: 'Intermediate',
    minutes: 16,
    tags: ['HTTP', 'REST', 'HttpClient', 'RestClient', 'WebClient', 'OpenFeign', 'OkHttp'],
    domain: 'Backend integration',
    takeaway: 'Choose a REST client from the application’s execution model and operational needs, not from the shortest request snippet.',
    diagram: ['application code', 'client abstraction', 'connection pool', 'HTTP/1.1 or HTTP/2', 'remote service'],
    codeLabel: 'JdkHttpClient.java',
    codeLanguage: 'Java 11+',
    codeNote: 'The JDK client is the dependency-free baseline. The comparison in the surrounding sections explains when a higher-level or reactive client is a better fit.',
    code: `import java.net.URI;\nimport java.net.http.HttpClient;\nimport java.net.http.HttpRequest;\nimport java.net.http.HttpResponse;\n\nfinal class JdkHttpClient {\n    public static void main(String[] args) throws Exception {\n        HttpClient client = HttpClient.newBuilder()\n                .version(HttpClient.Version.HTTP_2)\n                .build();\n\n        HttpRequest request = HttpRequest.newBuilder()\n                .uri(URI.create("https://example.com/health"))\n                .timeout(java.time.Duration.ofSeconds(2))\n                .header("Accept", "application/json")\n                .GET()\n                .build();\n\n        HttpResponse<String> response = client.send(\n                request, HttpResponse.BodyHandlers.ofString());\n\n        System.out.println("status: " + response.statusCode());\n        System.out.println("body length: " + response.body().length());\n    }\n}`,
    links: [
      { label: 'Java HttpClient API', url: 'https://docs.oracle.com/en/java/javase/21/docs/api/java.net.http/java/net/http/HttpClient.html' },
      { label: 'Spring RestClient reference', url: 'https://docs.spring.io/spring-framework/reference/integration/rest-clients.html' },
      { label: 'Spring WebClient reference', url: 'https://docs.spring.io/spring-framework/reference/web/webflux-webclient.html' },
      { label: 'Apache HttpClient 5', url: 'https://hc.apache.org/httpcomponents-client-5.3.x/' },
      { label: 'OkHttp documentation', url: 'https://square.github.io/okhttp/' },
      { label: 'MicroProfile Rest Client', url: 'https://microprofile.io/specifications/rest-client/' },
    ],
    sections: [
      { heading: 'The client comparison', bullets: ['JDK HttpClient: standard-library, HTTP/1.1 and HTTP/2, synchronous send, CompletableFuture-based sendAsync, WebSocket support, and no framework dependency. It is a strong default for a small service or library.', 'Spring RestClient: synchronous fluent API with Spring converters, message codecs, filters, and a modern API for new blocking Spring applications. It is the current choice for new synchronous Spring code; RestTemplate is mature and largely maintenance-oriented.', 'Spring WebClient: non-blocking and reactive, supports streaming and Reactor composition, and fits applications already using WebFlux or reactive backpressure. Do not choose it only because asynchronous code sounds faster.', 'JAX-RS Client or MicroProfile Rest Client: standardized or declarative APIs that fit Jakarta EE and MicroProfile runtimes. MicroProfile Rest Client maps typed Java interfaces to HTTP calls and integrates with configuration and fault-tolerance features in supported runtimes.', 'OpenFeign: declarative interface clients with encoders, decoders, interceptors, and ecosystem integrations. It is convenient in Spring Cloud systems, but the generated network behavior still needs timeouts, metrics, retries, and tests.', 'OkHttp: focused HTTP transport with connection pooling, HTTP/2, WebSocket, interceptors, synchronous and asynchronous calls, and a small surface. Retrofit builds a typed REST API layer on top of OkHttp.', 'Apache HttpClient 5: mature low-level client with detailed connection, proxy, TLS, authentication, synchronous, and asynchronous controls. It is useful when transport policy matters more than a minimal API.', 'Vert.x Web Client: asynchronous, event-loop-oriented client that fits Vert.x applications and should not be casually mixed with blocking calls on event-loop threads.'] },
      { heading: 'Features that actually matter', bullets: ['Timeouts: connect, request, read, and overall deadline. A client without a deadline can pin threads until the remote system fails.', 'Connection pooling: reuse connections, cap per-host concurrency, and close idle connections according to service behavior.', 'HTTP protocol: HTTP/2 multiplexing can change connection and latency behavior; negotiate deliberately and measure.', 'Serialization: decide how JSON, validation errors, unknown fields, dates, and polymorphic payloads are handled.', 'Retries: retry only safe or idempotent operations, with bounded exponential backoff and jitter.', 'Resilience: circuit breakers, bulkheads, rate limits, and fallback belong around a client boundary, not hidden in random call sites.', 'Observability: record method, route template, status family, latency, timeout, retry count, and safe correlation identifiers. Never log authorization headers or full sensitive bodies.', 'Testing: use a local fake server, contract tests, and Testcontainers when the real boundary matters.'] },
      { heading: 'How to choose', paragraphs: ['Use the JDK HttpClient when you want a standard, dependency-free baseline. Use Spring RestClient when the application is blocking Spring MVC and benefits from Spring’s request and conversion infrastructure. Use WebClient when the whole path is reactive and non-blocking. Use a declarative client when interface-driven API definitions improve consistency across many services, but keep the transport and resilience policy observable.', 'The client is not the reliability policy. Configure a deadline, connection limits, error mapping, metrics, and retry rules explicitly. A beautifully fluent client can still create a small distributed denial-of-service against your own dependency if every timeout retries forever.'] },
      { heading: 'Interview follow-ups', bullets: ['What happens when the remote service accepts the request but the client times out?', 'How do you prevent retries from duplicating a payment or order?', 'Where do you enforce a per-dependency concurrency limit?', 'How do you distinguish a 4xx business rejection from a 5xx transient failure?', 'How would you test connection reuse, timeout behavior, and malformed responses?'] },
    ],
  },
]

export function subjectsForCategory(categoryId: string): Subject[] {
  return subjects.filter((subject) => subject.category === categoryId)
}

export function findSubject(slug: string): Subject | undefined {
  return subjects.find((subject) => subject.slug === slug)
}